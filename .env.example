# Northwest Custom Apparel Pricing Tool Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# API Configuration
# API_BASE_URL is set in server.js

# File Monitoring System (Development Only)
# Set to 'true' to enable file access monitoring and safety tools
# This adds overhead and should NOT be used in production
# Requires devDependencies: puppeteer and axios (npm install --dev)
ENABLE_MONITORING=false

# To enable monitoring:
# 1. Copy this file to .env
# 2. Set ENABLE_MONITORING=true
# 3. Run: npm install --dev (to install puppeteer and axios)
# 4. Start server: npm start
# 5. Access monitoring at: http://localhost:3000/api/monitor/stats
# See MONITORING_SETUP.md for detailed instructions

# Stripe Payment Integration (Phase 2 - 3-Day Tees)
# Get these from: https://dashboard.stripe.com/apikeys
# IMPORTANT: Never commit actual keys to Git!

# Stripe Test Keys (for development)
STRIPE_TEST_PUBLIC_KEY=pk_test_your_test_public_key_here
STRIPE_TEST_SECRET_KEY=sk_test_your_test_secret_key_here

# Stripe Live Keys (for production - KEEP SECURE!)
STRIPE_LIVE_PUBLIC_KEY=pk_live_your_live_public_key_here
STRIPE_LIVE_SECRET_KEY=sk_live_your_live_secret_key_here

# Stripe Webhook Secret (from webhook settings)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Environment (development | production)
# Controls which keys are used
STRIPE_MODE=development