# AE Mission Control (2026-07-19)

Per-AE cockpit for Taneisha Clark & Nika Lao (admin view-as). One page, identity-driven — never per-rep page copies.

## Surface
- Page: `/dashboards/ae-mission-control.html` + `dashboards/css|js/ae-mission-control.*` (dash-page conventions; prefix `aemc-`).
- Zones: action queue (overdue / due-today / new-untouched leads via `lead-followup-digest.js` `buildDigestModel` reuse · stale quotes (open, quiet ≥5d) · art awaiting approval · kits pending in Mikalah's queue) → KPI strip (YTD/MTD from `NW_Daily_Sales_By_Rep`, open quotes count+$, online-store commission QTD, lead win rate 90d) → My Leads/Quotes/Art/Orders panels (top 6 + view-all) → quick-actions rail (4 builders, **Quick Quote `/calculators/quick-quote/`** — Erik request 7/19, kit-request modal, one-click outreach modal, My Accounts (taneisha-/nika-crm), scorecard, commission-structure) → SanMar inbound-today card **rep-scoped free** (`o.salesRep` is already on `/api/sanmar-orders/inbound-today` orders; full view = `openInboundTodayModal()`).
- Art toasts: 45s poll `GET {API}/api/art-notifications?since=` (same contract as ae-dashboard.js); badge = awaitingApproval count.
- UI harness: `tests/ui/test-ae-mission-control.html` + `-stub.js` (admin session, per-rep fixtures, XSS probe).

## Data path (ONE call)
- Proxy `src/routes/ae-dashboard.js` → `GET /api/ae-dashboard/summary?email=` (**secret-only**): `Promise.allSettled` over 7 Caspio reads → `{rep, kpis, actionQueue, counts, panels, orders30Total, errors?}`; failed source ⇒ `errors.<key>` + null panel (page renders visible per-panel error — never blank). 3-min TTL cache per rep + `?refresh=1` (30s throttle). ~7 reads/miss ⇒ single-digit K calls/month.
- FE `server.js` forwarder `GET /api/crm-proxy/ae-dashboard/summary` (`requireCrmRole(['taneisha','nika','admin'])`): **rep email derived from SAML session**; `?viewAs=` honored ONLY for admin (view-as pill). Commission $ never reaches non-AE staff.
- Page route hard-gated like taneisha-crm (`requireCrmRole(['taneisha','nika'])`; admin auto-passes via permissionsFromRole). **AEs land here post-SAML** when relay is the default `/staff-dashboard.html` (explicit `next=` untouched; admins keep staff dashboard).
- In-page actions reuse existing forwarders: kit POST `/api/crm-proxy/marketing-shipments` (Requested_By session-stamped; `submissionId:''` OK), outreach `/api/crm-proxy/lead-outreach` (4 template keys mirror `lead-outreach-templates.js`), stale-quote "Reopen" = builder `?duplicate=<QuoteID>` via prefix map (EMB/EMBC/CEMB/SPC/SSC/DTF/DTG).

## Rep identity (the landmine — see LESSONS 2026-07-19)
- `AE_REGISTRY` in `ae-dashboard.js` = THE server-side email→{fullName, firstName} map (sync w/ FE `leads-common.js EMAIL_TO_REP`). New AE = row here + `REP_PERMISSION_BY_EMAIL` (lib/staff-saml.js) + EMAIL_TO_REP.
- Shapes: Form_Submissions/ORDER_ODBC/NW_Daily_Sales = full name; Quote_Sessions = `SalesRepEmail/Name` (builders default `sales@` when rep unpicked → panel = *attributed* quotes only); ArtRequests = full OR first name (filter ORs both); `SALES_REP_MAP` "Taneisha Jones" was a push BUG → fixed to Clark 7/19.
- New proxy filters shipped with this: form-submissions `?salesRep=`, quote_sessions `?salesRepEmail=`/`?salesRepName=`.

## Bonus & Commission card (added 2026-07-19, same day)
- MC card + KPI read **`Commission_Payouts`** (payroll system of record; daily 3PM-UTC `sync-commissions.js` refreshes current quarter; Approved/Paid rows locked). ONE Caspio read replaces the in-process InkSoft recompute. Aggregate response: `bonus{previousQuarter rows/total/allPaid, current, paidYtd}`; KPI = full current-quarter bonus (Online Store + Garment Spiff + Win-Back).
- Card links to the Flask Bonus Dashboard (`inksoft-transform…/commissions/{nika|taneisha}`); annual retention/growth/new-business tiers stay December-only.
- **Win-Back Bounty fix (commission-payouts.js)**: was YTD-cumulative every quarter (Q2 would re-pay Q1 — caught pre-payroll 7/19). Now current quarter = live YTD − prior quarters' `Revenue_Base`; a CLOSED quarter reports its frozen row (`source:'stored-row'`) because account tables only hold live YTD. `/api/commissions/win-back` endpoint stays cumulative by design.
- Bonus plan sources: `config/online-store-commission-config.js` (1%/5%/3%, baselines N $45,814 / T $51,582 per qtr, setup bonuses $250/$100 at $2,500) + `config/garment-tracker-config.js` (per-quarter spiffs) + tiers hardcoded in commission-payouts.js + rep PDFs in Python Inksoft static/. ⚠ setup bonuses only computed in `/online-store-commissions/detail` — NOT in the payout path; both 2026 bonuses were added manually as `Setup Bonus` rows on the Q2 payout (Erik-approved 7/19; `setupBonusPaid:true` in config guards double-pay). Final corrected Q2: Taneisha $785.61, Nika $231.22 — all 8 rows Paid, Pay 15, paycheck 2026-07-24.
- **Flask Bonus Dashboard (`Python Inksoft/web/templates/commissions.html`) fixes 7/19**: Mark Paid never sent `paycheckDate` (column nulled every time) + payroll # defaulted to stale "7" (caused a wrong Pay-7 entry) — both fixed; Paid rows now have an admin **Edit** button (re-saves via the same `/commissions/mark-paid`, which updates by ID regardless of status). History date column shows Paycheck_Date.
- ⚠️ **The two bonus surfaces use DIFFERENT total formulas — reconcile, don't assume equal (Erik flagged 7/21)**: Flask `/commissions` shows `quarterlyTotal` from proxy `/api/commissions/quarterly-report`, defined as EXACTLY `online.totalCommission + spiffs.totalBonus + winBackData.bountyAmount` (commission-payouts.js:672) — the 3 COMPUTED types only. MC sums ALL current-quarter `Commission_Payouts` rows type-agnostically (ae-dashboard.js:251-259), so it includes the manual `Setup Bonus` row. Result: the two totals differed by exactly the setup bonus (Nika $100, Taneisha $250) while the 3 shared components matched to the penny (cron materializes the same computation). The delta = Σ current-quarter payout rows whose type ∉ {Online Store, Garment Spiff, Win-Back Bounty} — grows if any future manual/annual row lands in-quarter.
- **FIX 7/21 (both surfaces now show the full paycheck total)**: (1) `commissions.html` `renderRepReport(…, history)` folds manual-adjustment rows already bundled in `data.paymentHistory` (filter: rep + quarter + type ∉ 3 computed) into the displayed total + adds a "🎉 Setup Bonus & Adjustments" section + breakdown line — NO proxy change, NO new API call, NO double-count risk (cron writes only the 3 computed types from their own fields, never `quarterlyTotal`; MC reads the table directly, unaffected). **Deployed Heroku inksoft-transform v268; verified live: Nika $231.22, Taneisha $785.61.** (2) MC `ae-mission-control.js` footnote now lists the component names actually present in the payout rows (was hard-coded to 3, undercounting) + tags non-computed rows "manual adjustment" — committed develop `ac19ae0c`, deploy pending (MC totals were already correct; label-only change).

## Growth radar "Money on the Table" + CRM sync review (2026-07-19 evening)
- **Radar**: proxy `GET /api/ae-dashboard/growth?email=` (secret, 6h cache) mines 24-mo ORDER_ODBC per rep — (1) rhythm break: ≥1.6× past the account's OWN median reorder gap (est $ = its avg order); (2) season ahead: spend in the same upcoming 45-day window LAST year, nothing recent. MC card renders top 12. Live first run: Nika ~$262K/189 accounts, Taneisha ~$15.8K/28. Attribution = order-time `CustomerServiceRep` snapshot (radar-grade, noted in code).
- **CRM sync findings** (rep-crm/house pages): `sync-crm-dashboards.js` was designed for Heroku Scheduler but NEVER scheduled → "16d ago" staleness + the "$X pending account sync" gaps. FIXED: in-dyno cron daily 6 AM Pacific (proxy server.js, spawns the script). Tier cards/bonus sum stale `YTD_Sales_2026`; headline reads fresh `NW_Daily_Sales_By_Rep` — gap line discloses the diff.
- **rep-crm win-back tile FIXED**: was 5%×full-YTD under a "Qx payout" label (4th surface with the cumulative bug) → now fetches `/api/commissions/quarterly-report` (the payroll engine) and shows the quarter-true bounty; REP_CONFIG.repName is FIRST name, report keys FULL name (prefix-match).
- **house-page assignToRep FIXED**: wrote only the Caspio rep table → next sync-ownership reverted it. Now also PUTs `sales-reps-2026` (same contract as reconcile dropdown). DURABLE ownership home is still ShopWorks `Cust.CustomerServiceRep` (ODBC agent re-mirrors on that customer's next ShopWorks change) — write-back agent = future work.
- Ownership chain: ShopWorks Cust → (bandit ODBC 15-min) → `Sales_Reps_2026` → (sync-ownership) → rep account tables; tier = human-set `CustomField01` in ShopWorks. Order rows' CustomerServiceRep = stale snapshot, never used for attribution.
- **🔴 ShopWorks WRITE-BACK IS NOT POSSIBLE (Erik, 2026-07-19) — never propose it.** Manual keying is the workflow; the tooling for it = **ShopWorks To-Do** (house page button + `GET /api/assignment-history/shopworks-todo`): dashboard assignments log `Account_Assignment_History` rows (pending until latest row per customer is SYNC-source); `sync-sales-reps` writes confirm/⚠revert rows when the ODBC agent posts that customer (Erik's ShopWorks edit triggers it ≤15 min → auto-clears); Copy-list + Mark-done/Accept buttons. Round-trip verified live. Also fixed: the FE `/api/crm-proxy/assignment-history` forwarder NEVER EXISTED — house-page audit logging had 404'd silently since it shipped.

## Purchasing tracker "My Purchasing" (2026-07-19 late)
- Proxy `GET /api/ae-dashboard/purchasing?email=` (secret, 15-min cache): JotForm **Purchasing form 241646601815152** (AE requests to Bradley; fields `yourEmail`, `Order_N` = ShopWorks WO#s, `typeA42` = PO#, `typeOf`) via `fetchJotformSubmissions` (`utils/jotform.js`, JOTFORM_API_KEY) → filter by rep email → join `PurchaseOrders` mirror (`id_Order` IN chunks; `date_POIssued`/`date_Received`/`sts_Received`/`VendorName`) + `ORDER_ODBC` flags. Status ladder: sent → ordered → partial/received → invoiced → shipped. MC card + forwarder; card links to the JotForm inbox.
- 🔑 JotForm answers: match `PO #` by TEXT (`typeA42` is autogen name); date/upload answers are objects — skip non-strings. Live: Nika 126 / Taneisha 76 requests in 60d.
- **SanMar invoice viewer = ONE shared component** (`shared_components/js/sanmar-invoice-viewer.js` + `.css`, `window.SanMarInvoiceViewer.open({wo,company,pos,orderedDate})`): paper-style on-screen invoice + Print/Save-PDF (print-sheet pattern), used by BOTH the Purchasing Portal Invoice column and the MC purchasing-card Invoice buttons — never fork a second copy. Feed: proxy `/api/sanmar-invoices/by-po/:po?orderedDate=` — **FIXED 7/19: had NEVER worked** (`GetInvoiceByPurchaseOrderNo` singular not in SanMar's XSD; plural op = "Unauthenticated" for our account) → now GetInvoicesByInvoiceDateRange window [issue,+45d] filtered to the PO. SanMar PO number == ShopWorks `ID_PO` (verified live: PO 113588 → inv 162092637). SanMar_Invoices Caspio mirror is STALE (nightly sync unscheduled, last April) — don't read it.
- **Purchasing Portal (company-wide)**: `/dashboards/purchasing-portal.html` (any staff; tile in staff-dashboard Frequently Used; MC card links to it) ← FE `/api/crm-proxy/purchasing-portal` (requireStaff) ← proxy `GET /api/ae-dashboard/purchasing-all` (`buildPurchasing(null)` = all requesters, cap 250, own cache key). Table: WO/company/requester/sent/type/PO#/vendors/ordered/received/status + search + requester/status filters. Harness `tests/ui/test-purchasing-portal.html`.

## Data-quality radar "Missing Info — Fix in ShopWorks" (2026-07-19 late)
- Proxy `GET /api/ae-dashboard/data-quality?email=` (secret, 10-min cache): scans rep's ORDER_ODBC orders by `date_OrderPlaced` ≥ 30d for missing essentials — ContactFirst/Last/Phone/Email, `Invoice_AddressBlock_Shipping`, `TermsName`, `date_OrderRequestedToShip` (missing / before placed / >1yr out), and tax sanity (`cur_Taxable01`>0 + `cnCur_SalesTaxTotal`=0 + customer not exempt). Joins `id_Customer` → **CompanyContactsMerge2026** (Cust mirror) for customer-setup gaps: `Customer_Type`, phone (`Company_Phone`/`Phone_Best`), `Payment_Terms`, `Has_Complete_Address`, `Is_Tax_Exempt` w/o `Tax_Exempt_Number`. MC card + forwarder `/api/crm-proxy/ae-dashboard/data-quality` (taneisha/nika/admin, ?viewAs=).
- 🔑 **An order is LISTED only with ≥1 err-severity issue** — blank ship-to alone is a warn (pickup orders are normal; without the filter 80/125 of Nika's orders flagged = noise). Warns ride along as context chips.
- 🔑 **OPEN, non-webstore orders ONLY (Erik 7/20).** `isActionable(o)` in `buildDataQuality` drops `sts_Invoiced`/`sts_Shipped`=1 (closed, re-keying pointless) AND `DQ_WEBSTORE_ORDER_TYPES = {31 Inksoft, 6 Online Store, 34 Shopify}` (contact/ship data lives on the storefront, never keyed into ShopWorks) — filtered BEFORE the customer join so customers are scoped to fixable orders. `id_OrderType` added to select. Caps raised to 100 (card paginates). Live after filter: **Nika 53→0** (27/28 phone-blank orders were InkSoft!), **Taneisha 35→17** + 14 customers. Deployed b2fa1c4.
- ✅ Ship METHOD now literal (concurrent 7/20 session): `Addr.ShipMethod` attached by bandit sync → `dqOrderIssues` flags "method chosen but blank ship-to" as err (`cur_Shipping>0` demoted to fallback). Both edits co-shipped in b2fa1c4 (their sync-orders.ps1 + Caspio col + migration left for that session).
- 🖥️ **FE = show-5 accordion + whole-card collapse (7/20, commit b1e5167d on develop, DEPLOY PENDING).** Shared `expandableRows(items, renderItem(it,hidden), {visible:5,noun})` + one delegated `.aemc-more-toggle` handler = "Show N more" on Missing Info (orders+customers), Purchasing, Money-on-the-Table, Due Dates. `wireCardCollapse()` = header-click collapse of the 6 secondary cards (`.aemc-collapsible` + `data-collapse-key`, localStorage `aemcCollapsed`; Needs-You-Today + KPIs never collapse). Page 3426→1841px collapsed. Bumped `?v=2026.07.20.1`. ⏳ Held from prod deploy: concurrent session's bandit-integration.html + SHOPWORKS_ODBC doc edits uncommitted in the shared checkout (`git add -u` would sweep them — 7/19 outage pattern); deploy once they clear.

## Order Due Dates card (2026-07-19 late)
- Proxy `GET /api/ae-dashboard/due-dates?email=` (secret, 10-min cache) + FE forwarder `/api/crm-proxy/ae-dashboard/due-dates` (same taneisha/nika/admin gates + viewAs) + MC card between My Purchasing and Money on the Table.
- Logic: rep's UNSHIPPED ORDER_ODBC orders (dedupe by ID_Order) vs `date_OrderRequestedToShip` (60d lookback, nulls excluded — data-quality radar owns missing dates). **Late** = due date passed, not shipped. **At risk** = due ≤7d AND blanks not fully received per `PurchaseOrders` join (`sts_Received`/`date_Received`; blanks ladder none→ordered→partial→received). Due-soon orders with blanks received = on-track count only. Rows reuse `aemc-purch--*` chips for blanks + new `aemc-due-flag--late/risk` chips.
- Per-rep separation is inherent: rep derived from SAML session, admin `?viewAs=` toggles Taneisha/Nika — same identity path as summary.

## TABBED REDESIGN (2026-07-26) — spine + 5 lazy tabs

Erik's ask: clear/concise/fun/modern, "not crammed onto one scrollable page, use tabs". The 16 cards had grown onto one ~3,400px scroll and four of them were facets of the same Q3 bonus.

**Shape**: greeting → since-last-looked diff → **SPINE** (bonus hero + YTD/MTD/streak + 30-day sparkline; hero stays IN FLOW at ~229px measured and a 48px `#mc-condensed` bar takes over on scroll via one IntersectionObserver — sticking the full hero would eat ~28% of an 800px viewport) → action rail → **sticky tablist** → 5 `role="tabpanel"` sections.

| Tab | Holds |
|---|---|
| **Today** (default) | The One Thing · `#aemc-queue` · `#aemc-due` · `#aemc-dq` · `#aemc-inbound` · `#aemc-purch` |
| **Money** | `#mc-kicker` team bar · commission tile · `#aemc-bonus-*` · `#aemc-earned` · `#aemc-targets` |
| **My Book** | `#aemc-growth` (+ P4: account list + season strip) |
| **Pipeline** | `#panel-leads/-quotes/-art/-orders` · open-quotes + win-rate + `#mc-conv` |
| **Wins** | `#mc-records` · `#mc-wonback` · `#mc-photos` |

- **Router**: NEW shared `shared_components/js/dash-tabs.js` (`window.DashTabs`) — first shared tab impl; 7 pages had each hand-rolled one. Full APG keyboard, roving tabindex, `1`-`9` **scoped to the tablist** (no global handler → can't hijack the kit modal's 9 text fields). `onActivate` debounced 250ms, visual switch synchronous.
- **Registry**: `TABS` in `ae-mission-control.js` holds function REFERENCES, so every existing `loadX`/`renderX` body was reused untouched. `mountTab` fires a tab's loaders on FIRST activation; `renderMountedTabs` re-runs renderers for already-opened tabs on rep-switch/Refresh. **Boot = 8 requests (only Today's), was 9 for all 16 cards**; also drops concurrent pressure on the 15s global fetch timeout from 9 to 5.
- **Hash**: `#tab=today|money|book|pipeline|wins`, `replaceState`, tolerant parse (bare `#money` aliases; unknown id → today SILENTLY, a stale link must not error-banner). `&focus=<id>` is preserved on write and reserved for the 7:45 digest deep link (parse ships, behavior is P5). `embroidery-bonus-plan.html` links updated to `#tab=money`.
- **Storage**: `aemcLayoutVersion='2'` (one-time `aemcCollapsed` reset — both reps had collapsed cards since 7/20 and would otherwise open Today to a stack of empty headers) · `aemcLastSeen:<repEmail>` (diff BASELINE only) · `aemcCelebrated:<repEmail>` · `aemc.onething.skipped.v1.<repEmail>` (14-day TTL). **Active tab is deliberately NOT persisted** — no hash always means Today.
- **Collapse scope shrank**: tabs replace whole-card collapse. `.aemc-collapsible` kept only on `#aemc-earned` + `.aemc-emb-card` (long reference cards inside a frequently-visited tab).
- 🔑 **`renderKicker` takes the TOP-LEVEL `teamKicker`, not `mine.teamKicker`** — the rep object is only `{rep, accounts, counts, bounties, ladder, totalBonus}` (verified live). The old `mine.teamKicker` read was ALWAYS undefined, so a rep clearing the top rung was told "Every milestone cleared this quarter" while the kicker was wide open, and the explainer silently kept its hardcoded `$740,000` instead of live Caspio config.
- 🔑 **Zero new Caspio calls for the whole redesign.** `trend{dailySeries,streak,records}` and `quotes.conversion` are derived in `fetchSales`/`fetchQuotes`, which already read every row and threw the detail away. The One Thing, seasonality, and the diff line are all client-side over data already fetched. Matters more than expected: the 2026-07 invoice billed **138% of cap / $358 overage**.
- ⚠️ **`Quote_Sessions` is effectively empty** — 8 rows for all of 2026, ONE with a `SalesRepEmail` (Erik's). So Open Quotes reads `0 · $0` and `quoteConversion.attributed` is 0 for both reps; the Pipeline tile says why instead of printing a meaningless 0%. Root cause is a separate spawned task.
- ⚠️ **Records are 2026-only** — `NW_Daily_Sales_By_Rep` starts 2026-01-05, no 2025 rows. UI must say "since the daily archive began", never "all-time"/"personal best". Streak = consecutive BUSINESS days with an invoiced order, as of the archive date (weekends skipped, not breaking).
- Harness: mirrors `<main>` verbatim, **must be edited in the same commit** (it fails silently). `?as=rep|nika` = non-admin session; `?degrade=trend,quotes,photos,empty-quotes,empty-photos` = null/zero shapes.

## Phase 4-5 backlog (not built)
- **My Book depth**: NEW `GET /api/crm-proxy/my-accounts` **façade** (resolve the table from the session + admin-only viewAs). 🔴 Do NOT widen the `['nika']`/`['taneisha']` gates on `/{rep}-accounts` — that would let each rep read the other's whole book incl. per-account revenue and margin, with a shared bonus pool. Admin already holds both role strings (`lib/staff-saml.js:133`), so the gate was never the blocker; the problem is the client choosing the table. Renders `Health_Score`, `Predicted_Next_Order`, `At_Risk`, `Avg_Order_Value`, `Margin_Pct` from the 86-column table the legacy CRM pages use ~15 of. **Harden in the same pass**: `q.orderBy=CompanyName ASC` + `defaultLimit 1000` + `maxPages 20` is 143 rows from silently dropping accounts (Taneisha is at 857), and `?search=` interpolates unescaped (`nika-accounts.js:129`).
- **Seasonality strip**: client-derived from `Jan_Active..Dec_Active`/`Primary_Month`. A 12-bar strip on a shared baseline, NOT a radar (twelve ORDERED months; a radar's area encodes nothing and makes Dec look adjacent to Jan).
- **Same-quarter-last-year compare**: forwarder already passes `quarter`/`year`, but 🔴 **the number is wrong until `EMB_ORDER_TYPES` includes retired type 1 for quarters ending ≤2025-09-30** (Q3-2025 ended at the Caps merge boundary, so it silently excludes every caps order and the YoY card would flatter 2026), and 💸 closed quarters cache at `TTL_LIVE` 5min though they can never change (`hist:` runs `pullOrders` at maxPages 60 — thousands of calls/month for a frozen number; needs a 24h closed-quarter TTL AND a lazy once-per-session fetch). Compare **revenue and account counts only, never reconstructed bonus dollars** — no `Rep_Bonus_Config` row exists for Q3 2025, so `configSource:'fallback'` would apply 2026 rates to 2025 data. Caption it "what would last year look like with today's book?" (rebuilding on current ownership moved Nika's 2024-Q3 down $79,430 and Taneisha's 2025-Q3 up $63,105).
- ❌ **Don't wire `/embroidery-bonus/dormant`** — it looked free (registered, no consumer) but `targets.winBack[]` is the same population already scored and ranked, and `targets.summary.winBackCount` supplies the only unique output. Two round-trips for two lists of the same accounts.
- Still open from Phase 3: morning Slack/email deep link (`#tab=<id>&focus=<id>`) · absorb taneisha-/nika-crm entirely · move rep access from email-keyed perms to a `sales` role + `Sales_Reps_2026` registry.
