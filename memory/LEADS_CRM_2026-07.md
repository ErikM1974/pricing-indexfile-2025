# Leads CRM — capture + Salesforce-lite pipeline (built & shipped 2026-07-18)

One system, two layers, all live in production. Frontend releases 1689–1696 (v2026.07.18.2→.12); proxy v2026.07.18.2→.7 (`d3f66dd`→`e6feff6`).

## Layer 1 — Unified capture (JotForm + in-app → `Form_Submissions`)

- **Single table**: everything is a `Form_Submissions` row. Lead formIds: `jotform-lead` (JFL) + `quote-request` (QRQ) + `webstore-request` (WSR) + `team-roster` (RST). NO separate Leads table — deliberate.
- **JotForm ingest** (6 forms: Leads NWCA #1 `21764724640151`, NW Embroidery Info Request `220514824751149`, Leads Decosource, Apparel Leads #1, Webstore Contact, Franchise Inquiry): webhooks registered on all 6 → proxy `POST /api/jotform/webhook?secret=` (`JOTFORM_WEBHOOK_SECRET`; the JotForm **API key** lives in `JOTFORM_API_KEY` — they got swapped once, fixed v953).
  - **Webhook is REST-first**: rawRequest LACKS upload URLs — handler fetches `GET /submission/{id}` via the API and normalizes `answers` (rawRequest = fallback only). `src/utils/jotform.js` + `src/routes/jotform.js`.
  - Dedupe = `External_ID` (JotForm submissionID); `External_Source` = `jotform:{formID}`; live IDs `JFL{MMDD}-{rand4}`, backfill `JFL{MMDD-of-original}-{4-from-subID}`.
  - Daily reconcile backstop: Heroku Scheduler `npm run jotform-reconcile` (7:30 UTC) → `POST /api/jotform/sync`.
  - **Backfill DONE**: ~1,766 rows via CSV import (`scripts/backfill-jotform-csv.js` → Caspio UI import; $0 Integrations quota); >60d rows = Archived; offline AE assignment applied.
- **Routing rule (Erik)**: exact-email match in `CompanyContactsMerge2026` → that customer's AE + `Matched_ID_Customer` auto-linked; else **Taneisha Clark**. Lookup failure defaults rep — never drops a lead.
- **Notifications on arrival**: Slack `#form-leads` card (rep + source shown) + EmailJS `template_new_lead` to the assigned rep (`send-lead-email.js`; Reply-To = the prospect; Erik Cc'd).
- **Attachments**: `GET /api/jotform/file?u=` staff passthrough streams JotForm uploads with the API key (`requireCrmSecretOrBrowserOrigin` + strict upload-host allow-list; MIME inferred from extension — JotForm serves octet-stream); `?download=1` forces save-as on BOTH it and `/api/files/:key` (keeps real filename). QRQ logo uploads live as text in payload fields — the UI extracts file links from field values (allow-listed hosts only).
- Forms Inbox excludes `jotform-lead` rows (they'd flood it) — `withoutJotformLeads()` in `dashboards/js/form-submissions.js`.

## Layer 2 — CRM v2 (kanban · workspace · timeline · digest · quote handoff)

- **`Lead_Activity` table** (DesignNotes-modeled): PK_ID auto, Submission_ID FK, Activity_Type (note|status|attachment|quote|system), Activity_Text **TEXT** (not STRING — 255 would truncate), Attachment_URL (allow-listed), Created_By, Created_At (server-stamped), Parent_PK dormant. Immutable v1. `GET/POST /api/lead-activity` (CRM-secret per-route) ← browser via `/api/crm-proxy/lead-activity*` (requireStaff).
- **`Form_Submissions.Lead_Value`**: estimated $; linking a quote **snapshots its TotalAmount in** (kanban $ totals cost zero extra reads); workspace drift-syncs on open.
- **Board** (`/dashboards/leads.html`): kanban default desktop / list <768px (persisted `nwca-leads-view`); 5 columns via `STAGE_OF` (RST "Entered in ShopWorks" = **Won**); `DRAG_STATUS` maps drops per form (blocked combos explain via banner); `window.moveCard(id، col)` = harness entry; Won/Lost columns show 45 days + "+N older — see List"; Mine chip (session email → `EMAIL_TO_REP`); column headers count + $.
- **Workspace** (`/dashboards/lead.html#<Submission_ID>` — **#hash, NEVER ?id= — quoted-printable mangles `=` in emailed links**): timeline + composer (Ctrl+Enter) + drop/paste/click uploads (`ArtworkUpload.uploadOne` → `/api/files/upload` → attachment activity) + panels: contact, follow-up (`Due_Date` + chips, overdue flag), value, ShopWorks match + **customer intelligence** (`GET /api/customer-history/:id` — orders/revenue/top items, 6h cache, fails-soft), linked quote (live `GET /api/quote_sessions?quoteID=` + `/quote/<id>` deep link), click-gated order history (`/api/crm-proxy/order-odbc`), submitted details, original artwork.
- **Follow-up digest**: proxy cron weekdays **7:45 AM PT** (staggered vs 8:00 approval digest) — `src/utils/lead-followup-digest.js`; buckets: overdue (`Due_Date<today`) / due today / new-&-untouched (New, no date, >48h, ≤60d); ONE Caspio read; per-AE via `resolveAEEmailLoose` (full display names!); admin `GET /api/lead-digest/scan` + `POST /api/lead-digest/send` (x-admin-key=ADMIN_KEY_DIGEST). `rep-email-map.js` += Jim/Bradley/Steve→art@/'General'→sales@.
- **Quote handoff**: workspace "Start a quote" buttons → sessionStorage `nwca-method-switch` {customer incl. customerNumber} + `{builder}?from=methodswitch` — rides existing `takeMethodSwitchPrefill`, **zero builder edits**; suggestions = `quote_sessions?customerEmail=` → one-click Link.
- **Shared core**: `dashboards/js/leads-common.js` (`window.LeadsCommon`) — vocabulary, formatting, payload/attachment parsing, crm-proxy helpers, `logActivity`. Activity breadcrumbs are client-posted (status/rep/link/upload/quote); Due_Date/Lead_Value edits deliberately NOT logged.
- **Manual capture** (`manual-lead` / MNL, v2026.07.18.7): board "+ New lead" modal → public `POST /api/form-submissions` (company falls back to "Individual — name"; `salesRep` prefilled from `EMAIL_TO_REP[staffEmail]`; est. value = follow-up PUT after the 201); Slack "📞 New PHONE/WALK-IN LEAD"; in the digest; enrichment SKIPS the rep email when the logger picked a rep (no self-notify). Creates a `system` "Logged manually (source)" activity, then reuses the hash-deep-link path to pop the drawer.
- **One-click outreach** (workspace "Email the lead" panel): 4 server-built templates (proxy `src/utils/lead-outreach-templates.js` — intro/quote-followup/checking-in/won-thanks; ALL lead values HTML-escaped, jest-locked) → `POST /api/crm-proxy/lead-outreach` `preview:true` renders subject+body inline → Send goes EmailJS `EMAILJS_TEMPLATE_LEAD_OUTREACH` (To=lead, Reply-To=AE only if `@nwcustomapparel.com` else sales@, from_name "AE — Northwest Custom Apparel") + server logs an `email` activity (client mirrors it locally; `Activity_Type` allowlist includes `email`).
- **🔥 hot badges + response timers** (`leads-common.js` `leadHeat`/`responseTimer`, pure client-side on already-loaded data): hot = deadline words (asap/rush/urgent/deadline/in-hands/needs by/needs <digit>) OR a payload **field VALUE that is a date within the next 60 days** ("needs by 7/29/2026") OR ≥48 pieces mentioned OR existing customer OR `Lead_Value≥500`. ⚠️ **Scans Summary + field VALUES only, NEVER raw Payload_JSON** — labels like QRQ's "Need By" would flag every submission (bug fixed v2026.07.18.13). Result cached on the lead object (`__heat`, non-enumerable); pages call `clearHeat(lead)` after value/customer-link/status edits. 🔥 + reasons tooltip on cards/list/workspace header; hot leads float to the top of New. Timer on untouched `Status='New'`: <1h green / 1–24h amber / >24h red; clears on status change.

## Gotchas learned building it (recur-prone)

- **Proxy utils must lazy-require `utils/caspio`** — its api-tracker timer keeps jest's event loop alive (suite "hangs"). Same rule as form-submission-helpers.
- **npm run eats `--flag` args on Windows** — invoke scripts as `node scripts/x.js --flag` directly.
- **`=` in emailed links gets QP-mangled** — all lead deep links are `#hash`.
- **Date-only strings (`YYYY-MM-DD`) parse as UTC midnight** → display a day early Pacific; pin to `T12:00:00` (leads-common `fmtWhen`).
- Harness stubs: ArtworkUpload uses **XMLHttpRequest** — fetch stub alone can't intercept; `test-leads-stub.js` patches XHR for `/api/files/upload`.
- In a **column** flexbox, `flex-basis` becomes HEIGHT (mobile search-box balloon; fixed with `flex:none` in the media query).
- **Origin/Referer-gated file endpoints 401 normal clicks** — `target="_blank"` + `rel="noreferrer"` (and privacy browsers) send NO Referer. `/api/jotform/file` is now ungated like `/api/files/:key` (the u= host allow-list is the real guard); links use `rel="noopener"` only.
- GET `/api/form-submissions` supports `formIds=`/`statusNot=`/`limit=` (≤2000); PUT whitelist incl. Sales_Rep/Matched_ID_Customer/Linked_Quote_ID/Lead_Value.

## Hardening + AE-portal batch (2026-07-19, multi-agent audit — `fix(leads):`/`feat(leads):` commits)

- **Quote prefill from a lead NOW WORKS** in all 4 builders. It was ~80% built but never fired: the workspace opened the builder with `window.open(...,'noopener')`, whose tab has an EMPTY sessionStorage, so `takeMethodSwitchPrefill()` read null; DTG had no consumer at all. Fix: cross-tab handoffs stash in **localStorage** (ts-stamped, 5-min TTL, take-once; reader drains sessionStorage first then localStorage) — `stashMethodSwitchPrefill()` in `quote-builder-utils.js`; DTG consumes via `fillFromQuote(null, customer)`. `tests/unit/method-switch-prefill.test.js`.
- **20 audit bugs fixed** (see LESSONS "Leads CRM hardening batch"): filter-desync phantom "0 of N", link-customer false-success + dead autoload, quote link/unlink (existence check + Unlink button), keyboard-openable cards/rows + drawer focus-trap/inert + mobile error banner, in-flight load guard, malformed-hash boot crash, new-lead value silent-drop, manual-lead source filter, input-revert on failure, outreach timeout dup-warning, order-history retry. Proxy: pagination `PK_ID` tiebreak, safe collision-retry (read-back before retry), honest sample partial-write (201+itemsWarning, not "resubmit").
- **NEW features**: **Export** button (current view → Excel-ready CSV, UTF-8 BOM + `=+-@` formula-injection hardening, `L.csvCell`). **Edit lead** (shared `L.openEditLeadModal`, both surfaces — edits contact/company/email/phone/customer#/summary; diff-PUT changed fields only; Payload_JSON stays immutable). **Delete lead** = **admin-only, server-enforced** (`server.js app.delete /api/crm-proxy/form-submissions/:id` requireCrmRole(['admin']) → proxy DELETE cascades Lead_Activity; generic mount 405s DELETE; Archive stays the any-staff soft path). Proxy PUT allowlist gained the 6 identity fields + email validation.
- **Attribution hardened**: `Updated_By`/`Created_By` were client-supplied (spoofable across staff) — now stamped from the verified session server-side (`stampSessionIdentity` on the form-submissions + lead-activity forwarders). Attachment/source URLs from the public POST are now host-allowlisted (`isAllowedAttachmentUrl`/`safeSourceUrl`), not just scheme-checked.
- **AE command-portal handoffs** (workspace `dashboards/lead.html`): (1) **Send samples** — head-actions button stashes contact info in localStorage (cross-tab, noopener rule) + opens `/catalog?topSellers=1`; `pages/js/sample-cart-page.js` prefills the contact form + logs a timeline breadcrumb on order success (gated on stash → no-op for real customers; also on the Stripe return via `window.finishSampleLeadHandoff`). (2) **Send to the art team** — `lw-panel-art` rail panel lazy-loads the shared `GarmentSubmitForm` bundle in a modal (clone of quote-view's Send-to-Steve `ensureArtFormLoaded`/`sts-modal`), prefilled from the lead; onSubmitted writes `Art_Request_ID` back + logs, panel then shows the linked request. Only proxy `/api/files/` artwork imports server-side (JotForm hosts aren't on the import allowlist → ride in notes). Zero backend changes (Art_Request_ID PUT pre-allowlisted; POST /api/artrequests is the AE-dashboard pass-through).
- **⏳ ERIK — grant Delete**: hard-delete shows only for the `admin` role (`Staff_App_Roles`); add others there (no deploy) to let them delete. Archived spam hard-delete is still a Caspio-datasheet action.
- **⏳ Marketing kit → Mikalah/ShipStation handoff = LAST portal piece, NOT built** — needs Erik to create 2 Caspio tables first (`Marketing_Shipments` + Erik-editable `Marketing_Kit_Items`). Full design in the multi-agent audit; ShipStation `lib/shipstation-client.createOrder` reuse (Phase 2). Build once the tables exist.

## Open / optional

- ⏳ **ERIK: EmailJS `template_lead_followup_digest`** + `heroku config:set EMAILJS_TEMPLATE_LEAD_FOLLOWUP_DIGEST=<id>` — cron activates itself once set (params: to_email/to_name/ae_name/digest_date/counts/overdue_html/due_html/new_html/board_link, triple-stache the *_html).
- ⏳ **ERIK: EmailJS `template_lead_outreach`** + `heroku config:set EMAILJS_TEMPLATE_LEAD_OUTREACH=<id>` — until set, Send returns a visible 503 (preview still works). Template fields: To Email `{{to_email}}`, Reply-To `{{reply_to}}`, From Name `{{from_name}}`, Subject `{{subject}}`, body = `{{{body_html}}}` (triple-stache — it's HTML).
- ✅ **Spam sweep DONE 2026-07-19** (`Updated_By='spam-cleanup-2026-07-19'` — ARCHIVED, not deleted, so reversible via "Show archived"): of the 138 CSV candidates 136 were already auto-archived, 2 archived now; plus a HUMAN review of all 32 live `New` leads archived 7 more (3× Eagle Stitches digitizing blasts, CLASSIC TEES vendor, SEO pitch, Animated Explainer pitch, 1 job-seeker). Digest newUntouched 32→23, all real. Hard-delete of archived spam rows = still Erik's call in the Caspio datasheet.
- ✅ DONE 7/18 late (proxy v2026.07.18.6 `4dfb885`): in-app QRQ/WSR/RST arrivals get JotForm-parity enrichment (fire-and-forget assignLead + Matched_ID_Customer stamp + rep email; roster keeps customer-chosen rep) — smoke-proven (QRQ0718-3130 → Jim + #3739, archived). 'House' rep → sales@ in rep-email-map.
