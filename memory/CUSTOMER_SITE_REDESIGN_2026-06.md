# Customer Site Redesign 2026-06 — Master Plan

## ⏯️ PICKUP HERE (updated 2026-06-11 end-of-day, Erik shutting down — 11 releases shipped)

**State**: ✅ **EVERYTHING LIVE through `v2026.06.11.11`**, develop == main == prod, tree CLEAN. The .11 release was deployed by ERIK'S PARALLEL SESSION (55 files: its DTF refactor + legacy /product app deletion + christmas-bundles/laser-tumbler edits) and carried this session's quote-cart Phase 2 commits with it. LIVE today: homepage+rotator · /catalog · product.html w/ 3-question CONFIGURATOR (decisions 17+18) · **Add-to-Quote CART at /quote-cart** (badge, toasts, pooled per-method group cards, tier meters, nudges; sessionStorage nwca.quoteCart.v1; parity gate exact: pooled EMB $588 / DTG $348 / below-min $175) · decoration gating · 🎩 /custom-caps store · quote-cart-engine (62-fixture parity lock) · all 0.57 margins swept · baselines re-locked.

**Next work items, in order**:
1. **Post-deploy validation of v2026.06.11.11's foreign half**: I verified /quote-cart 200 + product wiring live, but the parallel session's DTF refactor + legacy-deletion portion shipped on ITS gates, not mine — run `npm run test:unit` (expect 39 suites / 1,273+; it added tests/unit/dtf-save-parity.test.js) + smoke the DTF builder + confirm no page references the deleted /product legacy files (grep app.js/components/).
1b. **PROXY repo has the other session's UNCOMMITTED push-note fix** (`../caspio-pricing-proxy`: lib/dtf-push-transformer.js + lib/scp-push-transformer.js + src/routes/dtf-push.js + NEW tests/jest/dtf-push-transformer.test.js — DTF+SCP push notes gain shipping+accounting note). Review → run jest → commit → ASK ERIK → deploy proxy (`git fetch heroku` first!).
1c. **Stash `stash@{0}` "parallel-session DTF totals fix"** (backup: %TEMP%\dtf-totals-fix-stash.patch): the shipped .11 includes computeFeesAndTotals — diff the stash against HEAD to confirm it's redundant, then `git stash drop`; if NOT redundant, recover per the MEMORY warning entry.
2. **Quote-cart PHASE 3** (the spec is in CUSTOMER_QUOTE_CART_DESIGN_2026-06.md phasing): web-quote-service.js — save cart as quote_sessions+quote_items with NEW prefix **WQ** (via /api/quote-sequence/WQ), pre-save forceRefresh parity check, artwork upload per print-type group (proxy files API pattern), EmailJS notification to sales@ + customer copy w/ /quote/WQ-... share link, quote-view mixed-method grouping tweak, printable PDF, 'Request final quote' handoff. Erik decisions already locked: immediate customer share link; v1 saved quotes frozen.
3. Phase 4 backlog + P3 remainders (homepage search cutover to /catalog; quote-view reskin P5; calculators P6; SEO/analytics/legal/404 P7).

**✅ HATS E2E DONE 2026-06-12** — `CAP0612-9652` (8× C914, real card **$251.03**): webhook → Caspio `Processed` → ShopWorks order created → emails — ALL verified. quote_items + tax cols now populate live (today's `storefront-quote-items.js` fix proven on a PAID order); $28.50/cap = $23 base + $5.50 back logo; $228 + $23.03 WA tax = $251.03. Test rows (quote_sessions **PK 1680** + quote_items **PK 4960**) DELETED via proxy; Erik refunding Stripe (`cs_live_b1d0…KzF`) + voiding SW.
  - **✅ RESOLVED + IMPLEMENTED 2026-06-12 (Erik said "make it for custom embroidery sales")**: caps web orders now route to **Custom Embroidery** (`id_OrderType` **21** → acct **4050** Custom Embroidered Sales). Added `idOrderType: 21` to the caps `push` block in `config/storefront-channels.js`; storefront payload (`server.js` ~3501, `manageOrdersPayload`) sends it via conditional spread `...(pushC.idOrderType ? {idOrderType} : {})` — **caps ONLY**; the DTG tee channels OMIT it so they stay on the proxy default 6 (Online Store / acct 4003), byte-identical payload. Mechanism: proxy `manageorders-push-client` reads root-level `idOrderType` (same as the Order-Form push at server.js:4955). Locked by `tests/unit/storefront-channels.test.js` (caps=21, CTS/TDT undefined). **DTG tees still book to 4003 — Erik did NOT ask to change them (DTG ≠ embroidery).** ⏳ Live confirmation pending: next cap order's ShopWorks header should read order type "Custom Embroidery" / acct 4050.
  - **Note**: new-logo orders push with empty `DesignIDBlock`/`ExtDesignIDBlock` (EXPECTED — no design # exists yet; art team builds the design from the proof-first note). Confirm the customer's uploaded logo file is reachable by whoever proofs the order.

**Session facts a fresh context needs**:
- Harness task board: #1-#4 completed; #5 P3 + #6 P4 in_progress (both mostly done — see descriptions); #7-#9 pending.
- **Parallel sessions share this checkout** — `git status` before ANY deploy; unknown dirt = STOP (lesson recorded in LESSONS_LEARNED 2026-06-11 after a sweep incident; second incident avoided by holding deploy until the other session shipped).
- Preview gotchas: preview_screenshot wedges per-window (restart server); preview_click coords unreliable (use el.click() via eval); Node needs restart for server.js changes.
- Engine deps: product/quote-cart pages must load EmbroideryPricingCalculator + DTFConfig + services before quote-cart-engine adapters run (see product.html script order).
- index.html JS contract is LOAD-BEARING: `.hero-section` + `.homepage-sections` wrappers, all the #nav*/#results*/#filters* IDs — catalog-search.js has unguarded querySelectors (details in 2026-06-11 status entries below).
- Erik's 4 hats decisions + 4 design decisions are all in the Decision log. Full audit evidence: CUSTOMER_SITE_REDESIGN_2026-06_FINDINGS.md. Hat lineup table + C112→C402 gotcha below.
- Known deferred nits: "$23.5" un-padded price formatting on showcase buttons (P3 rebuild); sample-cart sessionStorage still client-trusted until P5 server reprice; top-sellers-product still hardcodes the proxy URL (page-consistent; dies in P3 consolidation anyway).

> Status doc for the full customer-facing redesign + Custom Hats embroidery store.
> Full audit evidence (8-agent discovery sweep, 2026-06-11): [CUSTOMER_SITE_REDESIGN_2026-06_FINDINGS.md](CUSTOMER_SITE_REDESIGN_2026-06_FINDINGS.md)

## Status

- **2026-06-12 (DEPLOYED)**: **"Express Order" homepage section LIVE.** The two customer stores — Custom T-Shirts (`/custom-tees`, DTG) + Custom Hats (`/custom-caps`, cap embroidery) — are now branded together as **Express Order** and featured front-and-center. Erik's decisions: (1) replaced the hero golf+caps duo with two **photo cards** (PC54 tee + Richardson 112 cap; new `.express-*` classes in `nwca-2026.css` modeled on `.shelf-card`; SanMar CDN imgs load direct, no proxy); (2) golf promo relocated to its own full-width band at the top of `.homepage-sections` (UTM link intact); (3) nav "Custom Tees" → **"Express Order"** (`/#express-order`), Custom Hats added to footer Shop col + mobile drawer. Orphaned `.hero-duo`/`.duo-hats` CSS removed (homepage-only, grep-verified). **Express Order = the AE-assisted/hybrid model** (customer self-serves the order start → pays; an AE proofs + verifies + finalizes with the customer) — positioned for small/one-time/event orders that are slow for an AE to set up by phone. Preview-verified: both photos load, search JS contract intact (hero/sections hide, results show), mobile stacks, zero console errors.

- **2026-06-12 (real-photo imagery, DEPLOYED)**: Swapped the hero rotator + both Express Order cards to real NWCA customer-job photos (Caspio CDN, all 1200×1000 / 1800×1200). **KEY CONSTRAINT — hero rotator uses `mix-blend-mode: multiply` (floating-product look) → images MUST be product-on-WHITE or transparent.** Lifestyle / outdoor / scene / person photos go muddy and were excluded. Express cards switched from fixed `height:200px` to `aspect-ratio: 3/2` so 1800×1200 fit with no crop. Removed the `.hero-sticker` "Free samples" badge (HTML + CSS). Hero now rotates 9 white-bg shots (Erik viewed/approved each). **BACKLOG (Erik's 8 set-aside shots): showroom rack, gray-lumber/AWI/arrow-vtc caps outdoors, Erik profile + selfie, fills-frame Omega-back/General-Mechanical-vest → build a "Real NW businesses / inside our Milton shop" strip lower on the homepage to use them (framed-photo treatment, NOT the multiply hero).**

- **2026-06-12 (custom-tees AI-artwork feature, DEPLOYED `v2026.06.12.6`)**: /custom-tees hero swapped to a real AI-design shirt (`AI Artwork.jpg`) + NEW **"AI Artwork + Full-Color DTG" feature section** between the hero and the garment picker — positions NWCA as experts at printing AI-generated art (design in ChatGPT/Midjourney/DALL·E/Firefly → upload **300 DPI transparent PNG** → full-color DTG; no setup, no minimums). 5 sample design tiles (Squad 51 / Momma Bear / 404 Knight / Built for Adventure / Dog Mom, all 1200×1000). Built via a design workflow (map → 3 concepts → synthesize). Markup in `pages/custom-tees.html` (after hero `</section>`); styles `.band-ai` / `.ai-band-*` appended to `pages/css/custom-tees.css`; reuses `.band`/`.band-title`/`.btn-hero`/`.hero-underline`/`.stage-num` motif + page tokens; FA6 icons (page already loads FA). CTA = no-JS anchor → `#stage-gallery`. **GOTCHA: the AI tiles do NOT use `mix-blend-mode: multiply`** (unlike the homepage hero + `.gallery-card-hero`) — AI designs arrive on their OWN backgrounds (Momma Bear + Dog Mom are on BLACK for dark shirts), and multiply muddies black-bg art. Tiles are bordered cards, `object-fit: cover` at `aspect-ratio: 6/5` (= the 1200×1000 image ratio → zero crop). Image-hosting best practice reconfirmed w/ Erik: **Caspio CDN, never repo/Heroku-slug** (CDN + no-deploy swaps + lean repo). FOLLOW-UP (optional): /custom-tees `og:image` still the old blank PC54 tee.

- **2026-06-12 (gallery SanMar-style card upgrade — DEPLOYED LIVE `v2026.06.12.7` rel 1347)**: /custom-tees "Pick your garment" cards upgraded per Erik's SanMar comparison: per-card **blurb + fabric facts** (parsed server-side from SanMar PRODUCT_DESCRIPTION already in Caspio Sanmar_Bulk via /api/product-details — API-driven copy, future Caspio Card_Blurb column overrides), **live reference price** "$X.XX/shirt · 12 shirts, full-front print" + 24/48/72 ladder (Erik's picks), **featured-color variety pass** (no more wall of Jet Black; swatch hover previews color), auto **Best Seller badge** (style_rank ≤3), **ordered/pluralized category chips**, **sort** (popular/price/A-Z), price footnote. NEW `GET /api/cts/gallery-extras` in server.js (5-min cache; per-style copy 24h): prices = `CTS_PRICING.quote().perShirt` via the SAME getCtsPricingConfig the checkout 409-gate uses → **parity by construction**; fail-closed (per-style priceError → visible "Pricing unavailable", endpoint failure → toast; NEVER stale/guessed). NEW pure module `pages/js/cts-gallery-merch.js` + `tests/unit/cts-gallery-merch.test.js` (15). **FIX riding along: `getCtsServiceCode` shared 5-min promise-memo in server.js** — per-style config used to re-fetch the same 5 service codes per style; the 20-style burst 429'd the proxy (staleness unchanged ≤5 min, fail-closed kept, checkout path now makes fewer proxy calls). Gotcha: quote() returns `shirtsSubtotal`/`perShirt` (NO `subtotal` field). Verified live in preview: 20/20 styles priced (PC54 $17.58@12 → $12.50@72), blurbs/fabric render, variety pass confirmed (Jet Black→Navy→Athletic Hthr→Dk Hthr...), price sort works, zero console errors. Erik's deferred choices: sample-print overlay on card photos (Tier-1 CSS-overlay plan in the 2026-06-12 discovery workflow), Caspio Card_Blurb/Featured_Color/Badge columns (needs proxy-repo change — proxy NOT checked out locally; lives only on Heroku).

- **2026-06-12 (gallery round 2 — badge fix + About panel + PC54 SALE — DEPLOYED LIVE `v2026.06.12.7` rel 1347 (PROD-verified: 20/20 priced, PC54 16.58/was 17.58))**: (1) Best Seller badge moved OFF the photo into a `.gallery-card-topline` body row (was covering the model — Erik screenshot). (2) Step-2 **"About this shirt — fabric & specs"** `<details>` panel under the product header: `CTS_MERCH.extractSpecs()` parses the already-fetched PRODUCT_DESCRIPTION into lead + spec bullets (paren-aware splitter — SanMar delimits specs with 2+ spaces but color lists use double spaces INSIDE parens; jest-locked). (3) **Per-style SALE mechanism (Erik: $1/shirt off PC54)**: Caspio Service_Codes **`CTS-SALE-{STYLE}`** (PC54 row = PK 250, $1, IsActive) → `config.saleOff` → `CTS_PRICING.quote()` subtracts off the final sticker (after rush, BEFORE LTM bake, clamped ≥0; OPTIONAL — absent/error → 0 = regular price, the safe direction; 5 jest tests). Loaded by server `getCtsSalesMap()` (ONE `?category=Custom Tees` call, 5-min promise-memo) AND browser boot (same single category call → `S.salesByStyle`; loadProduct sets `S.config.saleOff` per style) → card/studio/checkout-409 parity by construction. Card shows red SALE chip + strike-through was-price (gallery-extras returns `wasPrices` = same quote with saleOff:0). **Erik toggles IsActive / edits SellPrice in Caspio = sale on/off, NO deploy.** ⚠️ GOTCHA ×2 now: NEVER per-style `?code=` fan-outs against the proxy — service-codes sits behind a touchy per-IP rate limiter ("Monograms endpoints", 429 retryAfter 60s); both the original 5-codes-per-style AND the first sale-lookup-per-style versions 429'd a 20-style burst. Batch via `?category=` or promise-memo. Verified live: endpoint PC54 now 16.58/was 17.58 @12 (PC61 untouched), card renders chip+strike+discount ladder, browser quote applies saleOff=1, 1,346 unit tests green, zero console errors.

- **2026-06-11**: Discovery complete (8 agents, 7 audits + completeness critique). Homepage redesign agent in flight (preview-only — nothing ships without Erik's screenshot approval). New design system landed: `shared_components/css/nwca-2026.css` ("press-room editorial", promoted from custom-tees.css).
- **2026-06-11 (same session)**: **P0 hotfix wave DONE + preview-verified** — 7 zombie sendFile routes removed (incl. test-api/test-catalog-layout, also missing), `/cart` → 301 sample-cart, cart.html + order-confirmation.html deleted (dead JS flagged in ACTIVE_FILES.md, not deleted), webstore-info/inventory-details dead links repointed, validate-critical-paths.js cart block removed. **P0b DONE + tamper-tested** — sample price/sampleType now re-derived from BLANK pricing-bundle at add-to-cart (`resolveSamplePricing`), URL params inert; showcase margin now API-sourced (was stale 0.57 vs Caspio 0.53 — paid-sample prices rise slightly to match Caspio intent). index.html nav dead links (Marketing/Sale/Resources) ride with the homepage rewrite — verify at P1 review. NOT YET DEPLOYED — rides with the homepage release.
- **2026-06-11**: Hats v1 lineup curated + verified (9 styles, table below).
- **2026-06-11 (post-restart)**: **Search category-drift bug FIXED + preview-verified** — CATEGORY_KEYWORDS in product-search-service.js had stale 'Fleece'/'Headwear' (live: 'Sweatshirts/Fleece'/'Caps'); "hoodies"→0 became 266 products, "caps"→0 became 146. Added zero-result drift guard (inferred category dropped + warn on 0 hits). Was broken on PRODUCTION too. Homepage screenshots (desktop ×7 slices, mobile, caps-results) captured + presented to Erik — awaiting approval gate.
- **2026-06-11 (later)**: Original homepage agent DIED mid-task (wrote nwca-2026.css, never touched index.html — no transcript, no completion). **Orchestrator rebuilt index.html directly: DONE + preview-verified, awaiting Erik screenshot approval.** New index.html on nwca-2026.css + catalog-search.css (dropped main-redesign.css → brands.html is its last consumer; main.css/gallery-styles/modern-search-interface now dead-flagged). NEW `home-2026.js` (drawer close/Escape/scroll-lock, All-categories tile, hero un-hide patch for searchByBrand's inline-style hide that clearSearch never undid). **JS contract preserved** — all search machinery IDs/classes kept (app-modern/catalog-search/autocomplete-new/brands-flyout untouched); `.hero-section` + `.homepage-sections` wrappers REQUIRED (unguarded toggles in catalog-search.js); legacy #filtersSection/#clearFiltersBtn stubs kept hidden in drawer. Verified in preview: search (6 PC61 results), /?category=Caps deep link (277 products, breadcrumb, filter bar), drawer open/close/Escape/scroll-lock, mobile 375px (search VISIBLE — audit critical fixed; 10px cat-tile grid blowout fixed w/ min-width:0), zero console errors. Category tiles use /?category= URLs (shareable — head start on P2 URL state). Gotchas: preview_screenshot can wedge per-window (restart preview server to fix); preview_click coordinates unreliable in emulated viewport (use el.click() via eval to test handlers).

- **2026-06-11 (backend, for P2)**: **Proxy DEPLOYED LIVE same day (Erik-approved; 21/21 new tests; live-verified displayPrice + categories on prod). Deploy also reconciled 5 production commits that sat on heroku/main but were MISSING from origin/main — proxy repos now in sync (lesson: always `git fetch heroku` before proxy releases).** The branch `catalog-pricing` moves the catalog "from $X" card price server-side: `/api/products/search` products now carry additive `displayPrice` (number|null) + `displayPriceLabel` ("from $24") = HalfDollarCeil(cheapest-size MAX(CASE_PRICE) / Caspio BLANK MarginDenominator 0.53, 1h-cached) — replaces the frontend's stale `(PIECE_PRICE/0.57)+15` literal in product-search-service.js (processProducts currently OVERWRITES the field client-side, so deploying the API alone changes nothing visibly; P2 frontend must switch to the server fields + delete MARGIN/FIXED_ADDITION). Also NEW `GET /api/categories` → `{categories:[{name,count}]}` (live, 1h cache, 15 cats — "Caps" not "Headwear") for the nav/mega-menu taxonomy. Missing cost/margin ⇒ null price, never a fallback number.

- **2026-06-11 (Wave 2 DEPLOYED LIVE `v2026.06.11.4`)**: nwca-2026-core.css split + interior primitives (+NWCA-2026-GUIDE.md class reference) · **/catalog page LIVE** (URL state, facets, server displayPrice cards, autocomplete v2, quick view, category landing; portrait-image overflow fixed — grid-track max-height trap) · **product.html REBUILT + LIVE** (decoration pricing tabs via the SAME shared pricing services as the calculators — EMB PC61 $25/$25/$21/$20/$19 + CAP 112 $30/$26/$24/$23 verified exact; old /product app dead-flagged on disk) · **homepage cutover**: hero CTA + category tiles + drawer/mega/flyout/brand links → /catalog (home-2026.js capture-phase interceptor; homepage search box still in-page — cutover pending) · robots.txt live. **P3 REMAINING**: retire legacy product pages after soak; method-matrix (DTG tab shows on jackets/bags — Erik decision); get-a-quote target (mailto → quote form); homepage search cutover. **P4 started**: channel-registry extraction agent in flight (server.js, jest-locked, no deploy).

- **2026-06-11 (P4 server core — NOT deployed, uncommitted)**: **Custom Hats 'custom-caps' channel server core BUILT.** NEW `pages/js/custom-caps-pricing.js` (pure EMB-cap-parity engine: blankOSFA÷tier-margin+EmbCost(tier,8K)→CeilDollar, CAP-AL back-logo add-on, qty≥8 structured BELOW_MINIMUM, NO LTM/digitizing, CAPS-SHIP-* threshold shipping fail-closed) + `tests/unit/custom-caps-pricing.test.js` (44 tests — all 9 lineup styles match the verified table EXACTLY, also re-verified against LIVE bundles). Registry entry in config/storefront-channels.js (CAP prefix — ⚠ NEW, not in Quote Mgmt's prefix list; designTypeId **2**=Embroidery, artistId 24=EMB default, swLocationMap CF→'Cap Front'/CB→'Cap Back', proof-first banners) + server.js bindings (`rebuildCapsQuote` server twin, sizeWhitelist ['OSFA'], SanMar stock gate w/ NEW per-channel `stockConflicts` — caps aggregate by CATALOG_COLOR per the stale-sized-partId trap; checkout 400s BELOW_MINIMUM). **Caspio CREATED + verified live**: Service_Codes `CAPS-SHIP-FLAT` (PK 242, $7.99) + `CAPS-SHIP-FREE-OVER` (PK 243, SellPrice=THRESHOLD $100 — ⚠ 8-cap min ⇒ every order ≥~$184 ⇒ shipping effectively ALWAYS free until Erik raises it) + table `CAPS_Catalog_2026` (51 rows: 9 styles × hero colors w/ COLOR_NAME+CATALOG_COLOR, style_rank, is_active; 220 excludes Black/Navy). GAP: no proxy route serves CAPS_Catalog_2026 yet — server tries `GET /api/caps/catalog` then falls back (loudly) to the registry-pinned 9-style whitelist; follow-up = ~30-line dtg-top-sellers.js clone in the proxy. ERIK-DECISIONS open: artistId 24 ok? ship-promise window 7-10 biz days after proof approval? fork caps EmailJS templates? Unit suite 1198 green (was 1141; the 2-channel registry-shape lock was updated for the 3rd channel). Integration suites' 6 fails are PRE-EXISTING (reproduced on clean tree).

- **2026-06-11 (DEPLOYED LIVE `v2026.06.11.5` + proxy 2x)**: **Decoration method gating LIVE end-to-end** — Caspio `Decoration_Method_Rules` (15 cats, every cell Erik-tunable) + `Decoration_Method_Overrides` + proxy `GET /api/decoration-methods` (1h cache, 502-on-failure) + FE `decoration-methods.js` (cotton gate; blend→warn note; API-down→EMB-only+visible alert) + catalog `?method=` filter. Live-verified: PC61=4 tabs, PC90H=DTG+blend warn, CT103828=EMB only, method=dtg→5,467. **Hats P4 steps 1-2 DEPLOYED (dormant)**: CHANNELS registry (28-test characterization lock, 1,141→1,198 suite) + `custom-caps` channel + `custom-caps-pricing.js` (44-test parity lock = lineup table exact) + `rebuildCapsQuote` + proxy `GET /api/caps/catalog` (CAPS_Catalog_2026, 51 rows) + Caspio CAPS-SHIP-FLAT 7.99/CAPS-SHIP-FREE-OVER 100. No /custom-caps route registered yet (zombie-route lesson) — storefront page is the remaining P4 piece. **OPEN ERIK-DECISIONS (hats)**: artistId 24 (EMB default) OK? · 7-10 business-day promise window after proof approval? · fork caps EmailJS templates vs reuse tee ones? · CAP prefix needs adding to Quote Mgmt prefix list pre-launch · shipping effectively always free at $100 threshold (8-cap min ⇒ cheapest order ~$184).

- **2026-06-11 (🎩 HATS STORE LAUNCHED `v2026.06.11.6`)**: **/custom-caps LIVE** (9-cap gallery w/ live CAP-bundle prices, hero-color swatches w/ live stock, OSFA stepper 8-min, tier ladder, front logo required + back logo add-on, rights ack, proof-first copy, free shipping, Stripe checkout vs deployed repricer) + success page + homepage duo-hats card flipped to live link ("New — order online"). Preview-verified pre-launch: real Stripe session CAP0611-0242 ($812.54 client==server, row deleted from Caspio post-test), all negative paths (409 tamper / 400 below-min / rights / rush / style). Quote Mgmt routes CAP → read-only view. **PENDING: Erik's real ~$200 E2E test order** (verify webhook→Caspio→SW push→emails, then VOID in SW + refund in Stripe — tees playbook). Deferred: caps email template fork; OSFA size-translation test push confirmation.

- **2026-06-11 (`v2026.06.11.8` LIVE — quote-cart Phase 0 + pricing fixes)**: **quote-cart-engine.js deployed DORMANT** (pure orchestration; adapters call staff authorities — EMB runs the builder's calculator class, DTG posts to the builder's own endpoint, SCP byte-copies builder tier/rounding w/ source-canary test, DTF uses the service w/ builder conventions; 62 parity fixtures from live captures, 24 fixture files; singleItemPreview for the configurator + priceCart pooled groups). **SCP manual-cost override GATED** (was live-tamperable; 20 gate tests across all 5 services). **Erik's chip fixes shipped**: public SCP calculator dark-garment parity (underbase = flat screen, was inflating per-piece) + DTG FF_JB/JF_FB unpriceable combos removed (re-enable = add Caspio cost rows). Unit suite now 38/1,264. Pre-existing pricing-baselines drift (5 reds, live Caspio movement vs 2026-05-24 lock) chip-flagged for re-lock — not deploy-blocking (deploy gate runs test:parser only). NEXT: Phase 1 configurator on product.html per decisions 17+18.

- **2026-06-11 (legacy /product ES-module tree DELETED, uncommitted)**: 16 dead files removed after repo-wide zero-reference verification — `product/app.js`, 9 components, 3 services (`state`/`quote-service`/`email-service`), 3 orphan CSS (`product-2025`/`product-redesign`/`quote-modal`). The hardcoded "+15.00" EMB adder died with `components/info.js` (live equivalent: product-search-service.js reads Service_Codes CATALOG-EMB-EST). KEPT (live via pages/inventory-details.html): `components/inventory.js`, `services/api.js`, `styles/product.css`; all of `product/js|css/` kept (`product/js/decoration-selector.js` verified orphan but retained pending owner decision). Also fixed: validate-critical-paths.js Product Display block (deps → pdp-configurator/product-2026, selector `#product-detail`→`#pdpContent`) + removed christmas-bundles.html's 404ing relative `product-search-service.js` script tag (page never used it). 1,273 unit tests green; product.html/inventory-details/christmas-bundles preview-verified. Note "retire legacy product pages" (top-sellers-product etc.) is still open — this was the module tree only.

- **2026-06-11 (later session — COMMITTED on develop, AWAITING CLEAN DEPLOY WINDOW: parallel DTF session mid-edit in tree)**: **(1) SCP customer minimum FIXED** (`c2b4e381`) — engine gates SCP below the data-derived lowest-tier minQty (13 today) with BELOW_MINIMUM exactly like DTF's 10; configurator chip now renders the per-method number (was hardcoded 'Starts at 10'); engine error list carries minQuantity. Erik's audit confirmed: EMB 1-7 + DTG 1-23 legitimately price from qty 1 w/ honest LTM; DTF 10 was already gated. Fixture (c2) locks it; suite 1,290. **(2) Quote-cart PHASE 3 BUILT + E2E-PROVEN** (`4afc1e87`) — web-quote-service.js (forceRefresh parity gate w/ PRICING_CHANGED confirm flow, WQ-{year}-{NNN} via /api/quote-sequence/WQ, builders' save conventions incl. BaseUnitPrice=billed + fee rows + footing assert), save panel w/ artwork upload per group, success panel w/ share link, emails reuse template_quote_email (customer + sales@, fire-and-forget). quote-view: additive WQ support (type label, method subheads when mixed, rep-tax label). REAL E2E run: WQ-2026-002 saved → Caspio session+item footed $504 exact → /quote/WQ-2026-002 rendered → test rows deleted (PK 1676/4958). Suite 44 suites / 1,315 green. NOTE: dry-runs+E2E consumed WQ-2026-001/002 sequence numbers.

- **2026-06-12 (DECORATED CARD PRICES LIVE — proxy deploy, decision #19)**: Catalog cards now show **"from $X with logo"** = the product page's 72+ EMBROIDERED price (cheapest color/size), replacing the misleading blank from-price (Erik flagged CTK84 $34.50 blank vs ~$47 decorated). Server-side only (catalog-display-price.js getDecoratedDisplayPricingConfig: EmbroideryShirts margin@72+ + Shirt 8K cost@72+ CeilDollar; Caps use EmbroideryCaps+Cap = hats-lineup math) — zero FE change (label passthrough). Parity-proven: PC61 $19 = ladder 72+ exact, CT105298 $35 = hats lineup exact, CTK84 $47. Erik decisions: 72+ anchor + "with logo" wording. Bonus: decorated-cap-prices.js stale 0.57 fallback removed (visible 500 on missing margin). Tests 37/37.

## Decision log

| # | Decision | Choice | Who/when |
|---|---|---|---|
| 1 | Design direction | Modern NWCA green refresh — press-room editorial (paper ground, ink-green, safety-orange, Bricolage Grotesque + Public Sans) | Erik 2026-06-11 |
| 2 | Hat pricing model | **Simple: logo included** — one price per cap incl. front logo; back logo flat add-on; customers never see stitch counts | Erik 2026-06-11 |
| 3 | Go-live process | Preview screenshots first, then deploy on approval | Erik 2026-06-11 |
| 4 | Golf tournament pages | Restyle to match new system (campaign stays live) | Erik 2026-06-11 |
| 4a | Homepage | **APPROVED + deploy authorized** (screenshots reviewed post-restart) | Erik 2026-06-11 |
| 4b | Hero photo | **Rotate several real prints** — 4-image crossfade (Setina tee / Fife hoodie / Carhartt jacket / engraved pint), 4.5s, reduced-motion-safe, zero CLS (verified 470px stable) | Erik 2026-06-11 |
| 4c | Hats teaser card | **Keep visible** pre-launch (COMING SOON badge, no dead link) | Erik 2026-06-11 |
| 5 | cart.html + order-confirmation.html fate | **RETIRE** (orphaned Bootstrap legacy, zero inbound links; no hotfix, no migration). `/cart` route → 301 to the sample-cart successor | Orchestrator ruling on auditor contradiction |
| 6 | inventory-details.html fate | **FOLD into the new templated product page** (inventory summary/tab); not migrated standalone | Orchestrator ruling |
| 7 | Catalog IA | **REPLACE** the in-page homepage SPA with a dedicated `/catalog` page (URL-driven state); homepage becomes marketing + entry points | Orchestrator ruling |
| 8 | webstore-info alert() mobile-menu stub | No isolated hotfix — page is rebuilt/merged in the share-link wave; new shared chrome fixes mobile nav everywhere | Orchestrator ruling |
| 9 | Hats: minimum qty / LTM | **8-cap minimum** — skips the $50 LTM tier entirely; no baked storefront LTM code needed; entry ≈ $240 (8 × $30 R112) | Erik 2026-06-11 |
| 10 | Hats: digitizing/setup fee | **Free setup, always** — "free logo setup" marketing; absorb ~$75 internal DD cost; no CAPS-DIGITIZE checkout line | Erik 2026-06-11 |
| 11 | Hats: proof flow | **Digital proof first** — emailed sew-out preview approved before production; promise = "ships N days after proof approval"; needsArtReview always-on | Erik 2026-06-11 |
| 12 | Hats: v1 cap lineup | **Claude curates 6-10** SanMar-carried OSFA best-sellers, each API-verified (pricing, inventory, images); swappable via Caspio later | Erik 2026-06-11 |
| 13 | Hats: artist routing | **artistId 24** (EMB push default — cap orders are embroidery orders) | Claude (Erik delegated) 2026-06-11 |
| 14 | Hats: ship promise | **Proof within 2-3 business days; ships 7-10 business days after approval** — proof gate stated honestly | Claude (delegated) 2026-06-11 |
| 15 | Hats: email templates | **Reuse tee EmailJS templates w/ caps params** (no new dashboard surface); fork only if copy diverges | Claude (delegated) 2026-06-11 |
| 16 | Hats: shipping threshold | **Keep $100 free-over** — 8-cap min ⇒ every order ships free; storefront markets "Free shipping, always" | Claude (delegated) 2026-06-11 |
| 18 | Configurator location scope | **Two locations priced exactly, everything beyond disclaimed**: front + optional 2nd location priced via the builders' own math (EMB: AL tier price @8K allowance; DTG: native combo pricing; SCP: additional-location matrix + screen setup, ink colors asked once 1-4; DTF: 2nd transfer at placement-mapped size) — each locked by a parity fixture. Sleeves/3rd logos/names-numbers/3D puff/oversize stitches = NOT configurable; "add in notes — rep prices on proof" line. EMB prices include standard 8K logo (builder default). Universal footer on every total: "Final pricing confirmed with your free proof." | Erik+Claude 2026-06-11 |
| 17 | Customer pricing display | **Configurator-first ("quote like a storefront")**: product page leads with 3-question configurator (qty → design location → two priced method chips), ONE total, matrix demoted to "See every quantity price ▾" expandable. Add-to-quote cart pools per print type (demo'd to Erik, approved "I take your recommendation"). Storefront styles (tees/caps) get Buy-now in the same slot. Quote-cart design + parity rules: memory/CUSTOMER_QUOTE_CART_DESIGN_2026-06.md (Phase 1 spec amended: configurator REPLACES tabs as primary, not pickers-on-tabs) | Erik→Claude 2026-06-11 |

## Phase plan (merged sequence — supersedes the three conflicting per-auditor orders)

- **P0 — Hotfix wave** (small, ships independently of redesign):
  - Remove/repoint dead nav links: `marketing.html` (deleted file, still in live nav + webstore-info footer), `contact.html` (inventory-details footer; no contact page exists anywhere).
  - Delete 5 zombie `sendFile` routes in server.js targeting missing files (`/marketing.html`, `/top-sellers-catalog.html`, `/index-new.html`, `/embroidery-pricing-standardized.html`, `/embroidery-pricing-professional.html`).
  - Retire legacy cart flow (cart.html, pages/order-confirmation.html) per decision #5.
  - Drop 'Sale'/'Resources' placeholder nav items (internal "Back to Sales Tool" copy leaks to customers) until real content exists.
  - **Price-integrity critical**: top-sellers-product.html reads sample price from a customer-editable URL param that flows into ShopWorks orders — close the hole.
  - Branded 404 page + Express catch-all (dead links currently land on raw "Cannot GET").
- **P1 — Homepage** (in flight) **+ nwca-2026 primitives**: add the missing primitives interior pages need (forms, data/pricing tables, alert banners, toasts, interior page headers, cart components); fix the 2 AA contrast edges flagged in nwca-2026.css; split a core layer so interior pages don't pull homepage band CSS.
- **P2 — Catalog**: dedicated `/catalog` page, URL-driven state (shareable/deep-linkable, working back button), server-side decorated pricing (extend `/api/products/search` to return decoratedPrice — kills the hardcoded frontend margin formula), API-driven categories for nav/mega-menu, full facet set (backend already supports brand/category/subcategory/color/size/price + facets; UI only exposed Brand), shared customer header/footer component on every page, autocomplete v2 with thumbnails, search-first mobile drawer (today the ENTIRE top nav incl. search is display:none ≤768px — mobile customers have no product discovery).
- **P3 — Product page consolidation**: ONE templated product page on nwca-2026 replaces five implementations (product.html ~9K lines, top-sellers-product 1.9K, richardson-112-product, inventory-details, golf-tournament-product pattern). Embed API-driven decoration pricing tabs (generalize golf-tournament-product.js — the only implementation that does it right) for EMB/CAP/DTG/SCP/DTF. Add sticky quote bar, qty/tier price slider, related products, live "ships by" promise. Golf product page stays as campaign landing, restyled (decision #4).
- **P4 — Custom Hats store** (see below).
- **P5 — Share-link tier reskin**: quote-view, invoice, customer-portal, embroidery-contract-pricing, golf pair, sample-cart rebuild (no system of record today + open legacy cart API — needs its own design pass).
- **P6 — Customer calculators token-skin**: 5 staff-shared pricing calculators; strategy is "unlink shared CSS per page, NEVER edit shared files in place" (blast radius: quote-builder-shell.css etc. serve the 3 staff quote builders + quote-management dashboard).
- **P7 — Cross-cutting** (fold into each phase as pages are touched): SEO baseline (sitemap.xml, robots.txt **with staff-path disallow — the entire staff app is publicly served + crawlable on the customer origin**, canonicals, OG tags on all 28 pages), analytics/conversion tracking (currently ZERO), legal pages (privacy/terms/returns — required for a card-taking site), favicon/manifest/brand assets (favicon currently hotlinked to Caspio CDN), email capture hooks, transactional-email brand pass.

## Custom Hats store — verified facts (live API, 2026-06-11)

**Pricing chain** (`calculateCapProductPrice`, embroidery-quote-pricing.js:632): blank OSFA price ÷ per-tier MarginDenominator + EmbroideryCost(tier, 8K stitches) → `Math.ceil` (CeilDollar) → + $50 LTM/qty if cap qty ≤ 7 → + AL-CAP(tier) for back logo. Caps tier separately from garments, ALWAYS.

| Endpoint | What it serves |
|---|---|
| `GET /api/pricing-bundle?method=CAP&styleNumber=…` | Cap tiers (1-7 margin 0.55 + $50 LTM; 8-23/24-47/48-71/72+ margin 0.53), 8K-stitch cost rows ($17/$17/$13/$11/$9.50), blank sizes/costs, CeilDollar, locations CF/CL/CR/CB |
| `GET /api/pricing-bundle?method=CAP-AL` | Back-logo add-on: $6.50/$5.50/$4.75/$4.50/$4.25 per cap by tier (5K base, +$1/1K) |
| `GET /api/service-codes` | AS-CAP stitch ladder: ≤10K INCLUDED $0 / 10-15K +$4 / 15-25K +$10 → natural "logo included" price protection. DD digitizing $100 sell/$75 cost (DDT text $50, DDE edit $50) |
| `GET /api/decorated-cap-prices?brand=Richardson` | Prebuilt server-side "as low as $X" card prices (verified matches formula to the dollar) |
| `/api/product-details`, `/api/sanmar/inventory/:style`, `/api/size-pricing` | Richardson 112 fully in SanMar pipeline: 112 colors w/ COLOR_NAME+CATALOG_COLOR, CDN images, live inventory, $6.75 OSFA blank |

**Richardson 112, front logo included** (blank $6.75): qty 1 → $30 + $50 LTM = $80 · qty 6 → $38.33/cap · qty 12 → $30 · qty 24 → $26 · qty 48 → $24 · qty 72+ → $23. Back logo adds $6.50→$4.25/cap by tier. Port Authority C112 (blank $3.61): $24/$24/$20/$18/$17.

**OSFA detection**: pricing-bundle `sizes[]` is the discriminator — 112/C112 return `[{size:'OSFA'}]`; fitted caps (NE1000) return S/M / M/L / L/XL → **excluded from v1**. OSFA maps to ShopWorks Size06. Beanies are flat headwear = GARMENT pricing, not cap (`isFlatHeadwear`) — exclude from v1. ~86 of 133 Richardson styles are factory-direct with HARDCODED blank costs (`richardson-factory-direct.js`) — **v1 must stick to SanMar-carried styles** (Erik's API-pricing rule). Richardson 112's CATEGORY_NAME is EMPTY in Sanmar_Bulk (isCapProduct falls back to `^\d{2,3}$` regex); inventory has stale sized partIds with 0 qty — aggregate stock by CATALOG_COLOR, not size.

**Build approach** (from tees blueprint): clone the custom-tees chassis (~70% reused untouched: Stripe checkout + webhook idempotency ladder, server-authoritative reprice w/ 1-cent tolerance, EmailJS confirmation/shipped emails, HMAC /order-status, admin artwork views, ShipStation tracking chokepoint). **FIRST extract a channel-config registry in server.js** — channel string `'custom-tees'` is hardcoded in ~6 scattered whitelists (shipped-email gate, service banner, rush default, stock banner, art-review wording, cart-key clears); a hats order would silently get no shipped email otherwise. Write `custom-hats-pricing.js` FRESH against the EMB cap contract (NOT adapted from custom-tees-pricing.js — DTG uses halfDollarCeil/<24 LTM; caps use CeilDollar/≤7 LTM; wrong-but-plausible prices guaranteed) + jest parity lock vs internal EMB builder. Create Caspio service codes BEFORE building (CAPS-LTM?, CAPS-DIGITIZE?, CAPS-SHIP-* — pending decisions #9/#10; note CTS-SHIP-FREE-OVER's SellPrice holds the THRESHOLD dollars, a semantic trap). Replace size grid with one-click OSFA qty; replace free-placement designer with fixed-location picker (front + optional back) — cap photos break the garment-geometry heuristics, and placement is advisory anyway. Key on server-stamped locations, never composite code strings (the `_FB` indexOf bug class). Tee-specific constants that WILL silently mis-push hats if missed: designTypeId 45→EMB type, artistId 224, SW_LOC dropdown map (caps use different OnSite values), 'LTM-75' part, 'Full Color DTG' design details, 'Port & Company Core Cotton Tee' fallback name in THREE places.

**Inherited soft spots to close while touching the code**: `/api/quote-sessions/:quoteId/shipstation-tracking` has NO auth; QuoteID prefix decision is irreversible once live orders exist; `refresh=true` on quote_sessions lookups is LOAD-BEARING (cache-poisoning found live 2026-06-09); SanMar inventory proxy route caches ~5 min with no bypass.

## Custom Hats v1 lineup (curated + live-API-verified 2026-06-11, per decision #12)

Customer price = front logo INCLUDED (8K stitches assumed, ≤10K free via AS-CAP); back logo add-on $5.50/$4.75/$4.50/$4.25 by tier. All cross-checkable picks matched `/api/decorated-cap-prices` to the dollar.

| Style | Brand / Name | Role | Blank | Qty 8 | Qty 24 | Qty 48 | Qty 72 | Colors | Live inv |
|---|---|---|---|---|---|---|---|---|---|
| 112 | Richardson Trucker | Flagship classic trucker | $6.75 | $30 | $26 | $24 | $23 | 112 | 1.09M |
| C402 | Port Authority Snapback Trucker | Budget trucker | $3.79 | $25 | $21 | $19 | $17 | 54 | 474K |
| 112PFP | Richardson Printed Five-Panel | Camo (Realtree/Mossy Oak/duck) | $8.25 | $33 | $29 | $27 | $26 | 27 | 285K |
| 256 | Richardson Umpqua Gramps | Premium heritage dad | $9.25 | $35 | $31 | $29 | $27 | 19 | 134K |
| 258 | Richardson Rope Cap | Trendy rope | $7.50 | $32 | $28 | $26 | $24 | 14 | 64K |
| 220 | Richardson Relaxed Perf Lite | Relaxed/women's-friendly | $8.00 | $33 | $29 | $27 | $25 | 15 | 30K ⚠ |
| C914 | Port Authority Unstructured Twill | Budget dad cap | $3.12 | $23 | $19 | $17 | $16 | 18 | 40K |
| STC26 | Sport-Tek RacerMesh | Performance/athletic | $4.23 | $25 | $21 | $19 | $18 | 10 | 96K |
| CT105298 | Carhartt Canvas Mesh Back | Premium workwear | $13.04 | $42 | $38 | $36 | $35 | 5 | 38K |

⚠ 220 has Black=1/Navy=223 — exclude those from hero colors; standby swap = Richardson **225** (all 13 colors stocked incl. Black).

**Critical operational findings:**
- **SanMar renumbered the C1xx trucker family → C4xx.** C112/C112LP/C112ECO/C115/C119 are DEAD at PromoStandards live inventory ("Product Id not found") while Sanmar_Bulk still has healthy-looking stale rows. Build on C402; **NEVER trust Sanmar_Bulk qty fields** — stock must come from `/api/sanmar/inventory/{style}` aggregated by CATALOG_COLOR (stale 0-qty partIds exist; some live colors lack bulk rows — render the intersection).
- `/api/products/search?category=Caps` is unreliable for cap discovery (20 rows, omits Richardson; brand=Richardson facet → 0; Richardson rows have EMPTY CATEGORY_NAME). Enumerate Richardson via `/api/decorated-cap-prices?brand=Richardson`.
- Richardson 115 is FITTED at SanMar (M/L), 255 isn't SanMar-carried; Flexfit/Outdoor Cap/Pacific Headwear have zero Sanmar_Bulk rows. Rejected list (33 styles + reasons) in the hat-lineup agent output.
- Every pick has per-color FRONT_MODEL photos + FRONT_FLAT/BACK_FLAT + swatches; none have side views.
- Cap tiers verified live: 8-23/24-47/48-71/72+ all MarginDenominator 0.53; Embroidery_Costs Cap@8000 = $17/$13/$11/$9.50; CeilDollar. The 1-7 tier (0.55 + $50 LTM) is moot with the 8-cap minimum.

## Price-integrity criticals (Erik's #1 rule violations found — fix in P0/P3)

1. **top-sellers-product.html**: customer-tamperable URL price param → ShopWorks orders (P0).
2. Hardcoded 0.57 margin + $15 embroidery math in frontend catalog/product code; catalog prices computed client-side from hardcoded formula (P2 server-side decoratedPrice kills it).
3. Hardcoded 10.1% tax, $10 shipping, $300/$100/$2,000 webstore fees, golf marketing prices in frontend files.
4. Silent fallbacks: richardson-112-product renders hardcoded product data on API failure; catalog service layer caches computed prices 5 min + silent fallback paths.
5. admin/c112-bogo-promo.html has hardcoded $17/$16 promo prices — do NOT copy patterns from legacy cap pages into the hats store.

## Mobile / a11y migration contract (every redesigned page)

Worst-mobile ranking: inventory-details, cart, webstore-info, top-sellers-showcase, top-sellers-product, richardson-112, dtg-compatible, screenprint-customer, product.html, embroidery-contract-pricing, golf pair, 3-day-tees (best). Standards: nwca-2026.css only base; zero inline style/script; one external CSS + JS per page; 44px targets; skip link; ARIA dialogs/tabs w/ focus trap + Escape; aria-live for async regions; escapeHtml all API data; visible fatal-error card (port custom-tees pattern); AA contrast (legacy #4cb354 on white = 2.96:1 FAILS — never reuse).

## Open follow-ups (from completeness critique)

- Staff app publicly served/crawlable on customer origin → robots.txt disallow now; auth gating is a separate future project.
- Server-side caching globally disabled; no performance measurements taken — measure during P2/P3 with preview tools.
- Transactional emails (EmailJS templates) are unaudited customer brand surfaces; hats store needs its own set. Erik still owes: delete stray "```html" line in EmailJS customer template (pre-existing).
- Sample-cart end-to-end flow (no system of record, open legacy cart API) — dedicated design pass in P5.
