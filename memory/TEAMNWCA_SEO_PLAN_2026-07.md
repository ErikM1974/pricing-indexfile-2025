# teamnwca.com SEO Plan v2 — coordinated with the .net recovery roadmap
*2026-07-14. Companion to `reference/net-seo-recovery-roadmap-2026-07.html` (Mehar's .net plan),
`reference/net-local-page-playbook-2026-07.html` + `reference/net-local-landing-page-template-2026-07.html`
(the local-page build kit for Mehar), and `memory/blog-content-differentiation.md` (Lane Split v2).
The two plans are designed to run in parallel WITHOUT competing — one business, two query classes.*

## The one-paragraph strategy

.net rebuilds **local + service** visibility ("custom embroidery near me", city pages, GBP).
teamnwca owns **product + style + tools** visibility (style numbers, brand catalogs,
comparisons, quote builders). Every .net page funnels product/pricing intent to teamnwca;
every teamnwca post cross-links .net for education and (new) local-service intent. Neither
site ever writes the other's query class. Success = combined non-brand commercial traffic
and quote requests, measured monthly, October 2026 checkpoint.

## What's already working — protect, don't churn

| Asset | State | Rule |
|---|---|---|
| SSR blog + sitemap + GSC | ✅ live, 5 published / 25 drafts banked | Monday autopilot publishes 1/wk; never burst-publish |
| Product SSR heads (3,197 styles, method-aware titles) | ✅ 92% specific | Never per-method URLs (doorway trap) |
| Product_Copy overlay | 546 styles seeded | Continue tail batches (~2,650 left), insert-only |
| Brand landing pattern (`/custom-carhartt`) | ✅ live, FAQ schema | Replicate — see wave 2 below |
| Blog↔PDP internal links (`/api/blog-product-map`) | ✅ self-wiring | New posts auto-appear on product pages |
| Entity stitching (`sameAs` both domains) | ✅ live | Ties both sites to one Knowledge-Graph business |
| GSC verify route + self-hosted favicon | ✅ | NEVER remove the `google9a8d...html` route |

## Phased plan (mirrors .net's phases so the sites compound)

### Phase 1 — now → week 3 (while Mehar fixes .net cannibalization)
1. **Keep the 1/wk blog cadence** (autopilot). Queue order: finish the style showdowns
   (zip-or-pullover, csv405-vs-csv400), then `premium-branded-jackets-eddie-bauer-north-face-...`
   **early** — it's the funnel target for .net's Eddie Bauer post (118 visits/mo); once live,
   ask Mehar to CTA-link it (his Phase 1 item 3 = adding those CTAs anyway).
2. **Autopilot collision check v2**: skip any topic matching .net's *local/service recovery
   targets*, not just its educational posts (rule now in blog-content-differentiation.md).
3. **Give Mehar the funnel-target list**: for each of .net's top informational pages, the
   teamnwca page its CTA should point to (Carhartt→/custom-carhartt ✅; Eddie Bauer→premium
   jackets post; cotton-vs-poly→pc54-vs-pc61 ✅; hoodie print-method→pc90h-vs-18500 ✅ reverse
   link exists, ask for the forward link).

### Phase 2 — weeks 3–8 (while Mehar builds local pages)
4. **Brand landing wave 2** (the proven /custom-carhartt pattern: static page, curated grid
   w/ real order stats, FAQ schema, no prices in schema): `/custom-safety-vests` (pairs with
   csv405 post + CornerStone hi-vis copy), `/custom-polos` (K500/LST253 + polos post),
   `/custom-new-era` (106 styles already copy-seeded). One per ~2 weeks, each added to
   sitemap-pages.xml.
5. **Every new .net local page links teamnwca** — "browse 3,000+ styles / get instant
   pricing" block pointing at the catalog + quote builders. This is Mehar's build; supply
   him the standard block (HTML snippet) so it's copy-paste.
6. **Product_Copy tail batches** continue (PA/P&C/District/OGIO/Nike rests) — each batch
   improves PDP meta + AI-answer quality for pages the blog links to.

### Phase 3 — weeks 6–14 (while Mehar pivots .net content)
7. **Claim the tool/comparison topics the .net roadmap floated** (they're ours by Rule 9):
   publish teamnwca posts/pages for "screen printing vs embroidery for uniforms"
   (product-anchored: which garments, which styles, link both builders) and a
   "custom apparel pricing: how quoting works" explainer that CTAs the quote builders —
   so when Mehar's Phase 3 arrives, the pages already exist to link instead of build.
8. **AI-search structure on every post** (this is the improvement .net's 497 citations
   prove out): open with a "The short answer" block, direct-answer H2s, FAQ schema on
   roundup posts. Retro-fit the 25 banked drafts opportunistically as they publish.
9. **Industry drafts publish through fall** (breweries, landscaping, auto shops… already
   banked) — each is a "custom [industry] apparel" commercial query .net doesn't hold.

### Phase 4 — ongoing
10. **Backlinks**: teamnwca inherits the "since 1977" story for PR anchored on the
    *catalog/tools* angle; supplier/brand directory listings (SanMar, Carhartt authorized
    decorator) should list BOTH domains. .net (615 ref domains) linking teamnwca sitewide
    (footer, done) passes authority to the newer domain (AS7).
11. **Monthly review** (with Mehar's report): GSC non-brand clicks both properties, SEMrush
    keyword counts (teamnwca baseline 42 @ 2026-07-12), Forms Inbox quote requests with
    blog/product referrers. **October 2026 checkpoint** re-examines the merge question.

## Hard rules (unchanged + new)
- Rule 9: no price ever appears outside the engine — on EITHER domain (flag .net's
  /dtg-calculator/ for retirement).
- Never duplicate .net text; never chase "near me"/city queries; never per-method product
  URLs; slugs never change after publish.
- Burst-publishing stays off — steady 1/wk beats dumps (Google treats sudden volume on a
  young blog as a spam signal).

## Local-page kit → teamnwca touchpoints (from the playbook + template, 2026-07-14)

The Tacoma template is solid for .net's lane. Four coordination items it doesn't yet
include (feed these into the email/brief for Mehar):
1. **Catalog cross-link block on every city page** — the template's service grid links only
   .net service pages. Add one block per page: "Browse 3,000+ styles with live pricing →
   teamnwca.com" (catalog + quote builders). Local visitor lands on .net, transacts on
   teamnwca. Keep .net's own quote CTA primary — this is the *catalog* path, not a
   replacement lead form.
2. **Brand mentions → teamnwca brand pages** — the template's "Why us"/FAQ name Carhartt,
   Nike, North Face, Richardson. Link "Carhartt" to `teamnwca.com/custom-carhartt` (live);
   as wave-2 brand pages ship (safety vests, polos, New Era), same treatment.
3. **NAP consistency is now CROSS-DOMAIN — ✅ VERIFIED 2026-07-14 against GBP screenshot**:
   `2025 Freeman Rd E, Milton, WA 98354 · (253) 922-5793` — teamnwca's Organization JSON-LD
   (index.html) matches. This exact NAP is the only one either site ever prints (entity
   stitching = one business; a mismatch on either side weakens Mehar's local signals).
   GBP also shows **4.9★ / 187 reviews** (2026-07-14) — the template's [RATING]/[REVIEW_COUNT].
4. **Template FAQ claims to confirm against real policy** — "same-day DTG", "7–10 business
   days", "no minimum DTG": confirm these match what teamnwca's tools/pages state, so the
   two domains never publish contradictory turnaround/minimum claims (same class of problem
   as prices outside the engine, softer stakes).

## Coordination with Mehar (.net SEO writer)
- **Mehar's email: umehar56@gmail.com** — Claude does NOT email him directly; questions
  about .net SEO go through Erik ("Erik, please ask Mehar…"). Gray-area topics (could go
  either site) → flag to Erik BEFORE writing (collision protocol §7 of the guide).
- **✅ Packet SENT to Mehar 2026-07-14** (roadmap + playbook + template + coordination
  guide) — the 3 roadmap amendments and 4 template additions are relayed; Mehar is building
  .net's local/service lane per the guide. Every future teamnwca blog/page must honor Lane
  Split v2 (never local/"near me"/city, never educational-craft, never anything .net covers
  with the same intent — cross-link instead).

## Open asks
- **Erik**: green-light brand-page wave 2 order (safety vests first is my recommendation —
  it pairs with the queued CSV405 post and the hi-vis Product_Copy already seeded).
