# 📂 Dashboard & Admin

### Handbook PDF Generator (`/scripts/handbook-pdf/` — NEW 2026-07-10)

| File | Purpose | Dependencies | Status |
|------|---------|--------------|--------|
| `/scripts/handbook-pdf/build_handbook_docx.js` | Rebuilds the printable Employee Handbook booklet (forms/Employee-Handbook-Latest.pdf) from LIVE hub chapters — cover w/ logo, TOC w/ page numbers, punch margins, per-chapter ghost-page compaction | npm docx + node-html-parser, Word COM for PDF export | ✅ Active |
| `/scripts/handbook-pdf/README.md` | Run instructions + ghost-page check recipe | — | ✅ Active |
| `/scripts/handbook-pdf/logo.png` | NWCA logo for the cover (from Caspio CDN) | — | ✅ Active |

### New Dashboard Pages (NEW 2026-04)
| File | Purpose | Dependencies | Status |
|------|---------|--------------|--------|
| `/dashboards/ae-mission-control.html` | Mission Control: six responsive tabs, original AE figures and records, native kit/outreach/full-list dialogs. Scoped shared controls and invoice/inbound/box-label presentation. | staff-mission-control.css, vendor-invoice.css, vendor-inbound.css, vendor-box-labels.css, ae-mission-control.js | Active; CSS draft under review |
| `/dashboards/js/ae-mission-control.js` | Mission Control controller. Identity via `/api/crm-session/me` + admin view-as, `/api/crm-proxy/ae-dashboard/summary` aggregate (per-panel visible errors), kit-request + outreach modals, stale-quote reopen via builder `?duplicate=`, art toasts (45s poll, `document.hidden`-guarded), rep-filtered inbound-today; esc() on every value. **2026-07-26**: `TABS` registry of function references plus `mountTab`/`renderMountedTabs` for lazy per-tab loading; `qs(refresh)` replaces six copy-pasted param builders (so Refresh now reaches all 7 cacheable endpoints, not just the summary); `dataOnce()` promise memo (two cards fed by one endpoint means one request — caches PROMISES not payloads, so stale data can never paint as fresh); `switchRep()` single entry point (fixes inbound showing the previous rep's rows); local ES5 `sparkSvg` + `countUp` (paints the true value BEFORE animating — rAF is throttled to zero in a background tab, which left the bonus reading $0); The One Thing ranked client-side from data already fetched, escaped at ONE chokepoint, localStorage skip with a 14-day TTL and a visible 'N skipped' affordance; since-last-looked diff (an annotation on live values only); confetti that seeds-but-does-not-fire on first sight and never during admin view-as. **2026-07-27 — Call List tab**: the three "who to call" lists moved off Money into their own tab as ONE ranked order of work with tap-to-call numbers and a log-a-call strip writing `Last_Contact_Date`/`Contact_Status`/`Next_Follow_Up` to the rep's own account table. Rows past the first 15 have no contact data until "See more" fetches it, so it uses a separate `progressive` render + `.aemc-loadmore` class — **NOT `expandableRows`**, which renders everything and hides the overflow, and whose `.aemc-more-toggle` has a document-level handler that would double-fire. Log controls are disabled under admin view-as (Erik holds both rep roles, so the write would land on her CRM page under her name); dates are built from local parts, never `toISOString()`, which is tomorrow after 5pm Pacific. **2026-07-27 — banner redesign (Claude Design "Briefing", stage 1 of 3)**: the hero answers "what does this pay me?" in money instead of a percentage — a 176px SVG dial plus three payout scenarios (locked in / at today's pace / at goal). ⭐ **ONE AXIS**: percent-of-goal 0–100 drives the dial arc, the dial's 85% tick, the track fill, the start mark, the pace flag and both endpoint labels; the old 130% display ceiling is gone (it put the 85% start line at 65% of the bar). Overflow past goal is clamped for drawing only — the label still reads the true percentage. The 85% caption owns its own row: sharing one absolutely-positioned line with the endpoint labels made it overlap "goal $66,609" below ~1300px. | DashPage, DashTabs, crm-proxy forwarders, sanmar-inbound-today.js, confetti.js | ✅ Active |
| `/dashboards/js/ae-dashboard-init.js` | **NEW (2026-09-04)** AE Dashboard bootstrap — was the inline <script> on the page (Rule 3). Initialises the galleries + Ruth's form, owns the Item-Type control (lazy-mounts each form, WAI-ARIA arrow keys), the ONE `data-action` click delegator (More menu, tab items, Grid/Board toggles, Requirements jump links — no onclick attributes remain), and fills the Requirements tab's art fees from `/api/service-codes` (`data-fee-price/rate/frac/name` hooks; visible warning when a code is missing or the API fails). | app-config.js, ae-dashboard.js, the 4 forms, art-ae.js, mockup-ae.js | ✅ Active |
| `/shared_components/css/staff-mission-control.css` | Scoped Mission Control owner using canonical tokens and shared controls; replaces dashboards/css/ae-mission-control.css. | tokens.css, components.css | Active |
| `/dashboards/past-due-orders.html` | **2026-09-04 review (10 items)**: stat tiles carry the dollar value under the count (`.pdo-stat-sub`), the no-PO tile says "Past due, no PO raised", card header gets a summary ("52 orders on the board · 4 reps") + "loaded h:mm", site favicon. Lock: `tests/unit/past-due-orders-page.test.js`. **NEW (2026-08-10)** Past Due Orders — every ShopWorks order that missed its requested-ship date and is neither shipped nor invoiced, grouped by sales rep, with the blanks status (no PO raised / ordered / partial / received) from the PurchaseOrders mirror. **NOT SanMar-only** — the SanMar Inbound board only sees orders with freight arriving, which is a fraction of the shop; vendors here include Logomark, Augusta, Richardson, Supacolor and "no PO raised yet". Window selector 30/60/90d, print stylesheet for the mono laser. Any logged-in staff (unlisted in `Staff_Page_Access` = any-staff, and that is deliberate — production, receiving and purchasing all have reason to look). Backed by the company-wide branch of the same `buildDueDates()` that feeds Mission Control's per-rep card, so the two can never disagree. | dash-shell.css, dash-page-helpers.js, past-due-orders.css/js, `/api/crm-proxy/ae-dashboard/due-dates-all` | ✅ Active |
| `/dashboards/js/past-due-orders.js` | Past Due Orders controller. **2026-09-04**: rep headings link to nika-crm / taneisha-crm / house-accounts (`REP_PAGES`), "no PO raised" cells link to the Purchase Request form (JotForm embed — cannot prefill the WO; the link title carries it), vendor shown under the blanks status, per-rep dollar totals, zero values render "—", column "Late · Due in", 5-minute refresh while the tab is visible + catch-up on return, "loaded h:mm" stamp. ⚠️ Uses a plain **same-origin** `fetch` with `credentials:'same-origin'`, NOT `DashPage.fetchJson` — fetchJson prefixes `APP_CONFIG.API.BASE_URL` (the Heroku proxy), and this endpoint is a `requireStaff` forwarder on THIS server that attaches the CRM secret; routing it through the proxy base would 401. On failure it renders nothing and shows the error banner, because a blank table would read as "nothing is late". | DashPage (showError/hideError), `/api/crm-proxy/ae-dashboard/due-dates-all` | ✅ Active |
| `/dashboards/css/past-due-orders.css` | Past Due Orders page styles (art-hub tokens only). **2026-09-04**: header controls wrap on phones ≤760px (the control row was 781px wide at 375px and scrolled the page sideways); rep-link, no-PO link, vendor line, stat sub-values. Past-due rows carry weight as well as colour so they survive greyscale; `@media print` forces the late badge to solid black because the shop's mono laser drops backgrounds — same rule as the SanMar inbound sheets. | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/css/ae-dashboard.css` | **NEW (2026-09-03)** AE Dashboard page shell: the maroon `:root` theme (moved out of an inline `<style>`), the `[hidden]` guard, the maroon canvas band the nav + content cards float over, page intro, `.tab-title` accent bar, `.ae-tab-head` title rows, segmented Grid/Board toggle, Review-tab loading/empty states, and the Transfers / Personalization launcher cards (`.ae-launch-card`, rendered by ae-dashboard.js from its SECTIONS registry). **Person colour-coding (2026-09-04)**: the whole `--art-theme*` family is re-scoped per pane — Steve panes (`#submit-tab`, `#view-tab`, `#review-tab`) GREEN, Ruth panes (`#mockup-ruth-tab`, `#digitizing-tab`) PURPLE, `#transfers-tab` Bradley's slate BLUE, `#personalization-tab` shop-floor BLUE — so gallery card headers, status chips, links and toggles recolour without touching gallery code. **Must be the LAST stylesheet on the page** — it owns the theme | art-hub.css tokens, ae-nav-v2.css | ✅ Active |
| `/dashboards/drive-access.html` | **NEW (2026-08-24)** Drive Access Center — read-only reference for the NCA-FS01 network drive map: which letters auto-map for each person or login, and who can Modify / Read only / Full control the audited folders. Admin → Access & Policy. **Admin-only**: listed in `ADMIN_DEFAULT_PAGES` (`lib/page-access.js`), so with no `Staff_Page_Access` row it is admin-only rather than any-staff; add a row in Access Admin to widen it, no deploy. By person / by drive toggle, search, print. Nothing here edits a mapping, an ACL or an account. | drive-access.css/js, `GET /api/staff/drive-access` | ✅ Active |
| `/dashboards/js/drive-access.js` | Drive Access Center controller. 🔴 **The snapshot is deliberately NOT in this file.** `/dashboards` gates `*.html` only, so this `.js` and its `.css` are served anonymously — UNC paths, share names and the file-admin account names would be public. It fetches them from the gated `/api/staff/drive-access` instead. Every stat is derived from the snapshot (a hand-typed count goes stale silently), and a group whose members hold different rights is broken out per person rather than rolled into one contradictory chip list. On any failure it renders the error and no cards, because an empty grid would read as "nobody has access to anything". | `GET /api/staff/drive-access` | ✅ Active |
| `/dashboards/css/drive-access.css` | Drive Access Center styles. MCA package layout recoloured to the house NWCA green (`#1a472a` / `#3a7c52` / `#4cb354`, page bg `#f5f7f5`), matching access-admin.css. `@media print` keeps colour on (retired-red and modify-vs-read-only carry meaning here, unlike the mono-laser sheets). | — | ✅ Active |
| `lib/drive-access-data.json` | The drive-map snapshot itself — server, policy, `verifiedAt`, 9 login profiles, 9 drives. Lives in `lib/` because that directory is **never statically served**; anywhere under a static mount would publish it. To update: re-verify the GPO, recursive AD group membership and folder ACLs, edit this file, bump `verifiedAt`, deploy. | served by `GET /api/staff/drive-access` | ✅ Active |
| `/staff-dashboard-v3/past-due-badge.js` | **NEW (2026-08-10; host moved to the header chip `.ws-pd[data-section="past-due-orders"]` 2026-09-03)** Live past-due count on the Past Due Orders link. Three states: red count when something is late (via `data-pastdue` on the host), green "All clear" on a **confirmed** zero, and **no badge at all** when the count can't be fetched — an unverified all-clear is the exact shape of Erik's #1 rule. Polls every 10 min, skips hidden tabs, and re-checks on tab focus. Same-origin fetch (requireStaff forwarder), not the proxy base. | `/api/crm-proxy/ae-dashboard/due-dates-all` | ✅ Active |
| `/dashboards/purchasing-portal.html` | **2026-09-05 review (10 items)**: stat tiles are BUTTONS that filter the table (accent tile clears; "Invoiced / Shipped" = both finished statuses), each with a sub-line (Ordered says how many are partial); a "Request → PO, median" turnaround tile computed from the rows; "Show finished" toggle (open work is the default view); `#pp-trunc` warning when the feed reports `truncated`; "N of M shown"; site favicon; phone hides requester/type/ordered/received. Lock: `tests/unit/purchasing-portal-page.test.js`. **NEW (2026-07-19)** Purchasing Portal — company-wide view of every blanks-purchase request sent to Bradley (JotForm Purchasing form, 60d) with requester, WO#/company, Bradley PO#, vendors, ordered/received dates, and status ladder (Sent→Ordered→Received→Invoiced/Shipped). Stat tiles + search + requester/status filters. Any logged-in staff; tile in staff-dashboard Frequently Used; Mission Control purchasing card links here. | dash-shell.css, dash-page-helpers.js, purchasing-portal.css/js, /api/crm-proxy/purchasing-portal | ✅ Active |
| `/dashboards/js/purchasing-portal.js` | Purchasing Portal controller. **2026-09-05**: renders `payload.truncated` (25 of 275 requests were silently missing), open-work default + finished toggle, tile filters (`applyTile`), JotForm submission link per row (`https://www.jotform.com/submission/{id}`), request→PO turnaround (median h + same-day share), Refresh → `?refresh=1` through server.js to the proxy, `cleanVendors()` trims/dedupes, aria-labelled invoice buttons, 5-min visible-tab refresh, "loaded h:mm". Base: same-origin `/api/crm-proxy/purchasing-portal` fetch (proxy joins JotForm × PurchaseOrders × ORDER_ODBC), flattens to one row per work order, client search + requester/status filters, SanMar Invoice buttons → shared SanMarInvoiceViewer; esc() everywhere, visible errors | DashPage, crm-proxy forwarder, sanmar-invoice-viewer.js | ✅ Active |
| `/shared_components/js/sanmar-invoice-viewer.js` | **NEW (2026-07-19)** Shared SanMar invoice modal (`window.SanMarInvoiceViewer.open({wo,company,pos,orderedDate})`) — fetches `/api/sanmar-invoices/by-po/:po?orderedDate=` per PO, renders a paper-style invoice document (header/terms/ship-to/line items/totals/Paid-Unpaid badge) + Print/Save PDF via print-sheet pattern. Used by Purchasing Portal AND AE Mission Control so the invoice never drifts between surfaces | APP_CONFIG, sanmar-invoice-viewer.css | ✅ Active |
| `/shared_components/css/sanmar-invoice-viewer.css` | Shared invoice-viewer styles (modal shell, paper-style invoice doc, `#smiv-print-sheet` @media print isolation) | art-hub.css tokens (with fallbacks) | ✅ Active |
| `/dashboards/css/purchasing-portal.css` | Purchasing Portal styles (2026 tokens; **2026-09-05**: tile buttons + `.is-active`, KPI tile, finished toggle, truncation notice, request link, status chips on `--state-*` tokens (were hex), `.pp-col-phone-hide` ≤640px; stat grid reuse, filter row, scrollable table, status chips matching Mission Control's purchasing palette) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/sanmar-payables.html` | **NEW (2026-07-20)** SanMar Payables — native replacement for the Caspio "Sanmar Vendor Portal" embed + email parse. TWO tabs: **Invoices = OPEN payables worklist** (SanMar `GetUnpaidInvoices` = what we still owe, vendor 1002 only, recent-90d default w/ older-items hint; **upload the ShopWorks payables export** → per-row Imported?/Paid? status + status filter [not-imported / to-pay / all-owed / everything]; per-row viewer; **Download ShopWorks 1002 CSV**) and **Marketing Fund** (SanMar 2026 allotment $35,110.95 → vendor 2425; remaining = allotment − net MRKFUND YTD, year-end projection, month bars, 2425 CSV). MRKFUND excluded from Invoices entirely. Admin+accountant via Staff_Page_Access. ODBC auto-sync of ShopWorks payables replaces the upload later. | dash-shell.css, dash-page-helpers.js, sanmar-payables.css/js, sanmar-invoice-viewer.js, /api/staff/sanmar-invoices/{unpaid,by-date} | ✅ Active |
| `/dashboards/js/sanmar-payables.js` | SanMar Payables controller — Invoices tab: `/api/staff/sanmar-invoices/unpaid` (open ledger, exclude MRKFUND, date-window filter) cross-referenced against an uploaded ShopWorks payables CSV (inline RFC-4180 parser; `normInv()` matches SanMar raw ↔ ShopWorks INV-/CR-/FTC-; Imported=row exists, Paid=date_Paid set) → status badges + filter, default = not-imported+unpaid; ShopWorks 1002 CSV export (M/D/YYYY, INV-/CR-, `$`-amounts, csvCell+BOM). Marketing tab: quarterly YTD `/by-date` filtered to MRKFUND, editable allotment, remaining/projection, month bars, 2425 CSV. SanMarInvoiceViewer drill; esc() everywhere; visible errors. Test seam `window.__SMP_TEST_SW__`. | DashPage, sanmar-invoice-viewer.js, /api/staff/sanmar-invoices/{unpaid,by-date} | ✅ Active |
| `/dashboards/css/sanmar-payables.css` | SanMar Payables styles (2026 tokens; tabs, stat grid, date/PO/invoice filter row, scrollable table w/ checkbox column, credit-row highlight, MRKFUND/credit/zero badges, marketing progress bar + monthly spend bars, export footer) | dash-shell.css, art-hub.css | ✅ Active |
| `/tests/ui/test-sanmar-payables.html` | SanMar Payables UI harness — real controller + CSS over stubbed fetch (no SAML/live SanMar); exercises both tabs, terms-aware checkbox logic (Net30/credit checked, MRKFUND disabled, $0 unchecked), net-amount math, marketing fund tiles/projection/month bars, XSS escaping | sanmar-payables.css/js, test-sanmar-payables-stub.js | ✅ Active |
| `/tests/ui/test-sanmar-payables-stub.js` | Fetch stub for the SanMar Payables harness — window-filtered by-date fixture (Net30 + credit + $0 + MRKFUND across the year, XSS probe) + by-po viewer payload | — | ✅ Active |
| `/tests/ui/test-ae-mission-control.html` | Real Mission Control markup and six tabs over local stubs. Native dialogs included in the enforced synchronization contract. | Same CSS owners/controller as the application; test-ae-mission-control-stub.js | Active |
| `/tests/ui/test-ae-mission-control-stub.js` | Fetch stub for the Mission Control harness — crm-session, ae-dashboard summary (per-rep fixtures plus `trend`/`quoteConversion`, XSS probes), bonus/growth/purchasing/due-dates/data-quality fixtures, finished-photos library, marketing-shipments, lead-outreach, inbound-today, art-notifications. **`?as=rep|nika`** stubs a NON-admin session — the rep experience that actually ships was previously untestable, because several behaviours are deliberately admin-suppressed (view-as pill hidden, no confetti, no diff-baseline write). **`?degrade=trend,quotes,photos,empty-quotes,empty-photos`** forces the null/zero shapes so degraded and empty states get designed too. ⚠️ **RENDER harness, never a CONTRACT harness**: it replaces `window.fetch` wholesale, so it answers URLs whether or not the route exists — that is exactly how the missing `due-dates` forwarder 404'd in production for a week while this stayed green. Route registration is verified by live probes only. | — | ✅ Active |
| `/scripts/sync-test-harness.js` | **NEW 2026-07-27** Copies the shared markup regions (tablist, bonus hero, all six `.mc-panel` sections, drawer shells) from `dashboards/ae-mission-control.html` into the harness. `--check` reports drift and exits 1 without writing. Run via `npm run sync:harness`. Exists because the harness MUST be a fork — the real page is SAML-gated and can't be fetched at test time — and that fork silently kept the old hero, tab bar and Today panel across three changes on one day, rendering a page that no longer shipped while reporting green. The shared set is declared once in `SHARED_REGIONS`; a region that legitimately needs to differ gets removed from that list with a reason, never a loosened comparison. | — | ✅ Active |
| `/tests/unit/art-submit-identity.test.js` | **NEW 2026-08-01** Locks the submitter-identity contract for all 4 AE art submit forms. `getSubmitterEmail()` used to end `\|\| 'ae@nwcustomapparel.com'` — an inbox nobody owns — so a missing staff session saved the request under a bogus `User_Email` AND sent the AE's confirmation into the void (14 rows did this; Steve still got his because his address is hardcoded, which is why it hid). Unidentified must now resolve to `''` so `handleSubmit` blocks visibly. Two of the six cases GREP ALL FOUR FILES so no form can quietly reintroduce the fallback or drop the guard. Mutation-checked. | garment/sticker-banner/jds/mockup-submit-form.js (`_getSubmitterIdentityForTest`) | ✅ Active |
| `/tests/unit/harness-drift.test.js` | **NEW 2026-07-27** Fails the build when the Mission Control harness drifts from the page. Asserts every `SHARED_REGIONS` block is byte-identical, plus two coverage checks derived from the page itself: every `.mc-panel` is in the guarded set (the hole that let the Call List panel drift), and every `data-tab` has a matching panel. Runs in `npm test` and `npm run test:guards`. | scripts/sync-test-harness.js | ✅ Active |
| `/dashboards/embroidery-bonus-plan.html` | **NEW 2026-07-25** Rep-facing Q3 2026 embroidery bonus plan — the three ways to earn, why Nika's and Taneisha's goals differ, and what counts. Linked from the Mission Control hero ("How this works") and the Flask bonus report's **Bonus Plan** button. Print stylesheet produces a clean PDF handout for a 1:1. A live page rather than a static PDF on purpose: the two `2026_Bonus_Cheat_Sheet_*.pdf` files it replaces drifted out of sync with the code within months. **Rewritten 2026-07-26**: the rung table became the continuous rate ($60/point above 85%), and the page now states two things a rep would otherwise discover at payout time — webstore accounts don't count (they pay through the online-store commission), and the team kicker measures a **wider** scope than the individual bonus (all company embroidery, webstores included, $310K/$340K). **2026-07-26: no figure is typed into this file any more** — every number loads from the config API via `embroidery-bonus-plan.js`, after a Caspio baseline edit left the page contradicting the rep's own dashboard. 🔒 `Staff_Page_Access` row restricts it to the two AEs + admin (it shows payout amounts). | embroidery-bonus-plan.css, embroidery-bonus-plan.js, art-hub.css | ✅ Active |
| `/dashboards/css/embroidery-bonus-plan.css` | **NEW 2026-07-25** Styles for the bonus plan page — standalone (no dash-shell), readable on a phone, with an `@media print` block that flattens the hero, hides buttons and avoids mid-card page breaks. Carries a `[hidden] { display: none !important }` guard, because `.ebp-note` is `display:flex` and `.ebp-table` is `display:table` — both beat the bare attribute the loader toggles. | — | ✅ Active |
| `/dashboards/js/embroidery-bonus-plan.js` | **NEW 2026-07-26** Fills every figure on the bonus plan page from `/api/crm-proxy/embroidery-bonus/config` — bounties, rate, dormancy, minimum, kicker tiers, and the whole rate table (goals, thresholds, per-point payouts). **The HTML ships with em dashes, not numbers**, so a hardcoded value can never be mistaken for a live one: `Rep_Bonus_Config` is no-deploy editable, and the first baseline change (Taneisha $89,039 → $66,609) left the handout contradicting her dashboard in five places with nothing to warn. On failure the dashes stay and a banner explains; on `configSource: 'fallback'` the numbers render but are flagged as possibly not the approved plan. | `/api/crm-proxy/embroidery-bonus/config` (SAML, AEs + admin) | ✅ Active |
| `/tests/ui/test-embroidery-bonus-plan.html` | Harness for `embroidery-bonus-plan.js` — four scenarios: success, config-fallback, network failure, HTTP 500. The failure paths are the point: they prove a Caspio outage cannot put a stale goal in front of a rep, which is invisible in normal use. DOM mirrors only the ids and `data-ebp` slots the script touches. | embroidery-bonus-plan.js/.css, test-embroidery-bonus-plan-stub.js/.css | ✅ Active |
| `/tests/ui/test-embroidery-bonus-plan-stub.js` | Fetch stub for the plan-page harness. Config payload is the **real shape** returned by `/api/embroidery-bonus/config` on 2026-07-26. The fallback scenario deliberately uses the OLD pre-webstore baselines ($235,000/$100,000) — the whole point of the flag is that those numbers look perfectly plausible. | — | ✅ Active |
| `/tests/ui/test-embroidery-bonus-plan.css` | Harness chrome for the plan-page test. External rather than inline because CLAUDE.md Rule 3 bans `<style>` content in HTML. | — | ✅ Active |
| `/tests/ui/test-embroidery-bonus.html` | Embroidery Bonus card harness — real controller + v3 CSS chain over stubbed fetch (no SAML/Caspio). Verifies the Q3 date range, fallback-config warning banner, per-rep bounty/ladder/total render, team kicker, and the "Who to call" dormant list. Loads `/staff-dashboard-v3/config.js` (NOT `shared_components/js/app-config.js`, which sets a different global) because `dashboard-endpoints.js` throws without `APP_CONFIG.API.BASE_URL`. | embroidery-bonus-controller.js, staff-dashboard CSS chain, test-embroidery-bonus-stub.js | ✅ Active |
| `/tests/ui/test-embroidery-bonus-stub.js` | Fetch stub for the Embroidery Bonus harness. Payload is **real captured output** from `computeEmbroideryBonus`/`computeDormant` against live ORDER_ODBC (2026-07-25), so the harness renders the same numbers the verified backend produces. Regenerate by re-running the route helpers and dumping to JSON. | — | ✅ Active |
| `/tests/ui/test-inbound-print.html` | **NEW (2026-07-22)** SanMar Inbound print-profile harness — loads the real `sanmar-inbound-today.js` over mock inbound data (no live API), opens the modal, and renders all 6 "Print for…" recipient reports + the dropdown into static preview panes for eyeball/screenshot. **2026-07-29**: asserts the pre-print re-pull (1 forced refresh / 2 inbound calls across 8 output actions) and waits on MutationObserver, never `setInterval` — a backgrounded tab throttles timers and stalls the run | quote-management.css, **sanmar-inbound.css**, sanmar-inbound-today.js, test-inbound-print.js | ✅ Active |
| `/tests/ui/test-inbound-print.js` | Harness driver + mock — 6 orders (2 AE reps + unassigned, multi-method, box-detail vs lines-only, backorder/short-ship, one received-excluded), mocks fetch/print, clones each print sheet into a visible frame | — | ✅ Active |
| `/tests/ui/test-purchasing-portal.html` | Purchasing Portal UI harness — real controller + CSS over stubbed fetch (no SAML/JotForm/Caspio); exercises stats, table, search + requester/status filters, XSS escaping | purchasing-portal.css/js, test-purchasing-portal-stub.js | ✅ Active |
| `/tests/ui/test-purchasing-portal-stub.js` | Fetch stub for the Purchasing Portal harness — one purchasing-portal payload with all 6 statuses, 3 requesters, and an XSS-probe company name | — | ✅ Active |
| `/tests/ui/test-finished-photos-library.html` | Finished Photos Library UI harness — real controller + CSS over stubbed fetch (no SAML/Caspio/Box); exercises stats, rep chips, account grouping, search/visibility filters, publish toggle, lightbox, XSS escaping | finished-photos-library.css/js, test-finished-photos-library-stub.js | ✅ Active |
| `/tests/ui/test-finished-photos-library-stub.js` | Fetch stub for the library harness — 5 photos across 3 accounts (2 reps + house bucket, XSS-probe company), inline-SVG images, stateful PATCH publish/hide | — | ✅ Active |
| `/tests/ui/test-phase1-widgets.html` | Phase 1+2 widgets harness — real Pride Wall/My Stuff/Command Palette controllers over mocked APP_CONFIG + fetch; exercises 6-tile wall rotation, pin/unpin persistence, Ctrl+K open/search/keyboard-nav/copy-toast (Win Bell removed 2026-07-23) | phase1-widgets.css, command-palette.css, test-phase1-widgets-mocks.js, test-phase1-widgets.js/.css | ✅ Active |
| `/tests/ui/test-phase1-widgets-mocks.js` | Mocks for the Phase 1 harness — APP_CONFIG, quote_sessions (1 paid CAP + 1 Accepted + 1 ignored), 8 inline-SVG library photos, localStorage scenario seeding | — | ✅ Active |
| `/tests/ui/test-phase1-widgets.js` | Module entry for the Phase 1 harness — imports + inits the Pride Wall + My Stuff + Command Palette controllers | pride-wall/my-stuff/command-palette controllers | ✅ Active |
| `/tests/ui/test-phase1-widgets.css` | Harness-only page chrome (dark ground, wrap, note styles) | tokens.css | ✅ Active |
| `/tests/ui/test-quick-access-layout.html` + `.js` + `.css` | **RETIRED 2026-09-03** (deleted — the Quick Access grid it measured is gone; superseded by `/tests/ui/test-workspaces.*`). Was the 2026-07-29 layout harness: 6 cards in order, computed count badges, Pricing spanning 2 tracks, Reference full-row. The `checkVisibility()` lesson it carried is in LESSONS_LEARNED_ARCHIVE. | — | ❌ Retired |
| `/tests/unit/custom-decal-pricing.test.js` | **NEW (2026-07-29)** 17 tests locking the oversize-decal math: sq-ft summing ACROSS lines before the tier is picked, boundary behaviour (a total exactly on a break stays in the lower tier), the per-tier floors that stop a bigger order costing less than a smaller one, setup waive, tax rounding, and the 52″ roll warning firing only when BOTH dimensions exceed it. Also asserts no hardcoded money — doubling the rate payload must double the price. | shared_components/js/custom-decal-pricing-page.js | ✅ Active |
| `/dashboards/lead.html` | **2026-09-05 review (9 items)**: header Refresh, Retry on failed lead/activity loads, h1 names the lead, art modal `hidden`, labelled controls, kit-modal focus trap, phone header. **NEW (2026-07-18)** Lead workspace (CRM v2 P2) — full-width "work a lead" record opened from the board/drawer/emails via `#Submission_ID` hash: activity timeline + note composer + drag/paste/click file attach (ArtworkUpload → `/api/files/upload` → timeline `attachment` rows), header status/rep controls, rail panels (contact, follow-up date + quick chips, est. value, ShopWorks match + customer-history intelligence, linked quote w/ live $ + Lead_Value snapshot re-sync, click-gated order history, submitted details, original artwork). No pollers. | leads.css, lead-workspace.css, leads-common.js, lead-workspace.js, artwork-upload.js | ✅ Active |
| `/dashboards/js/lead-workspace.js` | Lead workspace controller — crm-proxy form-submissions detail read + PUTs, lead-activity timeline reads/appends, public customer-history/quote_sessions/company-contacts reads; escapeHTML everywhere | LeadsCommon, DashPage, ArtworkUpload, crm-proxy forwarders | ✅ Active |
| `/dashboards/css/lead-workspace.css` | Lead workspace styles (2-column grid, composer, dropzone, timeline items, rail panels; reuses leads.css ld-* atoms; 2026 tokens) | leads.css, dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/leads-common.js` | Shared Leads CRM core (`window.LeadsCommon`) — pipeline vocabulary (STATUS_CHOICES/WON/TERMINAL/STAGE_OF/DRAG_STATUS), REPS + EMAIL_TO_REP, esc/date/money formatting, payload + attachment parsing (collectAttachments, viewUrl/downloadUrl passthroughs), crm-proxy fetch helpers + logActivity | DashPage | ✅ Active |
| `/tests/ui/test-lead-workspace.html` | Lead workspace UI harness — real controller + CSS over the shared fetch stub + faked XHR uploads (no SAML/Caspio); open with `#JFL0716-1234` | lead-workspace.js/css, leads-common.js, test-leads-stub.js | ✅ Active |
| `/dashboards/leads.html` | **2026-09-05 review (10 items)**: stat tiles filter (New / In Pipeline / Won all-time), board shows loading/failure/empty + Retry (was blank), named indicators, drawer is a dialog, phone header icon-only. **NEW (2026-07-18)** Leads CRM — unified lead board: JotForm website leads (`jotform-lead` rows, 6 forms) + Quote Request/Webstore/Roster from `Form_Submissions`; stat tiles, source/status/rep filters + search, detail drawer with per-form status pipeline, rep assignment, ShopWorks customer match (auto by email at ingest, manual search + link), click-to-load order history. Sidebar 🎯 Leads (top) + CRM & Customers card. No pollers (Caspio quota). | dash-shell.css, dash-page-helpers.js, leads.css, leads.js | ✅ Active |
| `/dashboards/js/leads.js` | Leads CRM controller — same-origin `/api/crm-proxy/form-submissions*` reads + status/rep/customer-link PUTs, public `/api/company-contacts/*` match/search, `/api/crm-proxy/order-odbc*` history; escapeHTML on every lead-sourced value (XSS) | DashPage, /api/crm-session/me, crm-proxy forwarders | ✅ Active |
| `/dashboards/css/leads.css` | Leads CRM styles (2026 tokens; toolbar, table, source badges, status pills, right drawer, match card, orders mini-table) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/lead-scorecard.html` | **2026-09-05 review (5 items)**: aria-pressed presets (cleared by Apply), `--w` value bar, Retry on failure. Lead Scorecard — per-rep close report over WON leads via `/api/crm-proxy/lead-scorecard`: date-range presets (Since Oct 2025 / YTD / 90d / all), per-rep table (leads closed, order value = orders AFTER inquiry, lifetime value), closed-leads detail w/ rep filter. Linked from the Leads board header. | dash-shell.css, dash-page-helpers.js, lead-scorecard.css, lead-scorecard.js | ✅ Active |
| `/dashboards/js/lead-scorecard.js` | Lead Scorecard controller — session-gated `/api/crm-proxy/lead-scorecard?since=&until=` fetch, preset ranges, per-rep bars + closed-leads table (deep-links to `lead.html#id`); escapes every value | DashPage, crm-proxy forwarder | ✅ Active |
| `/dashboards/css/lead-scorecard.css` | Lead Scorecard styles (2026 tokens; preset chips, date range, rep value-bars, tabular-num tables) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/unqualified-leads.html` | **2026-09-05 review (6 items)**: real tablist (aria-selected, arrow keys, labelled panel), rescan result is a status line (was shown as an ERROR), spam note via `hidden`, Retry. Unqualified & Spam Leads — review page for leads Claude tagged `Lead_Category='spam'|'unqualified'` (kept `Status=Archived`, off the board + win-rate). Spam/Unqualified tabs w/ counts + search; spam tab shows a delete-from-CSV note. Reads `/api/crm-proxy/form-submissions?category=`. Linked from Leads board header. | dash-shell.css, dash-page-helpers.js, unqualified-leads.css, unqualified-leads.js | ✅ Active |
| `/dashboards/js/unqualified-leads.js` | Unqualified & Spam controller — session-gated `form-submissions?category=spam\|unqualified` fetch, tab toggle, badge counts, client search; escapes every value | DashPage, crm-proxy forwarder | ✅ Active |
| `/dashboards/css/unqualified-leads.css` | Unqualified & Spam styles (2026 tokens; category tabs + badges, delete note, sticky-header table) | dash-shell.css, art-hub.css | ✅ Active |
| `/tests/ui/test-unqualified-leads.html` | Harness for the Unqualified & Spam page — real controller + CSS over stubbed category fixtures | unqualified-leads.css/js, test-unqualified-leads-stub.js | ✅ Active |
| `/tests/ui/test-unqualified-leads-stub.js` | Fetch-stub fixtures (3 spam + 2 unqualified) for the harness | — | ✅ Active |
| `/dashboards/marketing-shipments.html` | **NEW (2026-07-19)** Marketing-kit fulfillment queue (Mikalah) — Requested/Packed/Shipped tabs over `Marketing_Shipments` via `/api/crm-proxy/marketing-shipments`; per-row recipient/ship-to/contents/AE + lead deep-link, inline "Mark packed"/"Mark shipped" (carrier+tracking) PUTs. **Restricted via `Staff_Page_Access` row (shipping,sales,admin)**. AEs request kits from the lead workspace "Send kit" button. | dash-shell.css, dash-page-helpers.js, leads.css, marketing-shipments.css, marketing-shipments.js | ✅ Active |
| `/dashboards/js/marketing-shipments.js` | Marketing-kit queue controller — session-gated `/api/crm-proxy/marketing-shipments*` list + status/tracking PUTs; parses Items_JSON; escapes every value (recipient/company/notes are lead-sourced) | DashPage, /api/crm-session/me, crm-proxy forwarder | ✅ Active |
| `/dashboards/css/marketing-shipments.css` | Marketing-kit queue styles (2026 tokens; reuses leads.css ld-* atoms; inline ship form, tracking chip) | leads.css, dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/jim-mailing-list.html` | **NEW (2026-07-21)** Jim's Mailing List — the owner's manual prospect list. Deliberately oversized/plain-language UI (primary user is 83): one always-visible add/edit form + searchable card list w/ big Edit/Delete. Reads+writes `/api/crm-proxy/jim-mailing-list*` (session-gated forwarder → secret-only proxy `Prospect_Mailing_List`). Standalone — NOT the Leads CRM (no AE routing/Slack/digest). Any logged-in staff (no `Staff_Page_Access` row). Sidebar: top-level after Leads. | dash-shell.css, dash-page-helpers.js, jim-mailing-list.css, jim-mailing-list.js | ✅ Active |
| `/dashboards/js/jim-mailing-list.js` | Jim's Mailing List controller — same-origin `/api/crm-proxy/jim-mailing-list` GET/POST/PUT/DELETE; one form reused for add+edit, native confirm on delete, client-side search, green "saved" toast; escapes every value; errors via DashPage.showError | DashPage, crm-proxy forwarder | ✅ Active |
| `/dashboards/css/jim-mailing-list.css` | Jim's Mailing List styles — senior-friendly (2026 tokens; 19px+ inputs, ≥52px targets, high contrast, single column, `--art-theme` green) | dash-shell.css, art-hub.css | ✅ Active |
| `/tests/ui/test-jim-mailing-list.html` | Harness for Jim's Mailing List — real controller + CSS over an in-memory fetch stub (no SAML/Caspio); exercises add/edit/delete/search | jim-mailing-list.css/js, test-jim-mailing-list-stub.js | ✅ Active |
| `/tests/ui/test-jim-mailing-list-stub.js` | Fetch-stub (3 seed companies + in-memory CRUD) for the harness | — | ✅ Active |
| `/dashboards/seo-strategy.html` | **NEW (2026-07-14)** SEO Strategy — admin-only living reference: two-site lane map (teamnwca product/style/tools vs .net local/education), teamnwca phased plan, Mehar's .net recovery summary, coordination treaty (funnel map, collision protocol, NAP contract), scoreboard + change log. Admin-gated via `Staff_Page_Access` row. Sidebar: Administration. | dash-shell.css, dash-page-helpers.js, seo-strategy.css, seo-strategy.js | ✅ Active |
| `/dashboards/js/seo-strategy.js` | SEO Strategy controller — sticky-TOC smooth scroll + scroll-spy (static reference page, no API calls) | DashPage, APP_CONFIG | ✅ Active |
| `/dashboards/css/seo-strategy.css` | SEO Strategy page styles (2026 tokens; lane badges, status chips, phase blocks, callouts, sticky TOC, print rules) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/forms-library.html` | **NEW (2026-07-10)** Forms Library — central staff page for printable/fillable company forms, grouped by category. Driven by Caspio `Forms_Library` table via proxy `GET /api/forms-library` (Erik adds a row → form appears, no deploy). Each form: Download/Print PDF + optional Fill Out Online link. Sidebar 📄 Forms section (with the Inbox). | dash-shell.css, dash-page-helpers.js, forms-library.css, forms-library.js | ✅ Active |
| `/dashboards/form-submissions.html` | **NEW (2026-07-11)** Forms Inbox — saved fillable-form submissions (Caspio `Form_Submissions` + `Sample_Checkout_Items` via session-gated `/api/crm-proxy/form-submissions*`): stat strip, form/status/search filters, detail modal (status moves, print, sample check-ins, artwork "Create Art Request" push into Art Hub), Samples Tracker tab (item-level open checkouts, overdue-first, Mark Returned/Charged with parent-status rollup). | dash-shell.css, dash-page-helpers.js, form-submissions.css/js | ✅ Active |
| `/dashboards/js/form-submissions.js` | Forms Inbox controller — same-origin crm-proxy fetches (PII airtight path), stats, filters, self-describing-payload detail render, item check-in + rollup, art-hub push (POST /api/artrequests + Art_Request_ID write-back) | DashPage, /api/crm-session/me, /api/crm-proxy/form-submissions* | ✅ Active |
| `/dashboards/css/form-submissions.css` | Forms Inbox styles (2026 tokens; form badges, status chips, samples tracker groups, [hidden] modal fix, print-the-modal rules) | dash-shell.css, art-hub.css | ✅ Active |
| `/tests/ui/test-leads.html` | **NEW (2026-07-18)** Leads CRM UI harness — real controller + real CSS over stubbed fetch fixtures (no SAML/Caspio); exercises stats/filters/drawer/status+rep saves/customer match (found + prospect)/order history | leads.css/js, test-leads-stub.js, test-harness-ribbon.css | ✅ Active |
| `/tests/ui/test-leads-stub.js` | Fetch-stub fixtures for the Leads harness (6 leads across all 4 sources incl. an Archived backfill row; contact match hit + 404; order rows; PUTs mutate state) | — | ✅ Active |
| `/tests/ui/test-harness-ribbon.css` | Shared "stubbed harness" warning ribbon for tests/ui pages | — | ✅ Active |
| `/tests/unit/forms-staff-only.test.js` | **NEW (2026-08-17)** Locks the staff-only allowlist inside the PUBLIC `/forms` static mount — 27 tests, 2 gated files (one of them NESTED, pinning the basename match). Pins that every URL spelling resolving to a gated file collapses to one key (dot-segments, NTFS `::$DATA`, Win32 trailing dot/space, case, backslash), that the gate is registered ABOVE the static mount (ordering IS the security property), and that every OTHER PDF on disk stays PUBLIC — over-gating the handbook/waiver would bounce signed-out employees into a staff SSO they can't complete. 🔴 An earlier string-equality gate was MEASURED serving the full PDF anonymously to `….pdf::$DATA`. | server.js | ✅ Active |
| `/tests/ui/test-form-submissions.html` | Forms Inbox UI harness — real controller + real CSS over stubbed fetch fixtures (no SAML needed); exercises stats/filters/detail/art-push/check-in-rollup | form-submissions.css/js, test-form-submissions-stub.js | ✅ Active |
| `/tests/ui/test-form-submissions-stub.js` | Fetch-stub fixtures for the Inbox harness (in-memory submissions/items; PUTs mutate state so flows click through) | — | ✅ Active |
| `/forms/box-label.pdf` | **NEW (2026-07-11)** Box Label 8.5×11 (natively fillable PDF, 73 fields) — taped to the front of a finished shirt box; fillable twin = /pages/forms/box-label-form.html | listed in Caspio Forms_Library | ✅ Active |
| `/forms/final-qc-checklist.pdf` · `/forms/spoilage-damage-reprint-report.pdf` | **NEW (2026-07-11)** Ops batch: Final QC Checklist (85 AcroForm fields) + Spoilage/Damage & Reprint Report (86) — natively fillable; twins = qc-checklist-form.html / spoilage-report-form.html | listed in Caspio Forms_Library (Quality Control) | ✅ Active |
| `/forms/{embroidery,kornit-dtg,heat-press,laser,roland,compressor-support}-maintenance-log.pdf` | **NEW (2026-07-11)** Six equipment maintenance logs (55 AcroForm fields each, identical skeleton) — natively fillable; shared twin = maintenance-log-form.html?type=… | listed in Caspio Forms_Library (Equipment Maintenance) | ✅ Active |
| `/forms/operations-forms-complete-packet.pdf` | **NEW (2026-07-11)** All 8 ops forms as one 8-page fillable packet (501 fields) — for printing the full set; not in the registry (individual forms are) | — | ✅ Active |
| `/dashboards/js/forms-library.js` | Forms Library controller — fetch /api/forms-library, group by Category, render Download/Fill actions; escapes API text, refuses non-http(s)/relative URLs | DashPage, APP_CONFIG | ✅ Active |
| `/dashboards/css/forms-library.css` | Forms Library page styles (2026 tokens) | dash-shell.css, art-hub.css | ✅ Active |
| `/forms/customer-garment-drop-off-form.pdf` | **NEW (2026-07-10)** Customer Garment Drop-Off Form (blank printable) — customer-supplied garment intake: contact info, decoration checklist, size grid, liability waiver | listed in Caspio Forms_Library; fillable twin = /pages/forms/garment-drop-off-form.html | ✅ Active |
| `/forms/Customer-Supplied-Garments-Acknowledgment.pdf` | Customer-supplied garments liability acknowledgment (signed waiver PDF) | listed in Caspio Forms_Library | ✅ Active |
| `/forms/business-credit-application-no-personal-guaranty.pdf` | **NEW (2026-08-17)** Business Credit Application & Agreement — **No Personal Guaranty** (2 pages, 34 AcroForm fields, natively fillable). Customer-facing: we email it, the customer types + signs in any PDF reader and emails it back; page 2 carries the business-only agreement (clause 6 = no personal guaranty) plus an NWCA-use-only approval block (terms / limit / effective date / approved by / notes). ⚠️ `/NeedAppearances` is set on the AcroForm so a viewer that doesn't write appearance streams still shows the customer's typed answers — without it a returned copy can open BLANK. PDF-only by design (no `/pages/forms/` fill-online twin — the signed copy is the record). 🔒 **STAFF-ONLY** (Erik, 2026-08-17) — the only gated file in the otherwise-public `/forms/` tree; `gateStaffOnlyForms` + `STAFF_ONLY_FORMS` in server.js, drift-locked by `tests/unit/forms-staff-only.test.js`. | listed in Caspio Forms_Library (Payments) | ✅ Active |
| `/forms/embroidery-machine-operator-application.pdf` | **NEW (2026-08-25)** Employment Application — Embroidery Machine Operator (2 pages = one sheet front/back on 8.5×11, 59 AcroForm fields, natively fillable, `/NeedAppearances` set). Simple plain-language applicant form: contact info, availability, the "Do you have any sewing or embroidery experience?" section with experience checkboxes, work history, references, at-will signature block. Public by design (applicants fill it); regenerate via `python scripts/build-embroidery-operator-application-pdf.py`. | listed in Caspio Forms_Library (Employee / HR); generated by build-embroidery-operator-application-pdf.py | ✅ Active |
| `/forms/policies/credit-card-authorization.pdf` | Credit Card Authorization form (printable). 🔒 **STAFF-ONLY** (Erik, 2026-08-17) — second entry in `STAFF_ONLY_FORMS`; matched on BASENAME, which is what covers this nested path. ⚠️ **Linked from the Caspio policy “Credit Card SOP”, whose hub page is PUBLIC** — signed-out staff following that link now hit SSO first (intended: the SOP's audience is staff). A repo grep does NOT find this referrer — policy bodies live in Caspio; grep `Body_HTML` across `/api/policies-public/<id>` instead. | server.js `STAFF_ONLY_FORMS` | ✅ Active |
| `/dashboards/policy-migration.html` | **NEW (2026-07-10)** Policy Migration tracker — SweetProcess→Policies Hub project status: hub-live count, Erik question queue, wave log, per-document tier/status table with hub deep-links. Read-only snapshot; confidential HR rows excluded from data. | dash-shell.css, dash-page-helpers.js, policy-migration.css, policy-migration.js, policy-migration-data.json | ✅ Active |
| `/dashboards/js/policy-migration.js` | Policy Migration controller — stats, questions, wave log, tier-filtered document table | DashPage, fetch-timeout, policy-migration-data.json | ✅ Active |
| `/dashboards/css/policy-migration.css` | Policy Migration page styles (2026 tokens) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/policy-migration-data.json` | Policy Migration data snapshot (regenerated by Claude each work session; no confidential titles) | — | ✅ Active |
| `/dashboards/product-manager.html` | **NEW (2026-07-06)** Rep-facing non-SanMar Product Manager — add/edit products (COST in, never sell price; margin machinery prices them like SanMar goods; FixedPrice mode for specials), image upload to Caspio CDN, live-in-catalog toggle. Active rows merge into /api/products/search (proxy). | dash-shell.css, dash-page-helpers.js, product-manager.css, product-manager.js | ✅ Active |
| `/dashboards/js/product-manager.js` | Product Manager controller — list/filter/stats, add-edit form, /api/files/upload image flow, POST/PUT /api/non-sanmar-products. **Vendor is a curated `<select>` + Other… escape (2026-08-14)** — `NON_SANMAR_VENDORS` here MIRRORS quote-builder-utils.js and is drift-locked by tests/unit/non-sanmar-pricing-mode.test.js (VendorCode is filtered with exact uppercase equality, so free-typed spellings split vendor reporting) | DashPage, APP_CONFIG | ✅ Active |
| `/dashboards/css/product-manager.css` | Product Manager page styles (2026 tokens) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/portal-directory.html` | Customer portal directory — index of customer portals. **2026-09-05 review (9 items)**: a failed feed shows the error panel + Retry (was a silent half-directory), "Preview" opens the staff mirror (the old "Open" → `/portal/:id` just bounced staff to the customer login), clipboard fallback is a toast, `hidden` toggles, named dots/buttons | portal-directory.js, portal-directory.css | ✅ Active |
| `/dashboards/js/portal-directory.js` | Portal directory controller — customer list, search. Mockup list read is **same-origin** through the session-gated forwarder. | /api/customers, server.js `mockupForward` | ✅ Active |
| `/dashboards/css/portal-directory.css` | Portal directory styles | — | ✅ Active |
| `/dashboards/supacolor-orders.html` | Supacolor orders dashboard — local mirror of Supacolor jobs. **2026-09-05 review (10 items)**: loads `config/app.config.js`, chips are pressed-state buttons, error + Retry, labelled backfill dialog, no inline style | supacolor-orders.js, supacolor-orders.css, kanban.css | ✅ Active |
| `/dashboards/js/supacolor-orders.js` | Supacolor orders controller — 60s poll (paused hidden), view chips, filters, pagination, screenshot backfill (Claude Vision). **2026-09-05**: `API_BASE` from APP_CONFIG, `renderLoadError()` + Retry, `hidden` toggles, `aria-pressed` chips, backfill modal focus return + Esc | /api/supacolor-jobs | ✅ Active |
| `/dashboards/css/supacolor-orders.css` | Supacolor orders dashboard styles | kanban.css | ✅ Active |
| `/dashboards/roland-printer-supplies.html` | Roland Printer Supplies — JotForm supply-order embed (ink/parts/consumables) for Steve & Brian + printable PDF download (added 2026-06-02, dash-shell canonical) | dash-shell.css, art-hub.css, roland-printer-supplies.css, JotForm 261515595979071 | ✅ Active |
| `/dashboards/css/roland-printer-supplies.css` | Roland Printer Supplies page-specific styles | dash-shell.css, art-hub.css | ✅ Active |
| `/forms/NWCA_LG540_Order_Form_1page.pdf` | Roland printer printable supply-order form (1-page; downloaded from roland-printer-supplies.html) | — | ✅ Active |
| `/dashboards/caspio-api-reference.html` | **NEW (2026-07-09)** Caspio REST API **v4** platform reference viewer — full 107-endpoint catalog + querying/bulk/auth/webhook guidance, live filter. **ERIK-ONLY** (email-gated route in server.js `requireCrmEmail`; admin-override does NOT apply). Static — no API calls. Mirrors `memory/CASPIO_REST_API_V4_REFERENCE.md`. | dash-shell.css, art-hub.css, caspio-api-reference.css, caspio-api-reference.js | ✅ Active |
| `/dashboards/css/caspio-api-reference.css` | Caspio API reference page styles (2026 tokens; HTTP verbs → --state-* colors) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/caspio-api-reference.js` | Caspio API reference controller — renders the v4 endpoint catalog from a static snapshot + live filter/highlight + smooth-scroll nav (no API calls) | APP_CONFIG | ✅ Active |
| `/dashboards/table-usage-audit.html` | **NEW (2026-07-10)** Caspio Table Audit cleanup tool — all 163 tables + usage signals (code/view/rel/task/webhook) so admin can find + archive stale tables. **ADMIN-only** (`requireCrmRole(['admin'])` route in server.js). ⚠️ does NOT detect DataPage/Flex usage. | dash-shell.css, art-hub.css, dash-page-helpers.js, table-usage-audit.css, table-usage-audit.js | ✅ Active |
| `/dashboards/css/table-usage-audit.css` | Table-audit page styles (2026 tokens; usage/tier chips → --state-* colors) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/table-usage-audit.js` | Table-audit controller — static 163-table snapshot + filter/sort/search, per-table review (localStorage), live-existence check via `/api/caspio-schema/tables`, CSV export | DashPage, APP_CONFIG | ✅ Active |
| `/dashboards/manageorders-api-reference.html` | **NEW (2026-07-10)** ManageOrders / OnSite External API field cheat sheet — all push/pull endpoints + every ExternalOrderJson field (from public Swagger + tested gotchas), live filter. **ADMIN-only** (`requireCrmRole(['admin'])`). Canonical deep doc = `memory/MANAGEORDERS_COMPLETE_REFERENCE.md`. | dash-shell.css, art-hub.css, manageorders-api-reference.css, manageorders-api-reference.js | ✅ Active |
| `/dashboards/css/manageorders-api-reference.css` | ManageOrders cheat-sheet styles (2026 tokens; HTTP verbs → --state-*) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/manageorders-api-reference.js` | ManageOrders cheat-sheet controller — renders endpoints + 12 field models from an embedded snapshot + live filter/highlight (no API calls) | — | ✅ Active |
| `/dashboards/sanmar-api-reference.html` | **NEW (2026-07-10)** SanMar Web Services (SOAP) cheat sheet — 12 services/WSDLs, methods + fields + gotchas (catalogColor, 3000 cap, sizeIndex, 301/303), cadence, warehouses, v24.5 MAP/brand restrictions, live filter. **ADMIN-only** (`requireCrmRole(['admin'])`). Canonical = `memory/SANMAR_API_REFERENCE.md`. | dash-shell.css, art-hub.css, sanmar-api-reference.css, sanmar-api-reference.js | ✅ Active |
| `/dashboards/css/sanmar-api-reference.css` | SanMar cheat-sheet styles (2026 tokens) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/sanmar-api-reference.js` | SanMar cheat-sheet controller — 14 sections (services/methods/fields/bridge rules) from an embedded snapshot + live filter/highlight (no API calls) | — | ✅ Active |
| `/dashboards/shopworks-odbc-reference.html` | **NEW (2026-07-16)** ShopWorks OnSite ODBC reference — connection card (bandit bridge → 192.168.10.6 Data_ODBCMapping), vendor rules, FileMaker SQL gotchas, gap-fill table, searchable 2,630-field catalog. **ERIK-only** (`requireCrmEmail` route in server.js — page shows credentials). Canonical deep doc = `memory/SHOPWORKS_ODBC_INTEGRATION.md`. | dash-shell.css, art-hub.css, shopworks-odbc-reference.css, shopworks-odbc-reference.js, data/shopworks-odbc-schema.json | ✅ Active |
| `/dashboards/pricing-analysis.html` | **NEW (2026-07-29)** Pricing Analysis — embroidery + cap cost/margin/realization study. 24-month realization gap (85.3% of Caspio list, $442,001), ten-year units per quantity tier split caps vs garments, measured throughput, live Caspio ladder, **tier-break optimization** (fitted cost curve hours(q)=S+v*q, DP k-segmentation, small-order fee scenarios, reorder/consolidation economics), **corrected cost basis** (4 GL years; $26-29/production hour + $261/order on driver-based allocation, superseding the $89.74 hour-only figure) and the 21-year finding that small orders are overwhelmingly reorders, five priced rules, stated limits. **ADMIN-only** via `ADMIN_DEFAULT_PAGES` (lib/page-access.js) — no Staff_Page_Access row needed, defaults CLOSED. **GENERATED — do not hand-edit**: run `python scripts/build-pricing-analysis.py`. | dash-shell.css, art-hub.css, pricing-analysis.css, pricing-analysis.js | ✅ Active |
| `/dashboards/css/shopworks-odbc-reference.css` | ShopWorks ODBC reference styles (2026 tokens; calc/global/summary field badges → --state-*) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/css/pricing-analysis.css` | Pricing Analysis styles (2026 tokens only; proportion bars, stat tiles, rule cards; sign carried by glyph + colour, never colour alone) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/bandit-integration.html` | **NEW (2026-07-18)** Bandit integration reference — data-flow (bandit→proxy→Caspio/Box), live scheduled-task table, sync mechanics (ODBC read→clean→POST), thumbnails→Box, connect/troubleshoot, the per-second-rate-limit rule. Admin doc; static (no JS). Deep detail = `memory/SHOPWORKS_ODBC_INTEGRATION.md` | dash-shell.css, art-hub.css, bandit-integration.css | ✅ Active |
| `/dashboards/css/bandit-integration.css` | Bandit integration page styles (2026 tokens; flow strip, task table, status pills) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/api-usage.html` | **NEW (2026-07-26)** API Usage — Caspio call **attribution** (built after the $358 overage). Shows top tables/endpoints, calls by method, daily trend, and projection vs the 500K cap with the daily budget line. Deliberately NOT a billing gauge: Caspio → Plan and billing → Usage stays the authority, and the page states its own scope (rollup-backed vs single-dyno lower bound). ADMIN ONLY — needs a `Staff_Page_Access` row (`api-usage.html` / `admin`). Reads via `/api/crm-proxy/admin/{metrics,usage}` forwarders (upstream is secret-gated). Detail = `memory/caspio-api-usage-audit-2026-07.md` | dash-shell.css, art-hub.css, api-usage.css/js, dash-page-helpers.js | ✅ Active |
| `/dashboards/css/api-usage.css` | API Usage page styles (2026 tokens; scope banner, budget meter, attribution rows, daily trend bars) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/api-usage.js` | API Usage controller — fetches `/api/crm-proxy/admin/usage` (pacing + mode) then `/api/crm-proxy/admin/metrics?full=1` (breakdown); renders budget meter, top tables/endpoints with share bars (≥20% flagged hot), method chips, daily trend. Rule 4: all failures hit the error banner and stats blank to em-dashes, never zeros | DashPage, fetch (same-origin admin forwarders) | ✅ Active |
| `/tests/ui/test-api-usage.html` | API Usage **render** harness — real controller + CSS over a stubbed fetch (no SAML). `?scenario=` exercises rollup / dyno-lower-bound / insufficient / error. ⚠️ Cannot detect an unregistered route (see stub header) — probe forwarders live with a 401-vs-404 curl | api-usage.css/js, test-api-usage-stub.js, test-api-usage-harness.css | ✅ Active |
| `/tests/ui/test-api-usage-stub.js` | Fetch stub + fixtures for the API Usage harness (July 2026 shape: ~23K/day, thumbnail + Sanmar_Bulk hot, one 67K spike day) | — | ✅ Active |
| `/tests/ui/test-api-usage-harness.css` | Scenario-switcher bar for the API Usage harness (kept out of api-usage.css so it can't mask a real styling bug) | art-hub.css | ✅ Active |
| `/dashboards/sanmar-ftp-integration.html` | **NEW (2026-07-22)** SanMar Downloads — admin page that lists SanMar's FTP data files and streams the chosen one to the browser (replaces manual FileZilla). Primary = `SanMarPI-Bulk-*.csv` → import into `Sanmar_Bulk_251816_Feb2024` (the master every quote builder prices from). Backed by `/api/staff/sanmar-ftp/{list,download}` in server.js (basic-ftp, admin-gated). Needs `SANMAR_FTP_PASSWORD` config var. Deep detail = `memory/SANMAR_FTP_INTEGRATION.md` | dash-shell.css, art-hub.css, sanmar-ftp-integration.css, sanmar-ftp-integration.js | ✅ Active |
| `/dashboards/css/sanmar-ftp-integration.css` | SanMar Downloads page styles (2026 tokens; file table, flow strip, callouts, download buttons) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/sanmar-ftp-integration.js` | SanMar Downloads controller — fetches `/api/staff/sanmar-ftp/list`, renders the file table (pins the pricing master, human size/date/kind), builds per-file Download links to `/api/staff/sanmar-ftp/download`; handles not-configured/ftp-error/401 states | fetch (same-origin admin API) | ✅ Active |
| `/dashboards/sanmar-shopworks-converter.html` | **NEW (2026-07-23)** Admin tool — upload a SanMar price list, convert (100% in-browser, no upload) to a ShopWorks Parts import CSV. Auto-detects the ShopWorks Integration list (fixup: add sizes to descriptions, `_2XL`→`_2X`) vs a raw SanMar feed (full conversion). Route `requireCrmRole(['admin'])` in server.js. Detail = `memory/SANMAR_SHOPWORKS_PARTS.md` | sanmar-shopworks-converter.css/.js, sanmar-shopworks-parts.js, sku-validation-service.js, vendor/papaparse.min.js, vendor/xlsx.full.min.js | ✅ Active |
| `/dashboards/css/sanmar-shopworks-converter.css` | SanMar→ShopWorks converter page styles (2026 tokens; file picker, stats, preview table) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/sanmar-shopworks-parts.js` | Pure transform (unit-tested, node+browser): `detectFormat`, `fixupRow(s)`, `fullConvertRows`/`RawAccumulator`, `limits`, `toCsv`. Fixup rules verified vs existing parts; full path reuses `sanmarToShopWorksSKU` | sku-validation-service.js (full path) | ✅ Active |
| `/dashboards/js/sanmar-shopworks-converter.js` | Converter page controller — file input, PapaParse (CSV stream) / SheetJS (XLSX), calls the transform, preview + Download CSV | sanmar-shopworks-parts.js, Papa, XLSX | ✅ Active |
| `/dashboards/js/vendor/papaparse.min.js` | Vendored PapaParse (client-side CSV streaming) for the SanMar→ShopWorks converter | — | ✅ Active |
| `/dashboards/js/vendor/xlsx.full.min.js` | Vendored SheetJS (client-side XLSX read) for the SanMar→ShopWorks converter | — | ✅ Active |
| `/dashboards/js/shopworks-odbc-reference.js` | ShopWorks ODBC reference controller — fetches the schema JSON, renders 16 collapsible tables + live filter/highlight, stored-only toggle, curated gotcha notes, auto kind-badges for calc/global/summary prefixes | fetch (static JSON), DashPage (errors) | ✅ Active |
| `/dashboards/js/pricing-analysis.js` | Pricing Analysis sticky section-nav scroll-spy (IntersectionObserver). Fetches nothing — figures are baked into the gated HTML because a JSON under /dashboards would be world-readable (only *.html hits gateStaffPage) | none | ✅ Active |
| `/dashboards/data/shopworks-odbc-schema.json` | Static schema snapshot — 2,630 fields/16 tables pulled live from FileMaker_Fields 2026-07-16. Regenerate from `memory/shopworks-odbc-schema-catalog.txt` if ShopWorks extends Data_ODBCMapping. | — | ✅ Active |

| `/dashboards/volume-quote.html` | **NEW (2026-09-02)** Volume Quote — Erik builds a ONE-TIME price for a large embroidery order (Braun NW 500-pc request). Loads standard 72+ price, SanMar case + piece cost and stock per style, two levers (embroidery $/pc, garment margin denominator), cost model from Caspio `Service_Codes` `VOL-*` rows (ServiceType VOLUME, Erik-editable, no deploy) — 11 rows incl. a WORST-CASE production model (SPM, heads, handling, slack) that the page prices against when it is worse than the fitted log model, and generates an approval memo (copy/print) that states WHY the price is offered once and the terms that keep it from becoming a price list. Admin → Money & Payroll; in `ADMIN_DEFAULT_PAGES`. **Save quote** mints `VQ-YYYY-NNN` via `/api/quote-sequence/VQ` and writes `quote_sessions` + `quote_items` through the same-origin relays (read-only in Quote Mgmt; internal levers/margin kept in item 1 `LogoSpecs`, NEVER in Notes). **Customer quote** sheet prints/PDFs prices + terms only; the internal memo prints separately (two `body.vq-mode-*` print modes). | volume-quote.css/js, `GET /api/pricing-bundle`, `/api/product-details`, `/api/sanmar/inventory/:style`, `/api/service-codes?type=VOLUME` | ✅ Active |
| `/dashboards/js/volume-quote.js` | Volume Quote controller. **2026-09-05**: `SalesRepEmail = repEmailFor(name)` (StaffAuthHelper.STAFF_EMAIL_MAP; was hardcoded sales@ so Mission Control never attributed VQ quotes to the rep). Price math mirrors `embroidery-pricing-service.js` (case cost ÷ MarginDenominator + Shirt 72+ EmbroideryCost, half-dollar ceil, relative size upcharge). 🔴 No built-in cost constants — this file is served anonymously, so the hour rate lives in Caspio; missing VOL-* rows disable the cost panel with a banner (never a silent fallback). | DashPage, APP_CONFIG | ✅ Active |
| `/dashboards/css/volume-quote.css` | Volume Quote styles (art-hub tokens + dash-shell). `@media print` keeps only the memo card. | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/contract-break-even.html` | **NEW (2026-09-02)** Contract Break-Even — the contract price list grid (garments/caps 8K–20K, full back 25K–50K × 5 tiers at 4/12/36/60/144 pcs) with every cell re-costed from the Caspio cost model: setup + handling + sewing at VOL-SPM ÷ heads + thread + VOL-ORDER-COST, ÷ qty. Toggles: typical (8-head, no slack) / worst case (VOL-HEADS-WORST + VOL-SLACK); break-even cost / profit-at-the-card (card × stitches or the CTR-MIN-ORDER minimum ÷ qty). Admin → Analysis; in `ADMIN_DEFAULT_PAGES`. Print-friendly. | contract-break-even.css/js, `GET /api/service-codes?type=VOLUME`, `/api/contract-pricing`, `CTR-MIN-ORDER`, optional `VOL-THREAD-PER-1K` | ✅ Active |
| `/dashboards/js/contract-break-even.js` | Contract Break-Even controller. 🔴 No cost constants in the file (served anonymously): missing VOL-* rows → error banner, nothing rendered. Warns visibly when VOL-HANDLING-MIN is below the log-fitted 2.4 min and when no thread row exists. | DashPage, APP_CONFIG | ✅ Active |
| `/dashboards/css/contract-break-even.css` | Contract Break-Even styles (tokens + dash-shell; loss/thin cell states; print keeps colour). | dash-shell.css, art-hub.css | ✅ Active |
### Staff Dashboards
| File | Purpose | Dependencies | Status |
|------|---------|--------------|--------|
| `/staff-dashboard-v3/index.html` | **CANONICAL (V3 sole survivor, 2026-05-28)** Main staff dashboard served at `/staff-dashboard.html`. Uses @layer CSS (tokens → base → components → utilities → overrides) + unlayered theme. v2 and v1 files deleted; their URLs 301-redirect to canonical. Recovery: `git show v2026.05.27.5:staff-dashboard.html`. | staff-dashboard/{tokens,base,components,utilities,dashboard-v3-theme,dashboard-v3-patch-2}.css + modular controllers under shared_components/js/staff-dashboard/ (config.js + caspio-isolation.js retired 2026-08-27 — all reads same-origin, no Caspio embed) | ✅ Active |
| `/dashboards/ae-dashboard.html` | AE dashboard — the reps' page: Steve / Ruth intake forms, the two mockup galleries, Review Mockups, Transfers + Personalization launchers, Art Requirements & Fees. **2026-09-03 redesign** (floating nav card, person colour-coding). **2026-09-04 review (15 items)**: Rule 3 clean (0 inline styles / scripts / onclicks — bootstrap in `dashboards/js/ae-dashboard-init.js`, Requirements tab rebuilt on `.req-*` classes), art fees + GRT options from Caspio Service_Codes, ONE "needs review" count published by the galleries (`ae:counts`), nav badges live, arrow-key tabs, SVG item-type icons, JDS listed once, dead note modals removed, one name (AE Dashboard). Locks: `tests/unit/ae-dashboard-page.test.js` | ae-dashboard.js, ae-dashboard-init.js, the 4 *-submit-form.js, art-ae.js, mockup-ae.js | ✅ Active |
| `/dashboards/design-gallery.html` | **NEW (2026-08-05) — Design Vault.** Master design search across every source (digitized masters, ShopWorks thumbnails, Steve's ArtRequests, Ruth's Digitizing_Mockups, finished photos), deduped to one card per design number. Browse rails + date-seeded daily wall, instant local search over a client-cached ~39k-group index, windowed grid, inspector drawer, customer portfolio. Staff-gated by the `/dashboards` `gateStaffHtml` mount (replaces the unauthenticated `/pages/design-gallery.html`). | design-gallery.css, design-gallery{-search,-store,-grid,-rails,-drawer}.js, design-gallery.js, dash-page-helpers.js, toast-notifications.js, design-thumbnail-service.js, `/api/design-search/*` | ✅ Active |
| `/dashboards/css/design-gallery.css` | **NEW (2026-08-05)** Design Vault styles — art-hub tokens + dash-shell only; the sole hex values are the 3-line forest theme override. Light "gallery wall" (white-background artwork reads badly on charcoal). | art-hub.css, dash-shell.css | ✅ Active |
| `/dashboards/js/design-gallery-search.js` | **NEW (2026-08-05)** Pure search core — positional row decode, ranking ladder, filter composition, seeded daily wall, `DG.esc`. Dual browser/Node export so jest requires it directly. | — (pure) | ✅ Active |
| `/dashboards/js/design-gallery-store.js` | **NEW (2026-08-05)** Index lifecycle — streamed `/index` download w/ progress, IndexedDB cache, ETag revalidation via `/meta`, `/recent` delta-merge, `patchImage` for on-demand thumbnails. | `/api/design-search/{index,meta,recent}` | ✅ Active |
| `/dashboards/js/design-gallery-grid.js` | **NEW (2026-08-05)** Windowed result grid — dual-spacer chunking (200/chunk, ≤3 mounted) so 39k results hold ~600 DOM cards; batched thumbnail fill, density toggle, roving keyboard focus. Owns the shared card template. | `/api/thumbnails/by-designs` (via design-thumbnail-service.js) | ✅ Active |
| `/dashboards/js/design-gallery-rails.js` | **NEW (2026-08-05)** Browse surface — stat strip, "Fresh off the digitizer", "Recently stitched", top-client collages, date-seeded Today's Wall. Zero API calls (reads the loaded index). | design-gallery-search.js, design-gallery-grid.js | ✅ Active |
| `/dashboards/js/design-gallery-drawer.js` | **NEW (2026-08-05)** Inspector drawer — instant paint from the index, then 3 parallel hydrations with per-section inline errors + retry; merged URL-deduped image strip, hi-res lightbox, Copy/Share/Quote/prev-next. | `/api/digitized-designs/lookup`, `/api/artrequests`, **same-origin** `/api/mockups` (session-gated forwarder), `/api/box/thumbnail/:id` | ✅ Active |
| `/dashboards/js/design-gallery.js` | **NEW (2026-08-05)** Design Vault controller — URL state machine (`?q&tier&src&yr&has&customer&sort` + `#design=`), mode transitions, Esc unwind ladder, keyboard map, deep-search escalation, freshness pill, boot/offline surfaces. | all design-gallery-*.js, dash-page-helpers.js | ✅ Active |
| `/tests/unit/design-search-core.test.js` | **NEW (2026-08-05)** Locks the Design Vault wire contract — row decode, imgRef expansion, ranking ladder, filter composition, `/recent` delta-merge, seeded wall determinism (41 tests). Moves only WITH the proxy-side index tests. | jest, design-gallery-search.js | ✅ Active |
| `/tests/unit/design-gallery-xss.test.js` | **NEW (2026-08-05)** XSS regression lock — 7 hostile company/design names through card/grid/drawer render paths assert no script nodes, no `on*` attributes, no attribute breakout; plus source-level bans on handler templates and hardcoded proxy hosts (22 tests, jsdom). | jest, jsdom, design-gallery-{search,grid,drawer}.js | ✅ Active |
| `/dashboards/art-hub-dashboard.html` | ~~DELETED~~ Coordinator redirect (removed 2026-03-15) | — | ❌ Deleted |
| `/dashboards/commission-structure.html` | Online store commission structure reference | commission-structure.css | ✅ Active |
| `/dashboards/css/commission-structure.css` | Commission dashboard styles | - | ✅ Active |
| `/dashboards/access-admin.html` | **NEW (2026-06-30)** Erik-only RBAC control panel — manage staff roles (`Staff_App_Roles`) + page access (`Staff_Page_Access`). Code-gated `requireCrmRole(['admin'])`. | access-admin.js, access-admin.css, `/api/crm-proxy/admin-rbac/*` | ✅ Active |
| `/dashboards/js/access-admin.js` | Access-Admin controller — roles + page-access CRUD via admin-gated crm-proxy | /api/crm-proxy/admin-rbac | ✅ Active |
| `/dashboards/css/access-admin.css` | Access-Admin panel styles (NWCA green, no inline) | - | ✅ Active |
| `/dashboards/payroll.html` | **NEW (2026-07-27)** 🔒 Payroll — vacation/sick balances, hours by pay period, packet upload (PDF → verified → saved), and printable cut-apart slips (8.5×11, 6/sheet). **ERIK ONLY**: `requirePageAccess('payroll.html')` + an exclusive-email `Staff_Page_Access` row. **Never displays pay rates or salaries** (Erik 2026-07-27). | payroll.js, shared staff-payroll.css, `/api/crm-proxy/payroll/*` | ✅ Active |
| `/dashboards/js/payroll.js` | Payroll controller — leave balances, per-period hours, and the 3-step upload (read → reconcile → save). Sends only a job id on commit so figures can't be edited in transit. **(2026-08-03)** Applies the vacation carryover correction to the leave table and slips, refuses to print a flagged record, and downloads a per-run audit CSV. | /api/crm-proxy/payroll, vacation-carryover.js | ✅ Active |
| `/dashboards/js/vacation-carryover.js` | **NEW (2026-08-03)** Pure transform + validation for the vacation-slip carryover artifact. Payroll books hours to the tax year of the *check* date, so December vacation paid on a January check inflates both accrued and used by the same carryover; the slip subtracts it back out against Caspio's hand-maintained `Vacation_Annual_Entitlement`. 🔴 Sick hours are never transformed (WA statutory carryover). Date-effective via `Vacation_Eligible_Date`. Node-testable (UMD tail). | window.VacationCarryover; loaded by payroll.html before payroll.js | ✅ Active |
| `/shared_components/css/staff-payroll.css` | Canonical payroll styles; accessible ledger/reconciliation regions, responsive controls and six slip slots per Letter sheet. Legacy payroll.css preserved in the original-content fixture. | - | Active |
| `/dashboards/customer-portal-admin.html` | **NEW (2026-06-30)** Customer Portals admin console — list/add/enable-disable/delete portal invites (`Customer_Portal_Access`), send magic-link login emails, preview a customer's portal. Role-gated `requireCrmRole(['admin','accountant','art','sales','taneisha','nika'])`. | dash-shell.css, dash-page-helpers.js, customer-portal-admin.css/js, `/api/crm-proxy/customer-portal-access/*`, `/api/portal-admin/*` | ✅ Active |
| `/dashboards/js/customer-portal-admin.js` | Customer Portals console controller (CRUD + CRM lookup + send-link + preview) | DashPage, same-origin `/api/crm-proxy/*` + `/api/portal-admin/*` | ✅ Active |
| `/dashboards/finished-photos.html` | Factory finished-product photo capture + manage (mobile-first, iPad/phones). **2026-07-17 v2:** NWCA-logo top bar, 3 find modes (barcode Scan / Order # / company Search) resolving via work-order barcodes, design auto-select from design-sheet scans, client-side photo compression, caption chips, publish list w/ lightbox. SAML-gated (any staff). | finished-photos.js/css, vendor/html5-qrcode.min.js, proxy `/api/finished-photos` (open POST) + same-origin `/api/staff/finished-photos` (GET/lookup/PATCH/DELETE forwarders), `/api/company-contacts/search`, `/api/designs/by-customer` | ✅ Active |
| `/dashboards/js/finished-photos.js` | Finished-photos capture controller (find modes + barcode scanner + order/design lookup, design picker, photo compression ≤2000px JPEG, upload, publish/delete manage, lightbox) | APP_CONFIG, vendor/html5-qrcode.min.js, proxy `/api/finished-photos`, `/api/staff/finished-photos/*` | ✅ Active |
| `/dashboards/js/vendor/html5-qrcode.min.js` | **NEW (2026-07-17)** Vendored html5-qrcode v2.3.8 (MIT, ZXing-based) — Code 39/128 + QR camera scanning on iOS Safari for the finished-photos work-order scanner. Copied from npm `html5-qrcode` (also in package.json). | - | ✅ Active |
| `/dashboards/finished-photos.webmanifest` | **NEW (2026-07-17)** Web-app manifest for the capture page — makes Add-to-Home-Screen install as a standalone app ("Finished Photos", portrait, green theme). | images/fp-icon-192/512.png | ✅ Active |
| `/dashboards/images/fp-icon-{180,192,512}.png` | **NEW (2026-07-17)** Home-screen app icons for the capture page (NWCA logo on white, green base band; 180 = apple-touch-icon). Generated from the Caspio CDN logo. | - | ✅ Active |
| `/dashboards/css/finished-photos.css` | Finished-photos capture page + wall-poster styles (NWCA green, mobile-first, segmented modes, scanner sheet, safe-area, print `@media`, no inline) | - | ✅ Active |
| `/dashboards/finished-photos-library.html` | **NEW (2026-07-19)** Finished Photos Library — company-wide browse of every finished-product photo, grouped by account with the account's sales rep badge; rep filter chips + search + published/hidden filter + inline Publish/Hide + lightbox. Deep link `#rep=<full name>` (Mission Control "My Finished Photos"). SAML-gated (any staff). | dash-shell.css, dash-page-helpers.js, finished-photos-library.css/js, same-origin `/api/staff/finished-photos/library` + PATCH forwarder | ✅ Active |
| `/dashboards/js/finished-photos-library.js` | Finished Photos Library controller (one library fetch, client-side rep/search/visibility filters, account grouping, publish toggle, hash deep-link) | DashPage, `/api/staff/finished-photos/library`, `/api/staff/finished-photos/:pk` PATCH | ✅ Active |
| `/dashboards/css/finished-photos-library.css` | Finished Photos Library page styles (rep chips, account sections, photo grid, lightbox — tokens only) | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/css/customer-portal-admin.css` | Customer Portals console page styles (table, modal, badges) | dash-shell.css, art-hub.css tokens | ✅ Active |
| `/dashboards/taneisha-crm.html` | Taneisha's Account CRM dashboard. **2026-09-05 review (12 items, shared with Nika's)** — see rep-crm.js row | rep-crm.js, rep-crm.css | ✅ Active |
| `/dashboards/nika-crm.html` | Nika's Account CRM dashboard. **2026-09-05 review (12 items, shared with Taneisha's)** — see rep-crm.js row | rep-crm.js, rep-crm.css | ✅ Active |
| `/dashboards/css/rep-crm.css` | **SHARED** CRM dashboard styles (used by both reps) | - | ✅ Active |
| `/dashboards/js/rep-crm.js` | **SHARED** CRM service/controller (config-driven). **2026-09-05 review**: accounts count set on boot, `parseCalendarDate()` (local calendar days — no more day-early / false Overdue), Retry-able load (`retryLoad()`), archive fallback announced in the hint line, tier/at-risk/product `aria-pressed` sync, keyboard-openable cards, modal focus return, toast instead of alert(), `hidden` toggles | APP_CONFIG, REP_CONFIG | ✅ Active |
| `/dashboards/js/rep-calendar.js` | **SHARED** Calendar logic (config-driven) | APP_CONFIG, REP_CONFIG | ✅ Active |
| `/dashboards/house-accounts.html` | House Account assignment dashboard. **2026-09-05 review (13 items)**: stat tiles filter correctly (YTD Sales tile = show all), 5 labelled dialogs with focus return, Retry, Rule 3 clean, keyboard expanders | house-accounts.js, house-accounts.css | ✅ Active |
| `/dashboards/css/house-accounts.css` | **NEW** House Accounts dashboard styles | - | ✅ Active |
| `/dashboards/js/house-accounts.js` | House Accounts service/controller. **2026-09-05**: `retryLoad()`, `_openOverlay/_closeOverlay` focus helpers, `_syncStatTiles()`, `quickAssignFromSelect()` (data-change target), `parseCalendarDate()`, `hidden` toggles throughout, `staffName` from `/api/crm-session/me` for the audit trail | APP_CONFIG | ✅ Active |
| `/dashboards/monogram-dashboard.html` | Monogram orders dashboard | monogram-dashboard.css, monogram-dashboard.js | ✅ Active |
| `/dashboards/css/monogram-dashboard.css` | Monogram dashboard styles. **Theme = shop-floor BLUE `#2563eb` since 2026-09-04** (`--mono-theme*`, was NWCA green `--nwca-green*`) — personalization matches Names & Numbers and the staff-dashboard `f-floor` tiles | CSS variables | ✅ Active |
| `/dashboards/names-numbers-dashboard.html` | Names & Numbers roster dashboard | names-numbers-dashboard.js, names-numbers.css | ✅ Active |
| `/pages/names-numbers.html` | Names & Numbers roster form (team names, numbers, sizes) | names-numbers-controller.js, names-numbers-service.js | ✅ Active |
| `/shared_components/js/names-numbers-controller.js` | Roster form UI controller (tabs, groups, table, OCR, import) | names-numbers-service.js | ✅ Active |
| `/shared_components/js/names-numbers-service.js` | Roster API service (CRUD, Excel parse, OCR) | APP_CONFIG | ✅ Active |
| `/shared_components/js/names-numbers-dashboard.js` | Roster dashboard logic (search, filter, KPIs) | names-numbers-service.js | ✅ Active |
| `/shared_components/css/names-numbers.css` | Names & Numbers shared styles (shop-floor blue theme `--nn-primary #2563eb`; blue page band since 2026-09-04) | CSS variables | ✅ Active |
| `/dashboards/staff-login.html` | Staff authentication login page | — | ✅ Active |
| `/dashboards/quote-management.html` | Quote management dashboard (search, edit, manage quotes). **2026-09-05 review (10 items)**: identity from the SAML session (`/api/crm-session/me` — the page said "Guest" and disabled every delete for everyone), zero `onclick=`/`style=` (data-action delegation, `hidden` attribute), server-side window (`?createdAfter=`; a fruitless search widens to All Time once with a notice), stat tiles are filter buttons, "Pipeline Value" (active + accepted only), real ARIA tabs, generic modal + toasts replace alert/confirm/prompt, actions on one line (rows were 167px), phone header wrap + hidden columns, loaded stamp + 5-min visible-tab refresh, FA 6.4.0. Lock: `tests/unit/quote-management-page.test.js`. | quote-management.css, **sanmar-inbound.css**, js/quote-management.js, sanmar-inbound-today.js | ✅ Active |
| `/dashboards/js/quote-management.js` | Quote management page logic (extracted from inline `<script>` 2026-07-05 — Rule 3). **2026-09-05**: `initIdentity()` (crm-session/me → legacy StaffAuthHelper fallback; `currentUserRole==="admin"` = master delete), `onDelegatedClick()` for every `data-action`, `loadQuotes({silent,all})` with `createdAfter`, `TILE_MATCH`/`setTileFilter`, `openModal()` promise-based confirm/input, `showToast(msg, tone)`, `stampLoaded()`, ARIA tabs (`initTabs`/`paintTabs`). | APP_CONFIG, staff-auth-helper, caspio-date-utils | ✅ Active |
| `/dashboards/css/quote-management.css` | Quote management dashboard styles. **The SanMar Inbound modal's `.sit-*`/`.sl-*` styles moved OUT to `sanmar-inbound.css` on 2026-07-29** — they were only ever loaded here, so the same modal opened from AE Mission Control had no overlay and no print isolation | — | ✅ Active |
| `/dashboards/js/sanmar-inbound-today.js` | "SanMar Inbound Calendar" modal — pick any day → that day's POs (PO+WO, line items w/ color/size, per-box contents, logos), box labels + PDF; month-calendar picker. **Every print/label action re-pulls with `refresh=true` first** (`syncBeforeOutput()`, 2-min window) — a cached payload silently short-shipped the 2026-07-29 report by 4 POs. **Labels render through the shared `box-label-template.js` since 2026-08-04** (rush/follow-on wording lives there too); the modal fails loud if the template is missing. **Any page that loads this MUST also load `sanmar-inbound.css` + `box-label-print.css` + `box-label-template.js`** | APP_CONFIG, **sanmar-inbound.css**, **box-label-template.js + box-label-print.css**, /api/sanmar-orders/inbound-today + /daily-inbound (proxy) | ✅ Active |
| `/dashboards/css/sanmar-inbound.css` | **NEW (2026-07-29)** Self-contained styles for the SanMar Inbound modal + its 6 printable report profiles (`#sit-print-sheet`). Split out of quote-management.css, which ae-mission-control.html never loaded — the modal opened there `position:static` ~3,800px below the fold and, worse, without `body.sit-printing > *:not(#sit-print-sheet){display:none}`, so printing a report or label printed the whole dashboard. Carries its own border-box reset and restates `.modal`/`.modal-content`/`.btn-cancel` at `.modal.sit-modal` specificity, so it depends on no other sheet. **The 8.5×11 label styles (`#sit-label-sheet`/`.sl-*`) moved OUT to `shared_components/css/box-label-print.css` on 2026-08-04** when the label renderer became shared. **Load both wherever sanmar-inbound-today.js loads** | — | ✅ Active |
| `/shared_components/js/box-label-template.js` | **NEW (2026-08-04)** THE box-label renderer — one 8.5×11 per-box label template (`renderLabel`/`printSheet`) + the rush/follow-on badge wording + `METHOD_DARK` colors, shared by the SanMar Inbound modal AND the repack station so the two label surfaces can't drift (the RUSH flag reached one but not the other before this). Input = proxy order records (`/api/sanmar-orders/inbound-today` rows or `/label-data/:id` — same shape). Opts: `printedOn`, `repackedBy`, `qr` | box-label-print.css | ✅ Active |
| `/shared_components/css/box-label-print.css` | **NEW (2026-08-04)** Print styles for the shared box label (`#sit-label-sheet`/`.sit-label`/`.sl-*`): letter-portrait one-page-per-box, `body.sit-label-printing` isolation, mono-laser black-fill rush block, deep-link QR. Moved verbatim out of sanmar-inbound.css. **Load wherever box-label-template.js loads** (quote-management, ae-mission-control, box-labels page, both test harnesses) | — | ✅ Active |
| `/dashboards/digitized-designs.html` | Digitized designs management dashboard | — | ✅ Active |
| `/dashboards/old-designs.html` | Old designs archive search (Caspio embed) | old-designs.css, old-designs.js | ✅ Active |
| `/dashboards/art-hub-coordinator.html` | ~~DELETED~~ Coordinator workflow (removed 2026-03-15) | — | ❌ Deleted |
| `/dashboards/art-hub-ruth.html` | Art hub — Ruth's personalized view | — | ✅ Active |
| `/dashboards/art-hub-steve.html` | Art hub — Steve's personalized view. **Theme = Steve GREEN since 2026-09-04** (was crimson; person colour-coding — Steve green, Ruth purple). ⚠️ **Express Submit Form tab DELETED 2026-08-01** — its Caspio DataPage (`a0e1500073092d827fb74d968d9d`, now disabled in Caspio) wrote STRAIGHT into ArtRequests, skipping `POST /api/artrequests` where both submission notifications live, so those requests reached Steve's queue with nobody notified. Steve never used it (0 submissions); AEs did, and they moved to the AE form 2026-06-17. Submit art at `/ae-dashboard.html#submit`. **Do NOT re-add a submission DataPage** — `tests/unit/art-submit-identity.test.js` fails the build if one appears here or on ae-dashboard, in `.html` *or* `.js`. | art-hub.css, art-hub-steve.js | ✅ Active |
| `/dashboards/gear-publisher.html` | **NEW** 253gear publisher — turns a finished design into a DRAFT product on 253gear.com (options, priced variants, variant→image binding, tags/collections, SEO, alt text) so Steve never opens the Shopify admin. Linked as an external tab from Steve's queue. 🔴 Access is a `Staff_Page_Access` row with `Allowed_Emails` = Erik + art@ and NO roles (an emails-only rule is an exclusive allowlist) — `write_products` is catalogue-wide, so this must not default to any-staff. Deliberately NOT in `ADMIN_DEFAULT_PAGES`, which would lock Steve (role `art`) out. | gear-publisher.css, gear-publisher{,-store,-images}.js, dash-shell.css, art-hub.css, app `/api/gear/*` → proxy `/api/shopify/*` | ✅ Active |
| `/dashboards/css/gear-publisher.css` | **NEW** 253gear publisher styles — step rail, photo grid, job/audit panel. Tokens only, no raw hexes | art-hub.css tokens, dash-shell.css | ✅ Active |
| `/dashboards/js/gear-publisher-store.js` | **NEW** Draft model — state, validation, and `imagePlan()`. PURE (no DOM, no fetch). One image per (Style × Colour); Size never decides an image, which is what lets a shopper click a thumbnail without losing their size | — | ✅ Active |
| `/dashboards/js/gear-publisher-images.js` | **NEW** The photo grid, which IS the variant→image binding surface — each cell carries its (Style, Colour) so binding is correct by construction, never parsed from a filename. Validates ≥2048px long edge + consistent aspect ratio before upload | gear-publisher-store.js, proxy `/api/files/upload` via APP_CONFIG | ✅ Active |
| `/dashboards/js/gear-publisher.js` | **NEW** Wizard controller — screenshot OCR, duplicate check, classify, copy drafting (SSE), build/poll, audit gate, publish. Same-origin `/api/gear/*` so the SAML cookie rides along; every long call carries its own AbortController (fetch-timeout.js aborts at 15s otherwise) | gear-publisher-{store,images}.js, dash-page-helpers.js, app.config.js | ✅ Active |
| `/dashboards/design-queue.html` | **NEW** Design Queue — tells Steve which South Sound subject to draw next, and why. Ranks candidate subjects on TWO axes: vacancy (is the seat empty — is the best current Google result a Facebook thread or nothing) and living memory. Both are needed: Hotel Hesperides scores vacancy 17 but nostalgia 6 because it closed in 1923, and Never Never Land scores nostalgia 19 but vacancy 3 because Threadless and a local rival already sell it. Linked as an external tab from Steve's Art Hub, beside the publisher — the publisher answers HOW to ship a design, this answers WHICH one. | design-queue.css, design-queue.js, `/dashboards/data/design-queue.json`, dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/css/design-queue.css` | **NEW** Design Queue styles — thesis block, verdict chips, subject rows. All colour/shadow/radius via art-hub.css tokens; nothing hardcoded. | dash-shell.css, art-hub.css | ✅ Active |
| `/dashboards/js/design-queue.js` | **NEW** Design Queue controller — reads the static JSON, applies the two-axis verdict (draw / research first / skip), renders and filters. Uses textContent only, never innerHTML with data. | `/dashboards/data/design-queue.json`, DashPage | ✅ Active |
| `/dashboards/js/design-queue-briefs.js` | **NEW** "Draw these" — the working end of the Design Queue: exact shirt text, what to draw, and reference links per subject. 🔴 References are LINKS, never embedded images — every photograph belongs to a Facebook poster or the Tacoma Public Library, and rehosting would break the same no-reproduction rule Steve's brief sets. A `confidence` chip (confirmed / memory) stays visible so an unsourced subject can't be drawn from imagination. Briefs are hand-maintained in `253gear-growth/data/design-briefs.json`. | `/dashboards/data/design-queue.json` | ✅ Active |
| `/dashboards/js/design-queue-metrics.js` | **NEW** "253gear.com right now" panel — live Shopify store metrics on the Design Queue. 🔴 A block we lack the scope to read renders as PROSE naming the missing scope, never as a number: "0 visitors" is indistinguishable from a measurement and somebody will act on it. Catalogue health works today; sessions need `read_reports` (+ Level 2 protected customer data) and sales need `read_orders`. | `/api/gear/store-metrics` | ✅ Active |
| `/dashboards/data/design-queue.json` | **NEW** Scored subject data behind the Design Queue. 🔴 Deliberately a STATIC FILE, not Caspio: that account is metered per call and has already taken a $358 overage, so a page Steve opens daily must not spend quota to read ~70 rows that change monthly. Regenerated by `253gear-growth/scripts/build-design-queue.js` and shipped with the deploy. | generated — do not hand-edit | ✅ Active |
| `/dashboards/bradley-transfers.html` | Bradley's Supacolor transfer queue (heat-transfer orders to subcontractor). **2026-09-05 review (11 items)**: loads `config/app.config.js` (API base no longer hardcoded), chips are pressed-state buttons, error state + Retry, labelled dialog, no inline style/onerror | bradley-transfers.js, bradley-transfers.css, art-hub.css, caspio-pricing-proxy /api/transfer-orders | ✅ Active |
| `/dashboards/production-shifts.html` | Production team daily shift schedule + WA labor rules (clock-in/out, breaks, lunch). **2026-09-05: production React builds** (unpkg, integrity-pinned); JSX still transpiled in-browser by Babel standalone — the one console warning on this page is Babel's own and is expected. | production-shifts/{styles.css, app.jsx, data.js}, React 18 + Babel CDN | ✅ Active |
| `/shared_components/css/staff-schedules.css` | Canonical production schedule owner: Public Sans, scoped tokens, timeline/table, work rules and one-page department schedules. Replaces retired production-shifts/styles.css. | — | ✅ Active |
| `/dashboards/production-shifts/app.jsx` | **NEW** Production Shifts React app (Header, DaySummary, MasterTable, Timeline, WorkRules, LaborRules) | data.js (window.NWCA_SCHEDULE + NWCA_HELPERS) | ✅ Active |
| `/dashboards/production-shifts/data.js` | **NEW** Production Shifts employee data + time helpers (window.NWCA_SCHEDULE, NWCA_HELPERS) | — | ✅ Active |
| `/forms/NWCA-Meal-Period-Waiver.pdf` | **NEW** Voluntary Meal Period Waiver — employees download, fill out, return to Bradley Wright | served via `/forms` static route, linked from production-shifts dashboard | ✅ Active |
| `/forms/policies/*` (9 files) | **NEW** SweetProcess-migrated policy attachments — screen-print & Shopify workflow diagrams, Providing Good Artwork guide, Polycom + SoundPoint IP 650 phone manuals, DTG ops manual + ink order form, credit-card auth form, new-clerk receiving checklist | served via `/forms` static route; embedded/linked from Policies Hub policies (Caspio) | ✅ Active |
| `/dashboards/js/bradley-transfers.js` | Transfer queue controller — 60s poll (paused when hidden), filter/sort, delete + link-Supacolor modals. **2026-09-05**: `API_BASE` from APP_CONFIG, `renderLoadError()` + Retry, `syncChips()` (chip ↔ dropdown ↔ rush ↔ clear), `data-onerror` thumb fallback, keyboard cards, delete modal focus return + Esc, identity from `/api/crm-session/me` for the delete audit | /api/transfer-orders, /api/transfer-orders/stats | ✅ Active |
| `/dashboards/css/bradley-transfers.css` | **NEW** Transfer queue styles (navy theme, rush pulse, status chips, modal) | — | ✅ Active |
| `/pages/transfer-detail.html` | **NEW** Single-transfer detail page with status transition buttons (Mark Ordered / Add PO / Ship / Receive / Cancel / Rush) + activity timeline | transfer-detail.js, transfer-detail.css, bradley-transfers.css, /api/transfer-orders/:id | ✅ Active |
| `/pages/js/transfer-detail.js` | **NEW** Transfer detail controller — fetches record + notes, status machine, 6 modals, localStorage-based user identity | /api/transfer-orders/:id (+ /status, /rush, /notes) | ✅ Active |
| `/pages/css/transfer-detail.css` | **NEW** Transfer detail page styles (header card, 2-col grid, timeline, action buttons) | — | ✅ Active |
| `/shared_components/js/transfer-actions-shared.js` | **NEW** Box file picker modal + `window.TransferActions.openSendModal()` — shared by mockup-detail, art-request-detail, Steve's dashboard | /api/box/folder-files, /api/box/shared-link, /api/transfer-orders, transfer-actions.css | ✅ Active |
| `/shared_components/css/transfer-actions.css` | **NEW** Self-contained modal styling for Send to Supacolor picker (navy theme, toasts) | — | ✅ Active |
| `/dashboards/js/steve-send-supacolor.js` | **NEW** Steve's dashboard header button handler — prompts for design #, looks up ArtRequest, opens shared modal | transfer-actions-shared.js, /api/artrequests | ✅ Active |
| `/dashboards/bradley-screenprint.html` | **2026-09-05 review (11 items — same set as the transfers twin, plus: the page had NO h1)**. Bradley's screen-print queue (Method='Screen Print' transfers to L&P Printing — no upstream API) | bradley-screenprint.js, bradley-transfers.css (shared), art-hub.css, /api/transfer-orders?method=Screen%20Print | ✅ Active |
| `/dashboards/js/bradley-screenprint.js` | **NEW** Screen-print queue controller — mirrors bradley-transfers.js, strips Supacolor-only chrome (auto-link, stale-link, job navigation) | /api/transfer-orders?method=Screen%20Print | ✅ Active |
| `/dashboards/js/steve-send-screenprint.js` | **NEW** Steve's "Send to Screen Print" button handler — opens shared modal in `method:'Screen Print'` mode (L&P Printing vendor) | transfer-actions-shared.js | ✅ Active |
| `/shared_components/css/art-hub.css` | **NEW** Shared art hub dashboard styles (CSS custom props for theming) | — | ✅ Active |
| `/shared_components/js/art-actions-shared.js` | **NEW** Shared art action modals (Log Time, Mark Complete, Send Mockup) — used by art-hub-steve.js + art-request-detail.js | art-hub.css, caspio-proxy API, EmailJS | ✅ Active |
| `/shared_components/js/art-hub-steve.js` | Steve's gallery card processing + notes panel — delegates modals to art-actions-shared.js | art-hub.css, art-actions-shared.js, caspio-proxy API | ✅ Active |
| `/shared_components/js/art-hub-ae.js` | AE gallery card processing (maroon theme, View Details only) | art-hub.css, ae-dashboard.html | ✅ Active |
| `/shared_components/js/art-ae.js` | **2026-09-04**: dispatches `ae:counts` after every render, exports `refresh`, `getNeedsReview`, `isLoaded`, `dateCutoff`; load failure reads as a sentence with Try again. AE Steve Mockups tab — API-driven art request gallery (replaces Caspio DataPage) | art-hub.css, ae-dashboard.html | ✅ Active |
| `/shared_components/js/ae-dashboard.js` | **2026-09-04**: listens for `ae:counts` (galleries) and paints the Steve/Ruth nav badges + the More-menu Review badge from ONE definition; Review tab renders the gallery's own needs-review rows; roving-tabindex arrow keys on both nav tiers; lightbox/review states use `hidden`; the dead Caspio note-iframe modals are gone. **NEW** AE dashboard: tab switching, modals, dropdown, notification polling + toasts | ae-dashboard.html | ✅ Active |
| `/dashboards/css/taneisha-crm.css` | ⚠️ DEPRECATED - use rep-crm.css | - | ⚠️ Legacy |
| `/dashboards/js/taneisha-crm.js` | ⚠️ DEPRECATED - use rep-crm.js | - | ⚠️ Legacy |

### Staff Dashboard Legacy JS (still loaded by V3 as bridge globals)
| File | Purpose | Status |
|------|---------|--------|
| `/shared_components/js/staff-dashboard-service.js` | ShopWorks ManageOrders API integration. **⚠️ DEAD as of 2026-07-25** — it existed solely as the garment-tracker bridge; the Embroidery Bonus card that replaced that tracker calls the API directly, so no HTML loads this file any more. Left on disk (flagged, not auto-deleted per CLAUDE.md dead-code policy). Its ~1,500 lines of garment sync logic and hardcoded proxy URL (Rule #7 debt) are no longer shipped to any page. | 🗑️ Dead — no `<script>` references |
| `/shared_components/js/staff-dashboard-init.js` | Initialization, widget toggles, auto-refresh | ✅ Active |

### Staff Dashboard V3 Refactor (in development on `refactor/staff-dashboard-v3` branch — 2026-05-12)
Polish + code-quality pass. Plan: `~/.claude/plans/this-is-a-big-parsed-unicorn.md`. Built in parallel at `/staff-dashboard-v3/`; cuts over once 90-day soak passes. v2 will move to `/staff-dashboard-v2.html` at cutover.
| File | Purpose | Dependencies | Status |
|------|---------|--------------|--------|
| `/shared_components/css/staff-dashboard/tokens.css` | **NEW (v3)** Design tokens — oklch + NW green, space/motion/z-index/font-size/focus-ring scales, semantic alias layer. Wrapped in `@layer tokens`. | — | 🚧 In dev |
| `/shared_components/css/staff-dashboard/phase1-widgets.css` | **Phase 1 widgets (2026-07-20)** Pride Wall (`.pw-`) · My Stuff (`.ms-`) · **2026 Goal chip** (`.goal-chip`, header-scoped to out-specify the big `.sales-goal-banner` rules; keeps #goal* IDs + is-loading so sales-goal-controller drives it unchanged). Loaded UNLAYERED after dashboard-v3-theme.css; all values from tokens. Reduced-motion + responsive. **Win Bell (`.wb-`) removed 2026-07-23.** | tokens.css | ✅ Active |
| `/shared_components/css/staff-dashboard/command-palette.css` | **Phase 2 (2026-07-20; tools-only + click-through close 2026-09-03)** Hero search bar (`.hero-search__*`, 52px, focus-within accent ring, `.hero-search__clear` × button) + anchored results panel (`.cp-panel` absolute under the bar, drops downward; `[hidden]` forced `display:none` — its own `display:flex` used to beat the attribute and drew an empty status strip under the search box). No backdrop, no toast since 2026-09-03. Unlayered, token-driven. | tokens.css | ✅ Active |
| `/shared_components/js/staff-dashboard/core/dashboard-endpoints.js` | **NEW (v3)** Endpoint registry — single source of truth for every URL the dashboard hits. Reads BASE from `window.APP_CONFIG.API.BASE_URL` (Rule #7). | window.APP_CONFIG | 🚧 In dev |
| `/shared_components/js/staff-dashboard/core/dashboard-fetch.js` | **NEW (v3)** Uniform fetch wrapper. Always throws on error (Rule #4 — no silent fallbacks), GET dedup, 30s timeout, 429 jittered retry. Exports `dashboardFetch`, `dashboardFetchJson`, `DashboardApiError`. | — | 🚧 In dev |
| `/shared_components/js/staff-dashboard/core/dashboard-store.js` | **NEW (v3)** Versioned state store — namespaced `nwca-dash:*` keys, per-key TTL + version stamping, safe corrupt-entry recovery. Replaces 10 scattered localStorage/sessionStorage keys. | — | 🚧 In dev |
| `/shared_components/js/staff-dashboard/core/dashboard-events.js` | **NEW (v3)** Delegated event router — replaces 22+ inline `onclick=` (Rule #3). Markup: `data-action="metrics:refresh"`. Listen: `events.register('metrics:refresh', fn)`. Auto-installs on import. | — | 🚧 In dev |
| `/shared_components/js/staff-dashboard/core/dashboard-errors.js` | **NEW (v3)** Centralized error UI — `showApiError(area, error, { onRetry })` replaces 4 inline error renderers. Retry buttons routed through the event delegator. | dashboard-events.js, dashboard-fetch.js, dashboard-ui-utils.js | 🚧 In dev |
| `/shared_components/js/staff-dashboard/core/dashboard-ui-utils.js` | **v3** Shared UI helpers — `escapeHtml`, `formatMoney`, `formatPercent`, `formatRelativeTime`, `formatDateRange`, `sparklineSvg`, `debounce`, etc. **2026-09-04**: `toLocalDate()` parses "YYYY-MM-DD" as a LOCAL calendar day (`new Date('2026-09-04')` is UTC midnight = Sep 3 evening Pacific — the revenue range showed a day early); `ANNUAL_GOAL` constant REMOVED (→ company-goal-service). | — | ✅ Active |
| `/shared_components/css/staff-dashboard/base.css` | **NEW (v3)** Reset, body, typography, layout shell, sidebar, header. Wrapped in `@layer base`. Includes responsive sidebar overlay for tablet (<1100px) + mobile (<768px). | tokens.css | 🚧 In dev |
| `/shared_components/js/staff-dashboard/widgets/dashboard-modal.js` | **NEW (v3)** `<dashboard-modal>` custom element — owns scroll-lock, focus-trap, escape-to-close, backdrop-click, ARIA dialog. Replaces duplicated logic in staff-directory + gap-report modals. | — | 🚧 In dev |
| `/shared_components/js/staff-dashboard/services/employees-service.js` | **v3** Roster service — fetches `/api/staff/employees` once via `load()` (async, throws visibly on failure), then `all()`, `active()`, `upcomingBirthdays(N)`, `upcomingAnniversaries(N)`, `search(q)` are sync over the loaded data. Roster was hardcoded here until 2026-08-27 (anonymously served — PII); data now in lib/staff-roster.js behind requireStaff. | dashboard-fetch.js, /api/staff/employees | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/sidebar-controller.js` | **DELETED 2026-09-04** — unmounted since the 2026-09-03 Workspaces restructure (the sidebar it drove is gone), nothing imported it. Its `sidebarSections` store key went with it | — | ❌ Deleted |
| `/shared_components/js/staff-dashboard/controllers/workspace-controller.js` | **NEW (2026-09-03, Workspaces)** The role-based tabs. One `section.ws-panel[data-ws]` per tab; the active one gets `is-on` (panels are display:none otherwise — the `hidden` ATTRIBUTE is reserved for nav-access gating and this controller never touches it). Default tab from `/api/crm-session/me` permissions (`admin` checked FIRST because the admin fan-out includes accountant/house/rep tags): sales→Sales, art→Art, production/shipping→Production, accountant→Office, admin→Sales, none→Everything; remembered per browser AND per email (`nwca-dash:workspace` store slot); `#ws=<tab>` hash wins. Generates the Everything tab from the other panels (union of `a.ws-link`, deduped by href, grouped by card `data-cat`, live filter), skipping gated nodes still hidden. Keyboard: arrows/Home/End; in-page `a[href^="#ws="]` links switch tabs. | dashboard-store.js, dashboard-ui-utils.js, nav-access-controller.js (permissions promise), auth-controller.js (email) | ✅ Active |
| `/shared_components/css/staff-dashboard/workspaces.css` | **2026-09-04**: ≤1100px rows wrap their description under the name (was display:none); Everything rows carry descriptions; phone header/tab-strip rules; legend + per-client bundle families removed. **NEW (2026-09-03, Workspaces)** Tab strip, `.ws-panel`/`.ws-card`, tool rows (`.ws-row`: family mark + name + who + description), tiles (`.ws-tile`), the job-family colors (one per family = the destination page's own header color: CRM navy+gold, purchasing slate blue, mockups purple = Ruth, **steve green `.f-steve` = Steve's Art Queue + Design Queue (person colour-coding, 2026-09-04)**, art requests crimson = the AE dashboard, shop floor blue, photos deep green, archive amber…) and the bespoke tiles that mirror their page (Shop Menu paper/wine/Bodoni, Supacolor mark, DST Studio dark+teal, Contract pink Fraunces, storefront cream Bricolage, My Accounts gold/silver/bronze, Design Vault, Photo Capture app icon). Loaded LAST and UNLAYERED on purpose (the only position that beats the unlayered theme + patch); everything `ws-` prefixed. | tokens.css | ✅ Active |
| `/dashboards/company-numbers.html` + `/dashboards/js/company-numbers.js` + `/dashboards/css/company-numbers.css` | **2026-09-04 second pass (7 items)**: "Production Turnaround" (static predictor) RETIRED → LIVE "Production Due" from the Past Due forwarder; team rows link to nika-crm / taneisha-crm / house-accounts; Revenue card carries the archive YTD chip (`#revenueYtd`); art widget `> 30 days` zombie bucket; ONE header "Refresh now" (`#cn-refresh-all`, bypasses caches) replaced 4 per-card buttons; `@media print` sheet; 1600px + three-up metrics from 1500px. **2026-09-04 review (15 items, all shipped)**: every live card re-reads on a 5-minute tick WHILE THE TAB IS VISIBLE (catch-up on return) and carries an `Updated h:mm` / `Failed h:mm` stamp (`.cn-stamp[data-stamp]`), header shows the last tick; entry BUNDLED by `scripts/build.js` ENTRY_BUNDLES (relative imports — was the hashed entry + 20 no-store source modules); "Money Collected" → "Online Payments (Stripe)"; accepted-quotes column says "last 30 days"; sample rows carry tel:/mailto: actions from ManageOrders `ContactPhone/ContactEmail`; Production card: no fake refresh button, footer prints `dataThrough` + `updatedAt`, no range tooltips; team-push title from the API's quarter; `% of goal` moved to the tooltip (one denominator per row), DEAD folds into House, full rep names; art-aging card styled by `.aa-*` classes here (no inline styles); phone header stacks, uppercase titles don't break mid-word. Lock: `tests/unit/company-numbers-page.test.js`. Company Numbers (2026-09-03) — the eight report widgets that left the staff dashboard (Orders Inbox, Online Payments, Sample Follow-ups, Revenue, Team Performance, Art Requests aging, Production Turnaround [static estimate], quarterly Team Push), same controllers, same dark stylesheet chain (NOT art-hub/dash-shell). Opened from the dashboard's goal chip and Admin → Analysis. Admin-only by default (`ADMIN_DEFAULT_PAGES`); widen with a `Staff_Page_Access` row. | orders-inbox/metrics/team-performance/production/embroidery-bonus controllers, company-goal-service.js, art-aging-widget.js, caspio-date-utils.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/orders-inbox-controller.js` | **NEW (2026-07-06)** Orders Inbox + Money Collected + Sample Follow-ups zones (replaced announcements) — quote_sessions last-7-days (paid web orders SAM/CAP/DTG/3DT, Accepted-unpaid, ShopWorks-Failed pinned loudly), Order_Payments totals+recent, sample orders w/o later order = rep call list. Rows link to /quote/{id}. | dashboard-endpoints.js (createdAfter param), dashboard-fetch.js, /api/order-payments/recent, /api/mo/orders | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/sales-goal-controller.js` | **v3** Header goal chip — progress fill + % + "$X / $3M" (aria-valuenow kept current). **2026-09-04: the goal comes from Caspio `CO-ANNUAL-GOAL` via company-goal-service** (was the `ANNUAL_GOAL` constant); when the row can't be read the chip uses the built-in default AND shows it — "$3M ⚠", amber, `.is-goal-fallback`, tooltip (never a silent constant). Pace/days-left/projected-EOY code deleted 2026-08-27. | company-goal-service.js, dashboard-ui-utils.js | ✅ Active |
| `/shared_components/js/staff-dashboard/services/company-goal-service.js` | **NEW (2026-09-04)** The annual sales goal, read from Caspio Service_Codes row `CO-ANNUAL-GOAL` (SellPrice = dollars) through the same-origin `GET /api/staff/service-codes?code=` relay. `fetchAnnualGoal()` → `{goal, source:'caspio'|'fallback'}` — never throws; a failed read returns the built-in default with `source:'fallback'` (NOT cached) and every caller renders `fallbackWarning()` (CLAUDE.md: a hardcoded number only as a fallback WITH a visible warning). `formatGoalCompact()` → "$3M". Edit the row each January — no deploy. | dashboard-fetch.js, server.js `/api/staff/service-codes` | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/celebrations-controller.js` | **NEW (v3, Phase 4.5)** Combined "Team" widget — single dropdown for birthdays + anniversaries (replaces 3 separate pills) + staff-directory modal with live search input. Uses `<dashboard-modal>`. | dashboard-events.js, employees-service.js, dashboard-modal.js | 🚧 In dev |
| `/shared_components/js/staff-dashboard/services/shopworks-service.js` | **v3** Wraps ManageOrders endpoints — `loadRevenueWindow(days)`, `loadYearOverYear(days)` (returns `lastYearError` on a failed comparison so the UI can warn visibly). Uses `dashboardFetch` (no silent fallbacks). `loadYtdSubset`/`getYTDRange` deleted 2026-08-27 (zero callers — YTD comes from caspio-archive-service). | dashboard-fetch.js, dashboard-endpoints.js, dashboard-store.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/metrics-controller.js` | **NEW (v3, Phase 4.4)** Revenue card with sparkline (220×32 inline SVG), AOV + order count, YoY badge. Reads `defaultRevenuePeriod` from Tweaks. **Does not** touch `#salesTeamList` — that's owned by team-performance-controller. 60-day window deliberately doesn't feed sales-goal banner (Rule #4 — partial-year would skew pace math). | shopworks-service.js, dashboard-errors.js, dashboard-events.js | 🚧 In dev |
| `/shared_components/js/staff-dashboard/controllers/auth-controller.js` | **v3** Identity via first-party `GET /api/crm-session/me` (2026-08-27 — retired the 8s-polled third-party Caspio DataPage embed + caspio-isolation.js with it). Applies welcome name, mirrors to legacy sessionStorage keys (nwca_user_name/email/role, caspioUser) for downstream pages, fires `caspioUserReady`. | dashboard-store.js, /api/crm-session/me | ✅ Active |
| `/shared_components/js/staff-dashboard/widgets/tweaks-fab.js` | **DELETED 2026-09-04** (dashboard review) — the floating Tweaks button only offered density and a "default revenue period" for reports that left the page on 2026-09-03; theme is fixed by the `<html>` attributes (dark · green · comfy). `metrics-controller` (Company Numbers) still reads the `tweaks` store key, which stays | — | ❌ Deleted |
| `/shared_components/js/staff-dashboard/core/dashboard-app.js` | **v3** Single ES module entry point. Loaded as `<script type="module">` from `/staff-dashboard-v3/index.html`. Bootstraps tweaks → auth → nav-access → sidebar/tools/goal/celebrations/production → async widget controllers, then a 5-min metrics refresh interval. | All controllers + tweaks-fab + dashboard-modal | ✅ Active |
| `/shared_components/css/staff-dashboard/components.css` | **NEW (v3)** All reusable components — sales-goal pill (Phase 4.1), announcements, tool categories + buttons, metrics cards (revenue + rep cards + sparkline), Team widget dropdown (Phase 4.5), staff modal (uses `<dashboard-modal>`), production predictor, garment tracker, error banners, skeleton loaders, tweaks FAB + panel. Wrapped in `@layer components`. Uses CSS nesting + container queries. | tokens.css, base.css | 🚧 In dev |
| `/shared_components/css/staff-dashboard/utilities.css` | **NEW (v3)** Single-purpose helpers — `.stack`, `.cluster`, `.text-*`, `.surface-*`, `.visually-hidden`, `.is-hidden`, `.hide-mobile`/`.show-mobile-only`, focus + spacing + width utilities. Wrapped in `@layer utilities` so direct usage beats component defaults. | tokens.css | 🚧 In dev |
| `/shared_components/js/staff-dashboard/controllers/production-controller.js` | **REBUILT 2026-09-04 — "Production Due"**: LIVE unshipped ShopWorks orders past their requested-ship date or due within 7 days, from `/api/crm-proxy/ae-dashboard/due-dates-all?days=30` (the Past Due Orders report's forwarder — ORDER_ODBC + PurchaseOrders). Four tiles (past due / at risk / past due with no PO / due this week on track), the 6 most urgent rows with blanks status, footer "as of + orders scanned" + link to the full report. Visible error + Retry via `showApiError('production')`. Replaced the "Production Turnaround" predictor — its static `production-schedule-stats.js` / `-predictor.js` (completions through Nov 2025, compiled 2026-01-10) were DELETED the same day; production logging stopped 2026-05-20 so they could never go live. | dashboard-fetch.js, dashboard-errors.js | ✅ Active |
| `/shared_components/js/staff-dashboard/services/caspio-archive-service.js` | **v3** Reads per-rep YTD totals via the same-origin requireStaff relay `/api/staff/daily-sales-by-rep-ytd?year=N` (own `ytdArchiveCache` store slot). Single source of truth for archived per-rep YTD data. | dashboard-fetch.js, dashboard-endpoints.js, dashboard-store.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/team-performance-controller.js` | **v3** Renders per-rep YTD revenue cards from the Caspio archive. Name normalization (Ruth↔Ruthie; House = House-Legacy + Erik/Jim/Dyonii/Adriyella + **DEAD**, the ShopWorks dead-accounts placeholder, since 2026-09-04). **One denominator per row (2026-09-04)**: bar AND text = share of team total; the company-goal share lives in the row tooltip, from company-goal-service (fallback → visible `.rep-goal-note`). Pushes total YTD to the goal chip. `refreshTeamPerformance()` for the Company Numbers tick. | caspio-archive-service.js, company-goal-service.js, sales-goal-controller.js, dashboard-events.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/embroidery-bonus-controller.js` | **NEW 2026-07-25** Renders the **Q3 Team Push** strip — company-wide **embroidery** for the quarter against the shared targets (basis narrowed from all order types 2026-07-26; scoping it to bonus-eligible accounts instead would have re-paid the reps' own work, since company eligible embroidery barely exceeds their combined baselines). 🔒 **Shows NO compensation**: every employee opens the staff dashboard, so per-rep bonus dollars live on each rep's own Mission Control instead. Calls `/api/crm-proxy/embroidery-bonus/team`, which forces `scope=team` server-side and returns `reps: {}` — the privacy boundary is in the backend, not this render. **Replaced `garment-tracker-controller.js`** (removed same day: that spiff paid per-garment off a style list and totalled ~$128/~$54 for Q2). No legacy `window.StaffDashboardService` bridge; every number including the targets comes from the API, never a hardcoded constant (the old controller's `BONUS_GOAL = 500`). Loud banner when the backend falls back to built-in targets. | dashboard-events.js, dashboard-errors.js, dashboard-endpoints.js, dashboard-ui-utils.js, `/api/crm-proxy/embroidery-bonus/team` | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/pride-wall-controller.js` | **Phase 1 "alive" layer (2026-07-20)** Pride Wall — ambient 6-tile strip of finished-product photos from `/api/staff/finished-photos/library` (published first, fallback all), one tile crossfades every 7s (no on-screen duplicates), hourly pool refresh, calm visible error state. **2026-07-29: the strip's markup became a `<details>` so staff can fold away its ~185px; this controller was NOT changed — it only sets `.hidden` and fills `#pwTrack`, both of which behave the same. Collapse state is owned by tool-grid-controller.js.** | /api/staff/finished-photos/library, dashboard-ui-utils.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/my-stuff-controller.js` | **Phase 1 "personal" layer (2026-07-20)** My Stuff row — per-browser pinned + recent tools (localStorage `nwca-mystuff-v1`), learned from delegated clicks on `.tool-btn`/`.nav-link`; ☆/★ pin toggle via dashboard-events. Purely additive — never hides or moves the tool grid. | dashboard-events.js, dashboard-ui-utils.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/nav-access-controller.js` | **NEW (2026-07-28)** Role-aware gating — reveals `[data-requires-role]` blocks (since 2026-09-03: the Admin tab + Admin panel, the per-rep My Accounts tiles, the House Accounts rows) only when `GET /api/crm-session/me` reports a matching permission. REMOVES the node otherwise, so admin tools also drop out of the Ctrl+K registry, which harvests from the live DOM. Fails CLOSED and logs loudly. **UX layer only — the real gate is `lib/page-access.js` + Staff_Page_Access.** | /api/crm-session/me | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/tool-grid-controller.js` | **NEW (2026-07-29 IA reorg)** No network. Persists open/closed state for any `details[data-collapse-key]` — since 2026-09-03 the Pride Wall and the "Go straight to a builder" / "More pricing" folds — into the `widgetCollapse` store slot. Its count-badge job (`.tool-category-count`) is dormant: the Quick Access grid it counted was retired with the Workspaces layout. | dashboard-store.js | ✅ Active |
| `/shared_components/js/staff-dashboard/controllers/command-palette-controller.js` | **Tool search (2026-07-20; TOOLS-ONLY since 2026-09-03 — Erik)** The hero bar / Ctrl+K. Harvests every `a.ws-link` on every workspace tab (+ My Stuff) from the live DOM, so it can never drift; gated-hidden nodes and the generated Everything list are skipped; one result per href. Results show tab · card; Enter opens the top match; no match → one row that opens the Everything tab with the query in its filter (`showWorkspace` + `#wsEveryFilter`). Closes on Escape, the × (`data-action="palette:close"`), picking a result, or a `pointerdown` anywhere outside `.hero-search-wrap` — capture phase, no preventDefault, so the click still lands (the old fixed backdrop swallowed it and a tab switch took two clicks). The customer/order/quote/design fan-out (`/api/staff/command-search`) was removed: a proxy call per keystroke, and each of those has a better search on its own page. | dashboard-events.js, dashboard-ui-utils.js, workspace-controller.js | ✅ Active |

### Dashboard Reports
| File | Purpose | Status |
|------|---------|--------|
| `/dashboards/reports/price-audit-report.html` | **NEW** Price audit report — SW 2025 vs 2026 pricing comparison for rep training (2026-02-15) | ✅ Active |
| `/dashboards/reports/price-audit-report.css` | **NEW** Styles for price audit report page (2026-02-15) | ✅ Active |

### Employee Bundle Pages
| File | Purpose | Dependencies | Status |
|------|---------|--------------|--------|
| `/dashboards/DrainPro-Bundle.html` | Drain-Pro employee bundle labels | Caspio datapage | ✅ Active |
| `/employee-bundles/streich-bros-bundle.html` | Streich Bros employee bundle labels | Caspio datapage | ✅ Active |
| `/employee-bundles/wcttr-bundle.html` | WCTTR (West Coast Truck and Trailer Repair) employee bundle labels | Caspio datapage | ✅ Active |

[Back to the registry index](../../ACTIVE_FILES.md)
